# TODO — 이어서 작업 (갱신: 2026-07-23 오후)

## 🟢 7/23 (목) 오후 — 사장님 재설치 실사용 피드백 5건 (커밋 5, HEAD `0af2665`)

**커밋(모두 푸시)**: 7288e42(홍길동) · 66eb28c(P11) · 0b0368a(P10-fix·홈) · 0af2665(capture --click). +docs.
사장님이 오전 재빌드판(13:10)을 재설치 → 실사용하며 준 피드백. 코워크 정리 지시 반영.

1. **확인① 작성자 "홍길동"** — L1100-04 작성자 칸에 '홍길동' 표시. = `company_profile.defaultAuthor`
   stub(seed)이 0097 이전 빌드에서 주입된 잔재. **근본 제거**: seed defaultAuthor='' + `0099`
   (기존 DB '홍길동'→빈값) + 엑셀뷰 EditCell auto=disabled(입력뷰와 일관). "가짜보다 빈칸".
   **dev 실증**: 미선택 시 작성자 칸 "로그인 사용자" placeholder(빈값), 사용자 선택 시 그 이름 자동채움.
2. **P11 뒤로가기** — 깊이 들어가면(심사대응→SQ준비도→양식) 되돌아갈 방법 없던 문제.
   `uiStore` 히스토리 스택(setPage push·goBack 복원·상한20, URL히스토리 호환) + `PAGE_LABELS` +
   PageHeader 좌측 [← 직전화면명] 버튼 + FormCanvas 툴바 [← 뒤로] + App 전역 Alt+←·마우스4번째 +
   `navBack` 가드(양식 작성 중 미저장=formStore.dirty 시 confirm). **실증**: ← 홈 / ← 뒤로 표시.
3. **홈 긴 업무명 말줄임(P11 소항목)** — 좁은 열에서 세로 꺾임 → TaskRow title `line-clamp-2`(2줄)+툴팁.
   **실증**: "스포트 팁 카운터↔교체대장 정합 확인 연…" 2줄.
4. **P10-fix 문서작성 비율** — "허전한 비율"→벤치마크 밀도. DocBrowseView: 우측 고정 칼럼 그리드
   (정기의무·예시·작성본·최근작성·상태, 값없으면 '—')+칼럼 헤더행 · 행 컴팩트(py-2) · 숫자밴드
   슬림(가로형·높이절반) · 검색 아이콘 여유 · 내 팀=팀뱃지 아웃라인 통합. **실증**: 밀도 확보.
5. **확인② 제목 깨짐** — TRY-OUT 엑셀뷰 제목부 셀 병합/괘선 어긋남. **엑셀뷰 캡처가
   Page.captureScreenshot 타임아웃(큰 시트 렌더)** → 육안확인 보류. **재설치 실물/별도 조사로 이월**.

**덤**: 금형 의무(0098)가 홈 생산팀 보드에 '설비 PM'·'금형 점검' 나란히 표출 확인. capture.mjs `--click`
확장(뷰 전환 캡처). **⚠️ 설치판 재빌드 = 7/23 13:58** (오늘 오전+오후 전부·0096~0099 번들·.env 0건) —
재설치 대기. 재설치 후 확인: ①작성자 실명(미선택=빈칸) ②P11 뒤로가기(← 버튼·Alt+←·마우스백)
③문서작성 밀도 ④홈 말줄임 ⑤**확인②=L1100-04 엑셀뷰 제목부 병합/괘선** ⑥금형 의무.

**▶ 남은 것**: ①**확인② 제목 깨짐**(재설치 엑셀뷰 실물 판단 — 경미/구조적) ②**웹 전환 방향**
(사내서버 vs 클라우드 = 사장님 유일 남은 결정) ③P9 확장(셀맵 나머지 form_fields 크래프팅)
④capture 엑셀뷰 캡처 타임아웃 개선(clip 지정 등).

---

## 🟢 7/23 (목) 오전 — 순차 진행: 경계3건·CDP캡처·P10확인·작성자auto·금형PM (커밋 4)

**커밋(모두 푸시, HEAD `b5aeb35`, 워킹트리 클린)**: 6f3634e(0096) · 13f9a28(CDP) · 031b1c6(0097) · b5aeb35(0098).
사장님 아침 지시 = "순차적으로 진행". 남은 백로그를 순서대로 처리.

1. **경계 3건 → 0096** (사장님 확정: 전부 frame). 측정시스템 평가(J1100-01/06)·연간설비
   보전계획 및 실적표(L1100-23)·결과는(L1100-06) = 제목/구획/서술 라벨이라 fact→frame.
   복사본 검증: fact−4/frame+4/total불변·멱등·잔여0.
2. **CDP 캡처 파이프라인 재구축** (검수 백본 복원, 어제 미실시분).
   - `scripts/capture.mjs`: node22 내장 WebSocket/fetch 로 최소 CDP(외부 의존성 0).
     `Page.captureScreenshot`(DPI 무관·가려진 창 정상·PrintWindow 오탐 원천제거). 기본프리셋/단일(--page)/config.
   - `main/index.ts`: dev 한정 `--remote-debugging-port=9222`(127.0.0.1, 설치판 미개방).
   - `main.tsx`: dev 한정 `window.__cap` 네비 헬퍼(zustand uiStore 래핑, 결정론 페이지 전환).
   - `.gitignore`: `captures/`(검수 이미지=재생성). 실증 6장(home·doc-browse·iatf-dashboard·form-builder 등).
   - **사용법**: (설치판 닫고) `npm run dev` → `node scripts/capture.mjs --page <id> [--form <code>] --out <dir>`.
3. **P10 실물 확인** (재설치 후 첫 검증): doc-browse 새 화면 정상 — 숫자밴드(301/167/17/31)·검색·
   책임팀/규정/정렬 필터·위계 행리스트·예시배지·[규정별 보기] 토글. CDP 캡처로 확인.
4. **작성자 auto 자동채움 스윕 → 0097** (사장님 방침: 활성 사용자·미선택 빈값).
   - 결재란 '작성'·'작성자'(text) 49건 → `type=auto`+`frame`+placeholder'로그인 사용자'. 반쪽 auto 4건
     (검사자·작업자·확인자·담당자) 표식 보정. 검토/승인/성명/평가자/확인 제외.
   - **자동채움 값 = 활성 사용자(§4·created_by 일치)**. FormCanvas useEffect 주입(폼로드·사용자전환
     반영, 저장본 이어쓰기는 원작성자 보존). auto=disabled 렌더.
   - **미선택 시 defaultAuthor 폴백 제거**(form-handlers meta default) → 빈값 + 저장 시 "사용자를
     선택하세요" 차단. **"빈칸이 가짜 이름보다 낫다"**(조작차단 동일 철학).
   - 실증: L1100-04 작성자 칸에 활성 사용자(류형석) 자동채움·"자동" 배지·편집불가.
5. **금형 PM 별도 의무 신설 → 0098** (사장님 확정: 별도 신설, 12절 미결 종결).
   - #60 '설비·금형 PM' → '설비 PM(예방보전)' 설비 전용 명확화.
   - 신설 '[월] 금형 점검(일상/정기/보관)' + L1100-25 연결(생산팀·8.5.1.5·손진식·월1회 조정가능).
     미연결이던 금형 점검이 관제탑 표출(의무 총 72→73)·이행 독립 추적 → SQ 금형영역 4_x 증거.
   - 복사본 검증: 의무+1·#60명확화·통합잔재0·멱등. sort_order=max+1 유니크.

**마이그 0096/0097/0098 전부 라이브 DB 복사본 선검증**(멱등·보존·의무연결). typecheck 통과. dev 3회 기동으로
0096~0098 라이브 적용됨(스냅샷 백업 자동). **⚠️ 설치판 = 7/22 16:14판 = 0096~0098·CDP·작성자auto
미반영 → 재빌드 필요**(사장님 재설치 전). 재빌드 1회에 오늘분 전부 포함.

**▶ 남은 것**: ①**P9 확장**(셀맵 176종 중 예시 없는 나머지 — 대부분 form_fields 크래프팅 P7식 선행)
②**웹 전환 방향**(사내 서버 vs 클라우드 = 사장님 결정 — 유일 남은 결정) ③설치판 재빌드·재설치
④capture.mjs 프리셋/네비 확장(작성 화면 등 정례 검수 세트화).

---

## 🟢 7/22 (수) 오후 — 코워크 병합지시: P9 1차검수 반영 + P10 + #5

**커밋(푸시, HEAD `d719db6`)**: a8665e2(P9검수+P10) · d719db6(#5).

- **P9 1차 검수 반영 4건(0095)** — 조작차단을 "강한데 억울한 사람 없는" 최종형으로:
  · **차단 규칙 재정정**(`shared/form-validation.ts` `isBlockEligibleLabel`): fact 유지하되 완전일치
    차단은 **다중 측정값·LOT·시리얼 라벨만**. 품번·단일수량·품명은 실존값 우연일치 오차단 방지로
    제외(공란 검증만). 실존 품번(84610-2S000)·소수 수량이 예시와 우연히 같아도 정당 입력 안 막힘.
    호출부(FormCanvas·form-handlers) label 전달. **검증: 차단=LOT·측정치·실측값 8건뿐**.
  · 0095: B1100-01 품명·품번&규격 frame→fact / D1100-03 참석인원 frame→fact / 예시 부서명
    구매팀→영업/자재팀 / 이름 예시 4건 제거 / 라벨 오타 걀과→결과. 멱등.
  · ④ 작성자 auto 통일 = 체계적 후속 스윕(결재란 작성만·검토/승인 제외)으로 남김.
- **P10 문서작성 화면 재구성(§0.9, DocBrowseView)**: 규정 카드 → **양식 행 리스트**(기본).
  ①숫자 요약밴드(전체/작성가능/예시있음/내의무연결 클릭=필터) ②검색+책임팀/규정/정렬
  ③건수 탭 겸용 ④위계 행리스트(팀뱃지+굵은 양식명+부가설명+상태·작성본수·최근작성일)
  ⑤기본 정렬=내 관련순(팀책임+4·의무+2·작성가능+1). [규정별 보기] 토글로 RegDetail 흐름 보존.
  FORM_LIST DTO 확장(respDept·hasExample·obligationLinked·lastWrittenAt·deprecated, 핸들러 1곳).
- **#5 머리행 고정(d719db6)**: `frozenTop=r===1`(canScrollX 게이트 제거) → 폭맞춤 세로 스크롤 시에도
  머리행 유지. sticky top 은 스크롤 없으면 무효과라 짧은 폼 무영향.

**⚠️캡처 세션 미실시**: CDP+DPI 스크린샷 도구(shotpw.ps1)가 저장소에 없음 → 별도 구축 필요.
코드는 typecheck+마이그 DB복사본+로직으로 검증. **사장님 재설치가 실물 검증**(폭맞춤·#5·홈 P3전환
25건·P10 새화면). 재빌드 1회에 P8+P9+P10+#5 전부 포함.

**설치판 재빌드 = 7/22 16:14. ★재설치 완료·검증(asar 20:52→16:14 교체, 0092~0095 번들).**
사장님 첫 실행 시 마이그 0092~0095 자동 적용(사전 백업). 확인 4포인트: ①신뢰성계획서·TRY-OUT
"한방에 딱" ②폭맞춤 세로스크롤 머리행 따라오는지 ③홈 25건 🔒"작성기록으로만 ✓" ④문서작성 K-Mobility
구도(숫자밴드·검색창·행리스트).

**★예약 후속 2건(코워크 7/22 오후 승인)**:
1. **CDP 캡처 파이프라인 재구축** — 검수 백본(지금까지 13장). `Page.captureScreenshot`(DPI 무관·PrintWindow 불필요)
   + dev `--remote-debugging-port` + zustand 네비 스크립트. **정식 도구로 저장소에 포함**(scripts/).
2. **경계 케이스 3건 → 0096 교정** — 코워크 권고 = **전부 frame**(측정시스템 평가·연간설비 보전계획 및
   실적표·결과는 = 제목/구획 라벨, 값 아님). **사장님 확인 떨어지면 0096**으로 fact→frame.

**▶ 남은 것**: ①사장님 **경계 케이스 3건**(측정시스템 평가·실적표·결과는 — 제목/값 판단) ②**금형
PM 분리 의무 신설**(12절 미결) ③(원하면) **캡처 세션 별도 진행** — CDP Page.captureScreenshot 로
DPI 무관 캡처 가능(dev에 --remote-debugging-port 추가 + zustand 네비 스크립트) ④P10 실물 확인
⑤④후속: 작성자 필드 auto 체계 스윕 ⑥P9 확장(셀맵 나머지 form_fields 크래프팅).

---


## 🟢 7/22 (수) 오전 — 코워크 통합지시(사장님 문의 3건 + #4 회신): #0 빌드·P8-3·0093·P9

**마이그 3개 신설(전부 DB 복사본 선검증 — 멱등·orphan 0·fact경고누락 0):**
- **0092** `form_examples_verdict_notes` — P6/P7 예시 114건 전수 검수 결과 보정. 구조 클린
  (orphan 0·field_class NULL 0·미래날짜 0). 유일 갭 = 판정/검토결과 fact 6건 why_note 미보유
  → '작성 방법' 탭 누락 해소(K1200-07·L2100-07·L4102-02·M1200-08·M1200-10·M1200-11).
- **0093** `obligation_form_links` — #4 의무→양식 연결 **25건**(사장님 승인). active 72건 중
  미연결 65건 전수검토 → 증거양식 명확한 것만 보수적 연결(7→32건). 회의·마감보고류 40건은
  억지연결 금지(관제탑 오도)로 미연결 유지. #44=J3100-03(대장·확정)·#60=L1100-12(금형
  L1100-25 분리는 12절 미결)·#8·#70=L3100-03 공유. title 정확매칭+미연결가드=멱등.
  **P3 자동전환 코드 확인**(team-handlers `formRecordToday`): form_code 붙는 순간 홈에서 수동완료
  사라지고 "🔒 작성기록으로만 ✓"/[작성기록으로 확정]으로 전환 = 추가코드 불요.
- **0094** `p9_examples_fieldclass` — P9. (A)**fact 오분류 8건 교정**: 0085 휴리스틱 `LIKE '%서명%'`이
  부서명·문서명·사업부부서명을 fact 오판(text라 정상입력 오차단) → frame(실제 '서명' 2건은 보존).
  (B)**예시 초안 53건×7종**(B1100-01·D1100-03·L3101-01·A8101-02·K1200-03·K1200-05·L1100-07).
  브라켓ASSY(84610-2S000)/현대위아/스폿용접 일관 시나리오·date과거·fact ※예시 경고·판정 select 제외.

**산출물**: 검수 체크리스트 `docs/p9-example-review-2026-07-22.md`(체크박스·fact플래그 — 사장님/품질
1차 검수 요망). AnswerPanel '예시 미준비' 문구에 "관리자에게 예시 등록 요청" 안내 추가. typecheck 통과.

**#0 빌드 판정**: 설치판 = **20:52 빌드**(P8 21:54·P8-2 22:15·KPI 22:32 **미포함**). 사장님이 본
L1100-04·L2200-01 잘림 = **재설치 전(P8 미적용) 화면**. dist 22:33 빌드(P8 전부 포함) 재설치 대기 →
오늘 최종 재빌드에 P8+P8-2+KPI+0092~0094 전부 실려 **한 번의 재설치로 모두 반영**.

**#1 P8-3 조사결론(v5 실제)**: 원본 시트 렌더는 v5에서 **ExcelSheetView 단일 컴포넌트**(읽기+입력 겸용)
= 이미 P8/P8-2 폭맞춤·60%하한·sticky·[폭맞춤⇄100%] 보유. 코워크가 나열한 별도표면(문서뷰=리플로우
820px·개정이력=시트미렌더)은 master 가정. 즉 "모든 시트렌더 통일"은 이미 성립.

**▶ 남은 것**: ①**사장님 P9 체크리스트 검수**(fact 분류 이상한 것 회신) ②금형 PM 분리 의무 신설 여부
(12절 미결) ③**재설치 후 실물검증**: L1100-04·L2200-01·L2300-01 엑셀뷰 폭맞춤·#5 세로스크롤 머리행고정
·홈 P3전환(연결 25건 "작성기록으로만 ✓"). ④P9 확장: 셀맵 176종 중 예시 없는 나머지(대부분 form_fields
크래프팅 P7식 선행 필요 — 이번은 연결+필드보유분만 착수).

---


## 🟢 7/21 (화) 오후·저녁 — 12번 P1~P7 + 전체검수 + P8/P8-2 레이아웃

**커밋(모두 푸시, 트리 클린, HEAD `05f4b2a`)**:
- 12번 지시서 **P1~P7 전 구현**(홈 재배치·정답 따라쓰기): 커밋 8개(2756b99~ebaabc6),
  마이그 0085~0090, field_class(fact/frame)·form_examples·예시값 조작차단 6종 셀맵.
- **전체 코드 검수 + HIGH/MED 수정**(`7974619`): H1=예시값 조작차단이 UI 전용이라 우회
  가능 → 데이터계층(main CREATE/UPDATE)으로 이동. M1 auto저장불가·M2 created_by·M3 rename·M4 히어로.
- **P8 레이아웃**(`576905a`): 작성 화면 잘림 해소.
  - AppShell — form-builder만 **full-bleed**(중앙 `min(1400px,94vw)` 틀의 **예외**. 그 외 화면 유지).
  - FormBuilderPage — viewMode 부모 승격 → 엑셀·문서 뷰 전환 시 목록+정답 **자동 접힘**,
    [입력] 복귀 시 복원(수동 folded/listCollapsed 불변, 강제접힘만 override).
  - FormListPanel 접기/펴기(44px 레일). FormCanvas viewMode props화(BOM·공정 워크벤치 단독사용 무영향).
  - ExcelSheetView — 기본 '폭 맞춤'(zoom) + [100%] 토글 + 첫 열 sticky. scrollbar-gutter:stable·useLayoutEffect.
- **P8-2 가로형 플로어**(`033d17f`): 폭맞춤 **가독성 하한 60%** — 극단 가로형(폭<60%)은
  60%+첫열·머리행 고정 스크롤로 자동 전환(깨알 방지). 규칙 하나로 세로/가로 자동 분기.
- **KPI 월별 실적 일괄 입력**(`05f4b2a`): 자동산출 불가 결론 → 수기 대량입력 UI.
  홈 KPI 섹션 버튼→35지표 한 화면(팀별·프리필·달성표시). IPC KPI_MONTH/KPI_SAVE_BATCH.
  검증 후 테스트행 삭제로 라이브 DB 원복(kpi_measurements 0건). 캡처 cap_kpi_*.

**검증(dev CDP, 176종 셀맵 폭 전수 조사)**:
- 폭맞춤 <60% = **유일 F2100-11**(자격인증교육평가서, 3648px·57열→60%). 나머지 상위(D1100-02·
  L2100-06 3072px 60%, A4200-03·B2100-02 3008px 62% …)는 60~88%라 폭맞춤으로 읽힘.
- 사장님 지목 **L2200-01(신뢰성시험계획서, 990px)** = 전체폭에서 **100% 완전표시**. 잘림은
  구버전 좁은 레이아웃 탓 → **재설치로 해결**. 캡처=`코워크 작업한거 모음/P1_캡처_260721/`
  cap_o~q(P8)·cap_r(L2200)·cap_s/t(F2100 플로어·스크롤).
- 진입경로: **AnswerPanel은 FormBuilderPage 단일 호스트** → 홈[정답보고작성]·문서·양식단독·
  SQ트랙·불량대책서 전 경로 `setPage('form-builder')` 수렴 = 자동접힘 일괄 적용(새는 곳 없음).

**설치판 = 22:16 재빌드(P8+P8-2 전부 포함) — 재설치 대기.** 사장님 캡처 확인 후 재설치 예정.
⚠️`app.setPath('userData')` 하드코딩 = `--user-data-dir` 격리 불가 → dev/설치판 같은 라이브 DB.
dev 캡처 시 설치판 먼저 닫을 것. CDP `ev` 반환은 `msg.result.result.value`(직전 undefined 원인).

**논의된 향후 방향(나중에)**: 사용자 지정 → **아이디/비번 로그인**(찬성 방향, app_users에 비번
컬럼만 추가하면 토대 완성. 현장 공용 단말은 PIN 병행. 시점=심사 후 사내망 웹 단계).

**▶ 내일 TODO(나머지)**:
1. (사장 확인 후) **설치판 재설치 검증** — L2200-01·F2100-11 엑셀 뷰 실화면.
2. **form_examples 114건 fact 내용 검수**(최우선) — P7 조작차단 예시값 품질·field_class 분류.
3. **설비 일상점검 양식코드 부여**(L3100-04/A4100 계열 등재).
4. **의무-양식 연결 확충**.
5. F2100-11류 **세로 스크롤 시 머리행 고정 실동작 확인**(이번엔 세로 스크롤 미발생으로 미검증).
6. **[object Object] 데이터 오염 조사** — F2100-11 저장 드래프트에 직렬화 안 된 값(기존 데이터 위생).
7. **KPI 월별 실적 일괄 입력 화면 완성**(`05f4b2a`) — 홈 KPI 섹션 [월별 실적 일괄 입력]
   버튼→35지표 한 화면(팀별 그룹·목표대비 달성표시·프리필). IPC=KPI_MONTH/KPI_SAVE_BATCH.
   **남음**: 사장님이 7월 실제 수치 입력(자동산출 불가) / 또는 ROUTEBAD 추출 자동산출 스파이크.
8. (새 PC 배포 필요 시) 0085 seed 반영 재빌드.

## 🟢 7/21 (화) 오전 — 설치판 재빌드·재설치 + 0085 seed 정본화

1. **★설치판 재빌드·재설치 완료**(0084 반영): typecheck 통과 → `npm run build:win`
   (08:15 빌드) → NSIS `/S` 무인 재설치 → **asar mtime 08:15 갱신 검증**+migrations/0084
   번들 확인 → 설치본 스모크 부팅 정상(migrate 0084까지 인식=`skip`(라이브 DB에 이미
   있어 정상)·seed 스킵·kb 854 재색인·크래시 0). 설치판=0084 포함 정본.
   ⚠️봇 셸 `ELECTRON_RUN_AS_NODE=1` 제거 후 빌드/실행(안 하면 app.setPath undefined 즉사).
2. **★0085 seed 정본화**(persons.json·teams.json): 신 조직도(2026-07-20)로 교체.
   - persons 14명(홍길동 더미+비재직 4명 포함) → **신 16명**(관리 서규하·김초연 / 영업자재
     차현수·서명진·조성민·김만진 / 생산 손진식·장석봉·예지용·김병철 / 품질 임하수·류형석
     ·박세진·이화준 / 개발 하헌·김민수). id p-001~p-016 팀순 재부여.
   - teams.name 구판("자재/납품팀"·"관리지원팀") → **team-theme 정본**(영업/자재팀·관리팀 등),
     managerId 신 팀장(team-qc p-011 임하수·team-de p-015 하헌).
   - **검증**: 빈 DB에 seed.ts 순서(teams→persons) 재현 → FK orphan 0·managerId 미존재 0
     ·구 인원 잔존 0. 서상규(사업부장/임원)만 persons 제외(기존 관례·의도).
   - **중요한 발견**: `persons` 테이블은 **읽는 코드가 0**(seed INSERT가 유일 사용처) =
     완전 죽은 테이블. 어제 검수 에이전트가 우려한 "관리자 화면에 구 조직도 뜸"은 실제로
     발생 안 함(표시 화면 없음). 반면 `teams` 는 PPAP·APQP·MSA 가 `LEFT JOIN teams`로
     팀명 표시 = 살아있음. 그래서 teams.name 정본화가 실효 있는 부분.
   - **⚠️실효 시점 = 신규 설치(빈 DB)만**. 현 사용자 PC 는 이미 시딩돼 seed 스킵 → 무영향.
     즉 0085 는 **다음 배포(새 PC/새 DB)** 때만 나타남. dist 설치판(오늘 08:15 빌드)은
     0085 seed **미포함** — 새 PC 배포 전 재빌드 필요(실사용 DB엔 즉효 아님이라 지금은 보류).

3. **12번 개발지시서 v5 대조 검증·개선**(코워크 작성분, 저장소 밖 루트 파일):
   `12_UIUX_홈재배치_정답따라쓰기_개발지시서_260721.md`. 코워크는 master 기준이라 v5 실제와
   어긋난 표기를 렌더러·DB 2트랙(Explore 2)으로 전수 대조 → **★0.5절 교정 레이어** 삽입
   (본문보다 우선). 치명 3건 인라인 교정: ①마이그 번호 "0083+ 다음"→**0085**(최신 .sql 0084,
   0054 결번) ②form_submissions `author` 없음→**`created_by` 이미 존재**, ADD 금지 재사용
   ③P6 시드 "연결 양식 전수"=**실제 7건**(M1200-11·L2100-07·M1200-08·L2300-01·K1200-07
   ·M1200-10·L4102-02). 경로 치환: TEAM_TODAY_BOARD=IPC채널·AiAuthorModal→copilot/
   ·ResizableSplit→shared/·SettingsMenu→layout/·sq_reg_map 역인덱스 전용IPC 없음(regSq 재사용)
   ·"22p"→28정의/26라우팅. P3 overdue 자동감지는 formDoneToday가 이미 충족(도래일 전진만 확인).
   **봇 넘길 때**: app_users 초기시드=persons.json 16명 재사용(팀장5=manager·서상규=executive
   ·나머지member) 안내하면 12절 블로커 해소.
4. **KPI 6월 실적 = 자동 산출 불가 결론(보류)**: 사장님 "6월 확정·자동산출만" 지시로 착수했으나
   MES 원본 6월 직접 집계 결과 **판정·불량 데이터 부재**로 결정론 산출 불가 판정. 근거:
   ①QMS_SQC 6월 검사 커버리지는 완전(자주 W 58,675·수입 I 6,073·패트롤 P 4,300·**출하 O 0**=수기)
   ②판정처럼 보인 QMS_SQCDESC.QCSTND를 6월 40.8만행 집계=OK 19%·**빈값 72%**·나머지 측정실측값
   혼재 → **NG(불합격) 안 잡힘**(불량은 미추출 ROUTEBAD 소관). 합격률·PPM 방어 불가라 **미입력**
   (OK÷전체는 'OK표기율'이지 합격률 아님). kpi_indicators 35종·kpi_measurements **실적 0건 유지**.
   ⚠️지표 1·2·3·6 owner_team 구판(품질보증팀·구매팀·생산기술팀) — 겸사 정본화 대기.

**▶ 다음 후보**: ①MES 기록 현황 발견 4종 현장 확인(품질팀·생산팀, 474fdf5 확인요청서 전달)
②KPI 진짜 산출 스파이크(ROUTEBAD 추출+판정로직, CPK 스파이크급 — 사장님 재지시 시)
③수기 KPI 6월 집계값 사장님 전달 시 입력 ④12번 지시서 봇 핸드오프(구현 착수)
⑤주기적 dmp 반입 운영 확정 ⑥(배포 필요 시) 0085 seed 반영 재빌드.

## 🟢 7/20 (월) 오후 — ★신 조직도 반영: 담당자 재배정 (0084)

**발단**: 사장님이 "전사 조직도"(2026-07-20) 제공 → 0081 이 쓴 구 조직도(2026.1/1 승인본)
대비 **인사이동 발생**. active 의무 72건 중 30건이 **비재직자에게 남아 있었음**(김기범 16
·고형진 9·박민수 3·안진성 2). 방치했으면 10월 심사에서 "담당자가 회사에 없다"가 됨.

1. **조직 변경 판독**: 품질/개발팀 **분리** → 품질팀(팀장 임하수 부장 ※26.7.23 부임 ·
   품질보증 류형석 과장 · 팀원 박세진 계장 · 수입검사 이화준 대리) / 개발팀(하헌·김민수).
   이동 = 서명진 과장 품질(개발)→자재관리, 조성민 대리 자재관리→납품관리.
   신규 = 임하수·류형석·박세진·이화준·김병철(보전)·예지용(생산)·김정현/노성현(납품반 용역).
2. **★0084 담당자 재배정**(39건 = 비재직 30 + 팀내이동 9). 사장님 결정 3건:
   ①품질팀장 시스템 의무 16건 → **임하수**(부임 전이라도 정본) ②검사 실무 9건 →
   **이화준**(수입검사·성적서 수취·교정/측정기 6) + **박세진**(순회·출하·한도견본 3)
   ③서명진의 품질기술 3건(SPC·CP/CPK·ISIR) → **류형석** — 담당자가 자재로 가도 품질 존치
   (10월 SQ 최우선 항목이라 품질 소재가 안전). 자재 6건 → 서명진, 납품 2건 → 조성민,
   설비·보전 3건 → 김병철.
   가드 = `WHERE title=… AND active=1 AND assignee='<구담당자>'` — 0081 기본값을 그대로
   들고 있는 행만 교체(사용자 수정분 불가침·완전 멱등).
   검수 = integrity-auditor **PASS**: title 39/39 전건 1행 매칭(·—↔ 유사문자 포함),
   연쇄 재이동 공집합(서명진·조성민 수신∩발신=∅), 2회 적용 추가변경 0, 사용자값 보존 실증.
   결과 = **15명 / 72건 / 비재직자 0 / 미배정 0**.
3. **손대지 않기로 한 것(의도적)**:
   · `isir_packages.qa_manager` 김기범 2건 = **실제 제출본 판독값**(2025.09/2026.05 서명자).
     기록 원본이라 개서하면 위조 — 불변 유지.
   · `persons` 테이블(구 조직도 14명, `홍길동` 더미 포함) = **런타임 미사용**(seed.ts 최초
     시딩 전용 write-only, main 에 SELECT 0건). 관제탑은 assignee **문자열 직접 그룹핑**
     (PortalHome.tsx:567)이라 무영향. `홍길동`은 버그 아님 = 0016 의 의도된 데모 페르소나.
   · ⚠️단 **신규 설치 시 구 조직도가 뜨는 위생 문제**는 남음 → `resources/seed/persons.json`
     정비 = **0085 후보**(TPC팩 PII 분리 결정과 세트로 처리 권장).
4. **미배정 인원 = 사장님 확정(놔둘 것)**: 예지용 계장(생산) = 가공·조립 실작업(오리고 붙이는
   작업)이라 **기록 의무 주체 아님** / 김만진 계장(영업) = **납품기사**라 마찬가지 /
   김정현·노성현 = 납품반 용역. → 향후 세션에서 "미배정이니 채워야 한다"고 건드리지 말 것.
   임하수 부장도 **부임 전이지만 그대로 진행 확정**(대행 지정 없음).

**⚠️운영 공유 필요**: 임하수 부장 **부임 전(~7/22)** 품질팀 16건은 실질 공석 → 그 사이
관제탑 미이행 알림은 **데이터 오류가 아니라 정상 동작**. 대행 없이 그대로 가기로 확정.
**⚠️설치판 미반영** — 0084 는 dev DB 에만 적용됨. 심사 시연 전 재빌드·재설치 필요.

**▶ 다음 후보**: ①0085 persons.json 정비 ②MES 기록 현황 발견 4종 현장 확인(품질팀·생산팀)
③KPI 7월 실적 입력 ④주기적 dmp 반입 운영 확정.

## 🟢 7/20 (월) — LOT 계보 딥링크 + ★MES 기록 현황 (커버리지 판정)

1. **7/19 마감 잔무 종결**: 08:36 설치판 실설치(asar mtime 검증)+TODO 커밋. 발견=어제
   세션이 빌드까지만 하고 설치 전에 끊겨 있었음.
2. **LOT 계보 딥링크**(71396f3): '모의 역추적 훈련' 의무(관제탑 보드+정기 의무 행)·
   '역추적 소요시간' KPI 타일 → LOT 계보 조회 직행 버튼. 매핑=renderer lib/deeplink.ts
   (이름 기반 — 의무 제목 개정 마이그레이션 시 함께 갱신). 계보 화면 하단 되돌이 힌트
   (훈련 후 KPI 입력·의무 완료 처리) — 월 1회 훈련 루프 앱 안에서 완결.
3. **★MES 기록 현황**(fd8023a, 사장님 발제 "하위코드 기록이 들어왔는지 보여줄 것"):
   사이드카 `mes_records.db`(scripts/mes_records_build.py — QMS_SQC 헤더 240만+MAC_DESC
   22만 → 일자×구분×품번(설비) 집계, 10.3MB/28초. SQCDESC 840MB 불필요). 매일 관리 메뉴:
   구분별 30일 커버리지 스트립(채움/평일 공백 빨강/주말 회색)+품번·설비별 미기록 경과 표
   +일자별 표(설비점검=CONFIRMOR 확인율). 정직 표기 3종(기준일 이후 미판정·출하검사 O
   무기록 명시·미래 일자 오류 6건 제외).
   **실측 발견**: ①패트롤 평일 공백 3일(6월 중순) ②장기 미점검 설비 다수(AS CT 1150일·
   J22 1105일·파워밴딩기 3대 ~945일 — 미사용인지 누락인지 생산팀 확인 대상) ③주말 확인율
   0% 사례 ④출하검사(O)는 MES 기록 자체가 없음(수기 관리 — 심사 답변 준비).
   ⚠️함정: 사이드카 교체는 앱(dev 포함) 종료 후 — Windows 파일 잠금. 스크립트가 안내함.

**▶ 다음 후보**: ①관제탑 담당자 배정 사장님 검수 ②MES 기록 현황 발견 4종 현장 확인
(품질팀·생산팀 전달) ③KPI 7월 실적 입력 ④주기적 dmp 반입 운영 확정(반입 주기·담당 —
MES 다운로드 의무 재정의와 세트) ⑤SQ 워크리스트 D3(MES 기록 축적) 항목 상태 갱신 검토.

## 🟢 7/19 낮 — 어제 "다음" 3건 전부 완결 (0081·0082, LOT 계보 화면)

1. **★담당자 실명 기입**(0081, 9715014): 명단 대기가 **조직도로 해결** —
   `26년 TPC AM사업부 조직도.png`(2026.1/1 회장 승인본, 15명 실명) 판독 → active 의무
   72건 전량 배정. 원칙 = assignee 는 "기록의 확인 책임자"(팀장=마감보고·확인형·연간,
   실무자=역할 일치: 김초연=자금·근태·KPI, 안진성=납품, 조성민=자재, 고형진=검사 실무,
   김민수=Core Tool 문서, 서명진=검사·공정능력, 경영=서상규 전무). title 정확일치 +
   빈 값만 채움 가드(사용자 기입 불가침). **관제탑 개인별 보드 실명 그룹핑 실화면 확인.**
   ⚠️배정은 역할 추정 기본값 — 사장님 확인 후 관제탑에서 개별 수정 가능(ObligationModal).
2. **★LOT 계보 조회 앱 연결**(87dfcf6): 심사 대응 메뉴 신설 — 검색(바코드/품번)→양방향
   전개. 사이드카 `mes_trace.db`(신규 scripts/mes_trace_build.py: 덤프→lots 145만+edges
   328만, 289MB, 78초) — 본 DB 비대화 방지 분리, 읽기전용+mtime 재오픈(새 덤프 반입 시
   재실행만). IPC 3종(mesTrace:*), 재귀 CTE 최단깊이·상한 3000 정직 표기.
   **CDP E2E: 28236-2MAA0-D1 역추적 374 LOT·정추적 310 LOT 각 1.5ms** — 시연 카드.
   ⚠️tables.json 은 스크래치패드 소실됐던 것 → `추출결과\tspmes_tables.json` 으로 영구화.
3. **★팀별 KPI 확장**(0082, 1ebbb89): v4 §04 KPI 표 → kpi_indicators 29행(개발6·영자5
   ·생산6·품질5·관리7, sort_order 100+). 중복 3 제외·이중 지표 3 분리·정성 2 는 target
   NULL. '납기 준수율'→'고객 납입 준수율' 개명(이력 연속). 홈 KPI 스트립 팀 필터 칩
   (대표+5팀, normalizeTeam 이라 레거시 owner_team 흡수). 실화면: 품질팀 칩 7종 확인.
4. **검수 체계 첫 실전 가동**: 0081/0082 모두 integrity-auditor 에이전트 PASS 후 커밋
   (72:72 대사·멱등 2회·가드 실증 / 6→35행·개명 1:1·체인 diff 없음). 설계 의도대로 작동.

**E2E 신무기**: dev 를 `npx electron-vite dev -- --remote-debugging-port=9222` 로 띄우면
CDP 로 실화면 자동 조작+스크린샷 가능(node + repo 의 ws 모듈, 스크립트 예시=세션 스크래치
cdp_drive.js 패턴). ⚠️scripts/dev.mjs 는 인자 미전달 — electron-vite 직접 호출로.
⚠️PS5.1 커밋 메시지 큰따옴표 함정 재확인 — 메시지는 파일로 빼서 `git commit -F` 가 안전.

**▶ 다음 후보**: ①관제탑 담당자 배정 사장님 검수(수정은 화면에서) ②'모의 역추적 훈련'
의무·역추적 소요시간 KPI ↔ LOT 계보 화면 딥링크 ③KPI 월 실적 입력 시작(7월분)
④주기적 dmp 반입 운영 확정(사장님 논의 — MES 다운로드 의무 재정의와 세트).

## 🟢 7/19 — v4 통합뼈대 수용: 5팀 재편 + MES LOT 실측 + 일상업무 의무 35건

**원천**: `추가 뼈대 진행 모음_260717\TPC_AM사업부_통합뼈대_v4_최종.html`(사장님 제작 운영 정본)
+ Scan-to-Trace 구축워크북. 사장님 결정 3건: ①실조직=5팀, 앱 통합 ②Scan은 MES 실측 먼저
③스캔앱은 AppSheet 아닌 **사내망 경량 웹 단계**에서.

1. **★5팀 재편**(765d726): team-theme = 개발·자재/출하·생산·품질·관리. 레거시 7팀 표기는
   deptKeys/OWNER_HINTS 흡수(DB 무수정 호환). 영업→자재/출하(수주·견적 신규분은 개발팀 기입),
   총무→관리, 구매→자재/출하, 생산기술→생산.
2. **MES LOT 커버리지 실측**(mes_lot_coverage.py): 계보 POP_TRACE 328만 링크(2021~현재)·외주 42만
   = v4 SCAN 2·3 **이미 가동**. 빈 구간 = SCAN 1(입고 ASN, SCM 114행)+포카요케.
   **역추적 데모**(mes_trace_demo.py): 자재 LOT→생산 LOT 전개 **0.0ms**(그래프 적재 44초) — 심사 시연 카드.
3. **★0077 일상업무 의무 35건**: v4 §04 팀별 관리판+§05 매핑표 전개(기존 37건과 대사, 중복 13건 제외).
   일일 마감보고 5팀·생산일보·4M·순회검사·NCR·출하검사·납입지시·FIFO·자금일보·RFQ·APQP(일 16) +
   주 6·월 9·분기 1·년 3. 기록물·근거조항=note/clause_ref. 온보딩 체크리스트 양식 2종(F1101-03/04).
   ⚠️'아침 조회'는 의무화 안 함 — 마감보고 note에 리듬으로 포함.

**당일 후속(새벽)**: 팀명 확정=영업/자재팀(0078) · SQ 대시보드 주담당=영역 기준 규칙(수입검사
기준수립→품질, a8c484c+후속) · 팀 카드 중복배정·손실 이중계상 해소 · 체인 전수 감사(0079: 2_9
품질 fallback, 유일 실미배정) · 5_2 '자재 보관장' 개명(0080, kb/FTS 동기) · 감사도구
scripts/team_chain_audit.py 승격. ⚠️NSIS 설치 함정: 앱 종료 완료 검증 후 설치+asar mtime 확인 필수.

**▶ 다음(7/20~)**:
1. **★정합성 검수 체계 구축**(사장님 발제): ①결정론 검사기 — team_chain_audit 7종을 앱 내
   '정합성 점검' 관리 메뉴(IPC)로 내장, 검사 확장(form_code 중복연결·cadence 유효성·D-day 소스
   ·미해당 처리) ②마이그레이션 diff 검수 에이전트(.claude/agents) — 시드 규칙(적용된 마이그 수정
   금지·BEGIN/COMMIT 금지·resp_dept 정본 대조·0019/0071 함정) 자동 점검. 결정론 우선, AI는 보조.
2. 관제탑 5팀 보드 담당자(assignee) 실명 기입 — v4 STEP 2 (사장님 명단 대기).
3. 이벤트 원장 조회("이 LOT 계보 보여줘") 앱 연결 — 사내망 웹 단계와 세트.
4. §04 KPI 목표치 → kpi_indicators 팀별 확장 검토.

## 🟢 7/18 — MES DB 덤프 통째 입수 → 판독 성공 + 하위코드 앱 적재 (0076)

**대사건**: 회사 서버에서 받은 `tspmes_2026-07-17.zip`(3.4GB)의 정체 = **MES(TSPMES) Oracle 11gR2
전체 DB 야간백업**(6.2GB dmp, 테이블 135개). 일일 엑셀 드롭을 기다릴 필요가 없어짐 — 수년치가 통째로 있음.

1. **덤프 판독기 자작**(Oracle 설치 없이): `scripts/mes_dump_scan.py`(전 테이블 DDL/오프셋 목록)
   + `scripts/mes_dump_extract.py`(exp 바이너리 행 포맷 역공학 — 디스크립터/NULL=FEFF/종료=FFFF/
   Oracle NUMBER·DATE 디코딩/UTF-8·CP949 혼재 자동판별). 한계: LONG/LOB 테이블(POP_ITEM 도면)은 미지원.
2. **하위코드 전량 적재**(0076_mes_codes_seed): POP_CODE 2,906행·128그룹 → 앱 `mes_codes` 테이블.
   핵심 그룹: **QC_GBN**(W자주/I수입/P패트롤/O출하) · SPC_INSP(자주검사 항목 105) · MAC_CLGBN(설비
   보전항목 119) · ROUTEBAD(공정별 불량 251) · WRKCTR(설비 182) · CON_SCOPE(브레이징 조건범위+PLC주소)
   · DEPT(부서) · SPC_CNT(검사차수/시각). 스모크=멱등 2회 적용 OK.
3. **4종 기록 ↔ MES 테이블 매핑 확정**: 자주/수입/패트롤 = QMS_SQC(QCGBN 구분)+QMS_SQCDESC(측정값,
   ~840MB 최대 테이블) · 설비일상점검 = MAC_DESC · 검사규격 = **QMS_SPEC(SU/SL/NOMINAL → CP/CPK 원천!)**
   · POP_SQC/POP_QC_DATA 등 POP_* 검사계열은 빈 테이블(QMS_*가 실사용). 샘플 CSV+README =
   `8. 자주검사...\TPC AM사업부 하위코드들 모음_260718\추출결과\`.

4. **★CP/CPK 스파이크 완료**(같은날 오후, scripts/mes_cpk_spike.py): 2026년 검사 42.3만건·측정 250만행
   → **Cpk 1,344개 계산**. 판독 전문 = `docs/sq-levelup-2026-10/60_MES_CPK_스파이크.md`.
   함정 2개 해결(SUBSEQ≠항목, 헤더 POSITION이 정답 / SU·SL=NOMINAL±공차). 발견 4종: ①단위혼용
   109키(kgf↔MPa, 정규Cpk 열 제공) ②조건관리 항목은 Cpk 부적절(심사 방어논리) ③치수항목 중심정확
   ·산포개선 대상 ④범위초과 후보 4건+σ=0 43키+규격미등록 144키. QMS_SQC/SQCDESC 2026 전량 CSV
   +QMS_SPEC 전량+MAC_DESC 22.2만행 = 추출결과 폴더.

5. **설치판 재빌드·재설치 완료**(7/18 밤, 22:32 빌드): 0068~0076 전부 반영, 설치판 첫 실행에서
   0076 자동 적용 확인(mes_codes 2,906행). ⚠️버그픽스: SQ 대시보드 D-day가 IATF 심사일
   (company_profile, 기본 12-31)을 읽던 것 → sqtrack.audit_date(10-15) 기준으로 교정+날짜 병기.
   GNB 상단 배지는 IATF 정기심사용이 맞음(사이드바 설정에서 실제 날짜로 변경 가능, 현재 기본값 12-31).

**▶ 다음**:
1. **품질팀 확인**: 범위초과 후보 4건(유량 54.5/40~50 등, 60_ 문서 §4) 실물 대조 — 심사 전 필수.
2. mes_records 적재 파이프라인(0072 스키마) + 분기 공정능력 보고서 자동화(L4102-01 채움,
   사용자 CP/CPK 엑셀 수령 후 형식 확정).
3. MES 다운로드 의무 재정의 검토: 일일 엑셀 드롭 대신 **주기적 dmp 반입**이 현실적(사장님 논의).
4. POP_ITEM(BLOB) 추출기 확장(백로그) · 관제탑 MES 의무 담당자 지정(7/20 도래분).

## 🟢 7/17 — SQ 레벨업(10월, 600→700+) 심사 대비 하루 완결 (커밋 8건, HEAD 5156126, 전부 푸시)

**밤 추가분 (저녁 마감 이후)**:
5. **★체크포인트 ISIR 실사 반영**(9adaf25, 0074): 판독을 162개 체크포인트에 대조(6영역 에이전트,
   보수 기준 — met 1/partial 44/na 1/missing 116) → **SQ 대시보드 0→274/1000 가동**(G까지 326점).
   워크리스트=`docs/sq-levelup-2026-10/50_SQ항목별_실행워크리스트.md`(할 일 162건). 미터치 행만
   갱신(사람 체크 불가침 가드). 패턴: 기준=CP 명시(방어 가능)·기록=없음 → 처방=MES 다운로드+D3 축적.
6. **★ISIR 척추 시드**(1b2a742, 0075): 품번/ISIR Core Tool 가동 — 패키지 4(PSW 미승인 2건 approved=0)
   +26종 체크리스트 104행(present=판독 확인)+CP 라인 65행(공정 요약). 완비도 엔진이 판독 리스크와
   자동 정합(25460: 22종 중 20 제출).
7. **SQ 대시보드 타이포 전면 확대**(5156126, 사장님 "결론은 키우자"): 히어로 54px·KPI 40px·행 15px 등.
   ⚠️캡처 스크립트 DPI 인식 추가(shotpw에 SetProcessDPIAware — 없으면 우측 잘림 오탐).
8. **웹 전환 아이디어 논의**(타 코딩 담당자 제안): 답변 = 방향 맞음, 단 ①심사 먼저(10월까지 로컬)
   ②심사 후 사내망 경량 웹(로그인+체크+엑셀 업로드, IPC→API 래핑—DTO 계약 재사용이 자산)
   ③2027 SaaS(클린채널·예창패와 정합). 호스팅=사내 서버 권장(MES 옆). 1단계 설계 문서는 지시 대기.

**하루 요약**: 스캔본 354p(4품번×2권) 전수 판독 → 문서 5종 + 앱 신기능 2개 + MES 연동 사전 세팅.

1. **ISIR 전수 판독**(에이전트 8병렬, PyMuPDF 렌더): 1권=정기검사 세트/2권=ISIR 풀세트(관리계획서) 구조 확인.
   산출물 = `docs/sq-levelup-2026-10/` (00 종합보고=리스크 16계열 · 10 문서대장×4 · 20 심사추적맵(K1~K12
   기록물+현장검증+인터뷰 질문은행) · 30 보완액션 28건 · 40 CP주기의무 매핑표 · raw/ 8파일). 275a68d.
   사장님 요약 아티팩트 = https://claude.ai/code/artifact/08ca1a25-157b-4e1b-9d77-d907090bc2a8
2. **★SQ 심사 트랙**(226d97c, 0068/0069): 심사대응 메뉴 신설 — 품번 4종(지정 3+형제 2M100)×4단계
   (서류확보/정합성/현장검증/인터뷰) 체크리스트 75건 시드. 상태 순환·조치메모·양식 바로가기·D-day
   (app_config sqtrack.audit_date=10-15 **가정치**). EO 이력 확정 반영: 28236 C2MRE654=양산전 최종
   ISIR·C2MSE481=설변전 개발품(정상 이력, 즉답 스토리).
3. **★CP 주기의무 전개**(ed25771, 0070/0071): 관리계획서→하위코드→**관리자 확인 의무**(사장님 확정,
   현장 종이/MES 유지). 갭양식 3종(M1200-10 자주검사 C/SHEET·M1200-11 브레이징 조건관리·K1200-07 외주
   ISIR 접수대장)+의무 신규 9·기존 0043 보강 4+**'반기' cadence 신설**(4곳). 관제탑 보드 자동 표출 검증.
4. **MES 사전 세팅**(fb4c999, 0072/0073): 업체 API 거부(사내 서버) → 일일 엑셀 드롭 방향 확정.
   드롭 폴더 `D:\...\MES다운로드\`(README 규칙) · mes_records 스키마 씨앗 · 다운로드 일일 의무(생산팀,
   7/20 도래) · `scripts/mes_spike.py`(샘플 분석기).

**▶ 다음(7/20 월) 콜드스타트**:
1. **MES 샘플 4종 수령**(자주검사·수입검사·공정패트롤·설비일상점검, 같은날짜 1~2일치+화면캡처)
   → `python scripts/mes_spike.py "MES다운로드\2026-07-20"` → 파서·임포트 파이프라인 설계.
2. 관제탑 생산팀 보드에 MES 다운로드 의무 첫 도래 — **담당 팀원 이름을 담당자란에 지정**.
3. 사장님 대기: 알곤가접 심사 3종 라인 해당 여부(31728 라인에선 실존 확인됨 — CP 부재라 해당 시 중대)
   · 갭양식 3종 현장 서식 · 실제 심사일 · MES 업체에 read-only 계정/야간 자동 export 재요청.
4. 보완액션 실행 착수: A그룹(편철·정정 7건, 품질 1~2일 작업량)이 7월 목표 — `30_보완액션리스트.md`.
5. ⚠️**설치판 미반영**: 마지막 빌드 7/16 16:34 — 0068~0073(심사 트랙·주기의무·MES)이 설치본에 없음.
   다음 재빌드 때 포함(재빌드 전 electron 좀비 taskkill).

**⚠️신규 함정 메모(7/17)**: ①관제탑 보드 팀 매칭=form_code의 resp_dept **1순위**(owner는 폴백) —
양식 책임팀≠의무 소유팀이면 오배정(TPM×L1100-07 사례, form 미연결로 회피) ②같은 form_code를 의무
2건에 연결 금지(연결양식 오늘작성 auto-done 오검) ③PS5.1에서 커밋 메시지에 **큰따옴표 금지**(네이티브
인자 깨짐) ④적용된 마이그레이션 수정 금지 — 시드 문구 교정도 새 마이그의 UPDATE로(0071 §3 선례)
⑤sqtrack_items.code 규칙 '{품번}:{phase}-{seq2}' 불변(변경 시 status 고아).

---

## 🟢 7/16 — 포털 대전환 (커밋 13건, HEAD 133291d, 전부 푸시, **설치판 16:34 재설치 완료**)

**하루 요약**: 사장님 지시로 앱 전체를 삼성닷컴+FLOWX식 포털로 전환 + 코워크 SQ 가이드층 전체 이식.
목업 = 아티팩트 94cce01a(포털 v4)·7470de2d(경영브리핑=코워크 09 재게시).

1. **포털 1단계**(1e65518): GNB(카테고리 드롭다운, 회사명=company_profile)+관제탑 홈(팀별 오늘 할 일 보드,
   ✓=완료이력/양식 작성기록, 0063 obligation_completions+TEAM_TODAY_BOARD)+사이드바 삭제(설정→⚙팝오버).
2. **SQ 가이드층 이식**(6125dc5, 코워크 07/08 지시서): 0064 시드(가이드 42/불릿 372/체크포인트 162+app_config 계수)
   +0065 갭양식 13종 등재(**코드충돌 7건 재부여**: L2100-07·B1100-12/13·L1200-12·L1100-25·J3100-08·M1200-09,
   책임팀=v5 BOM 정본 교정, sq_reg_map 매핑 안 함=0019 함정)+템플릿 14xlsx 번들(extraResources).
   +Pretendard 번들(선언만 있고 미번들이던 것 해결 — 전 화면 폰트).
3. **홈 확장**(0a16a09): 0066 assignee+KPI 지표6종/측정 테이블 — 팀별⇄개인별 토글+KPI 스트립(월별 입력).
4. **SQ 트랙 완결**: ③가이드 패널(3ebb664, WriteGuidePanel 팀상세 4번째 섹션, 체크→제안 실시간, E2E 검증)
   → SQ 대시보드(cdfebde, 심사대응 첫 메뉴 — 점수+등급컷바+Top5+팀별손실 드릴다운→팀상세)
   → ④자체평가+Exporter(ba7689b, 0067+IPC 7종+템플릿 fill 08§3 셀맵 계약, **왕복테스트 통과**)
   → 정본 전환(76b5a15, 확정 평가 있으면 대시보드가 확정 점수 표시).
5. **UI 확정**(사장님 피드백 반복 반영): 타이포 확대(eb5b220)→파스텔 톤+FLOWX GNB 간격(c97a231)
   →**전 화면 중앙 칼럼**(aab6537→34ff2c1: min(1400px,94vw) **인라인 고정** — AppShell 래퍼가 정본,
   개별 화면 max-w 금지). 빨강=연체만, 오늘마감=앰버.
6. **IATF 대시보드+포털 2단계**(133291d): IATF 대시보드(조항 4~10장 커버리지·핵심 의무 이행·문서화)
   +규정·양식 찾아보기(카드 그리드→규정 뷰어(본문+하위양식)→작성 캔버스).

**다음 후보**: ①SQ ③잔여=form-builder 우측 가이드 패널 ②아침 브리핑 메일 연동(4단계 — **사장님 계정 구성 답 대기**:
공용/개인·몇 개, 다음·네이버=IMAP 가능 확인됨) ③도메인 입력 3종(팀별 일상업무 목록·담당자 명단·KPI 6월 실적)
④코워크 미결: VAATZ Ver4 xlsm(최우선)·13종 코드 A4100 정식 등재·규정 미지정 13건 ⑤경영브리핑(코워크 09) 잔여=
번다운·PPM 차트(월보 데이터 쌓이면).

**⚠️신규 함정 메모**: ①봇 셸에서 설치판 exe 실행 시 `Remove-Item Env:ELECTRON_RUN_AS_NODE` 필수(VSCode 상속
→ Node로 실행돼 창 안 뜸) ②PrintWindow는 가려진 창의 **낡은 프레임**을 반환할 수 있음 — 검증 캡처 전 창 전면화
③Tailwind `ml-auto`/`max-w-[min(...)]`가 안 먹는 사례 → 핵심 레이아웃은 인라인 스타일 ④마이그 실패 시
approvals_json NOT NULL (forms INSERT에 '[]' 필수).

---

# (이전) TODO — 이어서 작업 (갱신: 2026-07-14 야간)

## 📌 다음(7/15) — 내일 바로 진행

### 사업 트랙 (7/14 야간 지원사업 전수조사 후속 — 보고서: `~/Documents/지원사업조사_IATF품질문서봇_20260714/보고서_지원사업전수조사.md`)

1. **경남 창업-BuS 신청**(상시~11/23, 예비창업자 명시 가능, 지역제한 없음): 참가신청서+사업계획서만. 055-291-9355. https://www.k-startup.go.kr/web/contents/bizpbanc-ongoing.do?schM=view&pbancSn=176755
2. **스타트업 법률지원 신청**(무료·상시, 기술기반 예비창업자=별도서류 없음): **TPC IP 서면정리 자문 의뢰**(기존 사업 선행과제와 연결). onestop@kised.or.kr / 044-410-1753. pbancSn=176938
3. **🔥부산 B-스타트업챌린지(7/29 17:59 마감) 신청 판단**: 예비 신청 가능·투자금 3억(제목). ⚠️**수상 시 1개월 내 법인 전환 의무 → 7/13 결정(예창패 우선·사업자등록 보류)과 충돌** — 먼저 첨부 공고문에서 3억 지급구조(일괄투자? 상금pool?) 확인(051-715-1741) 후 "3억 실투자면 예창패 4천만 포기 가치 있음" 판단. 본선 10/8이라 수상 확정 전까진 예비 신분 유지됨. pbancSn=178368
4. **NIPA 모두의 AI 공고문(hwpx) 확인**(8/11 마감, 자격·내용 전부 첨부에만): https://www.nipa.kr/home/2-2/16852
5. KDATA AI·데이터 문제해결은행 유선(1899-0247) — 예비창업자 트랙 자격·일정. / 경산시청·경북TP(본부=경산) 홈페이지 자체공고 훑기(K-Startup·기업마당 미게재분).
6. (선택) ir-search 정식 설치 = 사장님 직접 `git clone https://github.com/djfksjd/ir-search ~/.claude/skills/ir-search` 한 줄. / **12월 말 diff 재조사**(2027 예창패·AI바우처 공급POOL 공고 포착).

### 앱 트랙 (기존 백로그)

1. **설치판 재빌드**(심사장 시연용): 7/14 오전 3커밋 반영 — UI P3·히트맵→모의심사·죽은코드정리. `build:win`+사일런트 설치. ⚠️재빌드 전 dev·electron 종료(`taskkill //F //IM electron.exe`). ⚠️NSIS /S=PowerShell Start-Process -Wait.
2. **부실 셀맵 5건 보강** · **TPC 하드코딩 잔여**(팩 분리와 함께) · **궤적 '현 페이스' 정직화**(준비도 일일 이력 저장) · 팀원 피드백→Phase 3 판단.
3. **클린채널 공사 착수**(TPC팩 분리, 2~3주) → 10월 PSST 준비.
4. (P3 대비점검 잔여) 사이드바 메뉴 *설명* 텍스트 `text-foreground/55`(11px)=~3.2:1 AA미달 — 원하면 `/70`으로 개선(장식성이라 보류).

## 🟢 7/14 야간 — 정부지원사업 전수조사 (ir-search 스킬, 816건)

- **도구**: 블로그 소개 스킬 ir-search(github.com/djfksjd/ir-search, 코드 전수검수 후 사장님 승인 하 실행). 정식 설치는 보안분류기 차단 — 스크래치패드 클론으로 대체. ⚠️기업마당 상세 fetch는 셸 인자 전달 시 404 → 파이썬 모듈 호출로.
- **수집**: K-Startup 216(전수)+기업마당 300(최근 20p)+NIPA 300 = 816건 전수검토 → 후보 28 → **원문검증 24건**(에이전트 3병렬 추출+직접 판정). 산출물 = `~/Documents/지원사업조사_IATF품질문서봇_20260714/`(보고서+jsonl+원문24). 프로필 = 프로젝트루트 `ir-search-profile.md`(**경산** 확정 반영).
- **A그룹(예비 신분 즉시 가능) 7건**: 🔥B-스타트업챌린지(7/29, 3억, 지역무관)·경남 창업-BuS(상시)·법률지원(무료)·KDATA(전국)·영산대BI(양산이라 강등)·구미 스타트업360/필드(이전조건이라 강등). **G-Star Pitchday=만39↓ 연령탈락**.
- **실측 확인**: 2026 AI바우처 3/30·AX바우처 6/26 전부 마감 → 2027.2 공급POOL 타이밍 정확(7/13 로드맵 재확인). 프리팁스=투자1천만↑+비수도권 기업(예비 불가) → 경진대회→법인→프리팁스→TIPS 연쇄. **예비단계 융자·보증 사실상 전무**(216건 중 2건뿐, 둘 다 지역 불일치) — 예비 자금은 경진대회·사업화 지원금뿐.
- **경산 재정렬**: '대구 소재' 요건 사업(TIPS IR·데이터AI)=경산 불가, 단 등록 시 대구 지점·연구소로 충족 가능. 최근접 거점=경북창경·경북TP. 경산 직접 공고 현재 0건(대학BI 미모집).

**7/14 야간 마감 상태**: 워킹트리 클린. ⚠️설치본은 여전히 7/13 14:07판 — 오전 3커밋 미반영, 심사장 시연 전 재빌드 필요. ⚠️화면캡처=카카오톡·사진뷰어 겹치면 PrintWindow(scratchpad shotpw.ps1)로.

## 🟢 7/14 오전 — 사장님 지정 3개 완료 (전부 커밋·푸시·실화면 검증)

1. **★UI P3**(e262a68, 심사장 시연 대비): ①글자배율=사이드바 스테퍼(90~140%, `webFrame.setZoomFactor` 전체UI 비례확대)+localStorage 영속→부팅 자동적용(App useEffect). ②제품정보 화면(AboutView)=앱버전(APP_INFO IPC=`app.getVersion`)·제조사(`company_profile` DB=하드코딩아님·고객마다 자기정보)·런타임(Electron/Chromium/Node/V8), 사이드바 ⓘ진입, PageHeader. ③포커스=전역 `:focus-visible` 링(Tab시만). 신규 preload `setZoomFactor` 노출.
2. **★히트맵 핫스팟→모의심사 직접연결**(2288104): UnmetHeatmap 핫스팟 클릭→그 팀×카테고리 최고배점 미충족 항목코드를 `onHotspotClick(itemKey)`로. MockAuditCard `initialItemKey`+`runNonce`(매클릭 재실행)→자동선택+AI예상질문 자동생성, 결과도착시 카드 정밀스크롤. Dashboard `openMockAuditFor`. 검증: 품질팀×검사시험(8)→2_7(50점) 자동생성+착지 실화면.
3. **★v4 죽은코드 완전정리**(db3b1da): `domain/` 11파일 삭제(참조0) · `register.ts` **727→189줄**(죽은핸들러 19개=CLAUSE/DOC/TASK/TEAM_LIST/DASHBOARD_STATS·FULL/DB_STATUS/REGULATION_LIST/BULK + 미사용 import) · `ipc-channels` 상수19 · `ipc-types` 타입맵19+자기참조 죽은타입12. 검증: 참조0 재확인·typecheck node+web·build 3타깃(main 254→237KB)·dev부팅 무에러·대시보드(DASHBOARD_V5)·홈 실렌더.

## 🟢 7/13 — 검수버그 3건+실명 5건 해소 + ★대시보드 v3 (콜드스타트 1순위)

**완료 (전부 라이브·푸시·설치판 반영, HEAD 5ccac83)**:
- **🔴검수 버그 3건 수정**(c649027, 보고서=docs/전체검수_2026-07-07.md): ①StepCard catch 부재→failed 상태+다시시도 버튼(영구 스피너 해소) ②migrate.ts 스냅샷 보존정리 파일명→mtime 정렬(복구 직후 최신백업 삭제 함정) ③team-handlers readinessPct `/(wsum||1)` NaN 가드.
- **실명 하드코딩 5건 선제거**(c649027): 프롬프트 실명 라인 삭제(form/scoring-handlers)·추출예시 실명→플레이스홀더(process-handlers)·seed 대표자/주소/전화 공란화(기존 설치본은 IGNORE라 유지)·**FALLBACK_MASTERS(실명 D:\경로) 삭제→미설정 시 안내에러**. ⚠️이 기기는 `company_profile.mastersDir` DB설정으로 대체 완료(라이브DB에 직접 기입, 경로 실존 검증). 번들 out/ 실명 0건 검증.
- **★대시보드 v3 구현**(5ccac83, 확정 목업=아티팩트 c8a47661): 원칙 "AI는 카드가 아니라 데이터 옆 ✦각주·버튼". 헤더밴드(카테고리 슬라이서)+KPI 6타일(✦검사시험 해소 +N점=결정론 산수)+팀레일+차트 6패널(도넛·팀 신호등 스택·팀×카테고리 히트맵(✦핫스팟 클릭→AI 접이식)·미충족 TOP5([AI 초안›])·준비도 궤적(필요 vs 현 페이스=최근 4주 작성 이력)·팀별 책임양식 퍼널)+오늘할일 스트립+**AI 카드 6종 접이식 강등**(기능 유지)+AI 채점 현황 접이식. 신규 파일=dashboard/v3/* 8개. DASHBOARD_V5 확장(openCases·formsFillable·sqNewDrafts4w, 신규 채널 없음). 팔레트 dataviz 6검사 통과.
- **스몸크**: dev 실화면 스크린샷 — KPI(D-171·준비도12%·미충족30·정기의무10)·도넛·신호등·TOP·궤적 실데이터 렌더 OK. 기준일 UTC 밀림 수정. 렌더러 로그 클린. ⚠️`npm run dev | head` 금지(EPIPE 다이얼로그) — 로그파일 리다이렉트로.
- **설치판 재빌드×2 + 사일런트 설치**: 8:13(버그픽스판)→8:40(v3 포함 최종). 설치본 exe=8:40 검증. ⚠️NSIS /S를 bash에서 `./exe /S`로 띄우면 행 걸림 — PowerShell `Start-Process -ArgumentList '/S' -Wait`로.

**오후 추가 완료 (전부 라이브·푸시·설치판 11:13판 반영)**:
- **대시보드 v3 비율 수정**(6b4cc10, 사장님 피드백 "창은 큰데 비율이"): max-w 1420 고정이 범인 → 2200 해제+행높이 minmax(320px)+차트·타이포 전면 확대. 1936×1048 실화면 검증.
- **★전앱 UI 업그레이드 P1·P2**(dccac39, 제안 아티팩트 e787cccf 확정: A안+파스텔·팀7색 유지·토큰 위임): 원칙="파스텔은 면에, 딥블루는 점에". P1=app.css @theme 파스텔 블루 스왑+브랜드 violet 제거(카테고리 식별색은 유지)+**ClauseCoverageView 부서색=team-theme 정본 교정**(검수 지적 해소). P2=`shared/PageHeader`(제목 20px 규격, **새 화면 필수 사용**)+11화면 이식(에이전트 위임, 실화면 3종 검증). 예외 4=대시보드(밴드)·불량대책서/양식단독(작업공간형)·프로세스작업장(브레드크럼).
- **★사업 로드맵 결정+딥리서치**(보고 아티팩트 8766529e, 공고원문 3표검증): 판매주체=**본인 신설 사업자**(TPC 순서: TPC 적용→명칭 제거→판매). ⚡핵심=**사업자등록 서두르지 말 것** — 예비창업패키지(4천만·자부담0)가 최적, 자격="사업자등록 無"뿐(재직자 금지조항 없음 원문검증, 연령무관). 타임라인: 10월 PSST 준비→12월 통합공고(확정 기점)→2027.2~3 ★예창패 접수→선정 후 협약말미 사업자등록→2028 초창패·디딤돌(AI 업력10년 예외)·공급기업 연쇄. 11월 데이터바우처 공급기업=사업자 필요라 예창패와 배타(건너뛰기 권장). NIPA 산업맞춤형 혁신바우처=법인한정·부적합. 상세=메모리 plan-productization.

**▶ 다음**:
1. **UI P3**(심사장 시연 대비): 글자 크기 배율 옵션(사이드바)·제품 정보 화면(버전·제조사)·포커스/대비 점검.
2. **사업 선행과제**: 클린채널(TPC팩 분리, 2~3주 공사)·TPC와 IP 서면 정리·데모 시나리오 → 10월 PSST 사업계획서 착수. 공고 모니터링 루틴은 사장님 "걸어줘" 대기.
3. 검수 잔여: 🟡죽은코드 2덩어리(domain/ 11파일·register.ts v4 19채널=삭제안전 검증됨) + TPC 하드코딩 나머지(팩 분리와 묶어).
4. 개발팀 감사 잔여: 부실 셀맵 5건 보강 / 팀원 피드백 → Phase 3 판단 / 히트맵 핫스팟→모의심사 직접 연결(MockAuditCard initialItemKey).
5. ⚠️스몸크 함정 메모: dev 좀비 인스턴스 있으면 캡처가 엉뚱한 창을 잡음 — 스몸크 전 `taskkill //F //IM electron.exe`. `npm run dev | head` 금지(EPIPE). NSIS /S=PowerShell Start-Process -Wait.

## 🟢 7/07 — 설치판 재빌드×2 + 제품화 3건 + FMEA 구판 재반전

**완료**:
- **설치판 재빌드+재설치 2회**(8:38, 9:03): .env 클린 검증 통과. 9:03판 = 어제 전체+오늘 오전 3건 포함.
- **제품화 3건**: singleInstanceLock(설치판만, dev 병행 유지) · 마이그 전 DB 스냅샷(VACUUM INTO→userData/backups, 최신5, 수동복구) · AUDIT_DATE→company_profile.auditDate(Sidebar 심사일 UI).
- **★FMEA 신판→구판 재반전**(팀 회의 결정): 삼보 외 1차사 신판 미도입, ISIR도 구판 진행 → 제출 표준=구판 4판. 마이그 `0058`=J1101-01 소프트폐기 해제(0049 되돌림, 이름 복원). 신판 모듈은 사이드바 '공정 FMEA (신판)'으로 재라벨, 전환 대비 보존. SQ 1_1(표준류)의 J-1101 매핑 자동 회복. 메모리 finding-pfmea-format 갱신됨. ⚠️0058은 앱 재시작 시 적용(스냅샷 첫 실전 가동 예정).
- **개발팀 허브 데이터 감사**(사장님 질문): 화면=DB 일치 확인. 발견 갭 → 아래 ▶2.

**추가 완료(오전 후반)**:
- **개발팀 감사 후속 ①③ + 정직화**(마이그 `0059`, 라이브 적용됨): 규정문서 행 5건 등록(H-1100 계약검토·J-1100 개발업무·J-1101 공정FMEA지침·J-4100 도면관리·A-6300 품질문제신고포상 — 이름·책임부서=정본 파일명 근거) + A6300-01 resp_dept=품질보증팀 + 지배 프로세스 매핑. **작성불가 2건은 셀맵 대신 정직화**: J3100-04=기준표(참고문서, 입력칸 원래 없음=정상) / J1100-14=123행×99열 5페이지 대형 검토서 → 대형/격자 파이프라인 후속 대상(반쪽 셀맵 금지). description 에 명시.
- **form_approvals 스키마 씨앗**(마이그 `0060`, 라이브 적용됨): 결재 sign-off 상태머신 저장구조 선행(submission_id FK·step·role·status). UI/IPC 미구현 — 다중사용자+PIN 때 사용.
- **시드 분리 규칙 문서화**: `docs/마이그레이션_시드분리_규칙.md` — 0058부터 스키마/데이터 분리, TPC팩 후보 표기. 제품화 '지금 미리' 전항목 완료.

**추가 완료(오후, 사무실)**:
- **설치판 스팟체크 5종 전부 검증**: AI키 실호출 OK(sonnet-4-5)·이중실행 잠금 실전테스트 OK(2차 인스턴스 즉시종료)·규정명/구판FMEA/심사일 DB 확인.
- **★SQ 미배정 18항목 해소**(0061 스키마+0062 데이터, 라이브 적용): ⚠️첫 배정안(카테고리 추정)을 사장님이 지적("구매팀이 왜 수입검사?") → **BOM 역산으로 재작성**. 실규정 매핑 14건(J-1103·M-1200·L-2100·L-1100·L-1200·M-4200·B-2300·A-6300 — 수입검사=품질보증팀!) + 생산팀 폴백 3건(1_4·6_10·6_11, sq_items.fallback_dept — 6_10은 B-1100 매핑 금지=분배엔진 거짓green 함정) + 2_9 대기 유지. 결과: 팀별 품질16·생기12·생산11·개발4·구매4·영업1·총무1, 미배정=2_9 단 1건. 구매팀 카드 설명에서 '수입검사' 제거(team-theme.ts).
- TEAM_SUMMARY 폴백 로직 추가(team-handlers.ts) — **⚠️폴백 3건 표시는 재빌드 후**(14건 매핑은 데이터라 현 설치판에도 즉시 표시).

**야간 완료**: ~~4차 재빌드~~(21:58 설치·실행, 0061/0062 번들) + ~~커밋·푸시~~(HEAD 2f18de7 이후 검수보고서 커밋) + **전체 검수(3에이전트 병렬)** → **`docs/전체검수_2026-07-07.md`** — Critical 0. 수정권고: 🔴버그 3(★StepCard catch 부재=영구 스피너 확정 / 스냅샷 보존정렬 / readinessPct NaN 가드) 🟠하드코딩 13곳(실명 4건 최우선) 🟡죽은코드 2덩어리(domain/ 11파일·register.ts v4 19채널 540줄=삭제안전 검증됨)+중복 6계열.

**▶ 다음 (내일 7/8)**:
1. **검수 후속 1순위**: 🔴버그 3건 수정(StepCard catch+3상태 / migrate.ts mtime 정렬 / wsum||1) → 재빌드. 보고서=docs/전체검수_2026-07-07.md.
2. 실명 하드코딩 5건 선제 제거 검토(서상규×2·이인창·이정훈·김권표 경로) — 클린채널 전이라도.
3. 팀원 피드백 → Phase 3/4 판단. / 판단 필요 4건(보고서 하단: DOCGEN 부활 여부·예약 채널·persons 시드·scripts 아카이브).
4. 개발팀 감사 잔여: 부실 셀맵 5건 보강. J1100-14 대형 파이프라인은 M-series 격자와 함께.

## 🟢 7/06 — 캔버스 순회검수 + 팀별 허브 UI 재편(Phase1+2) + 보안·제품화 감사

**완료 (전부 라이브·푸시)**:
- **캔버스 순회검수**(a34d240): 154양식 전수 → GREEN 92%. 표준화 양식 셀맵 보강(0057, B2100-03 2→18/21 등) + ALIASES 8건 + MAX_COLS 80.
- **🔐 .env 번들 차단**(4c9b70e): 설치판 API키 유출 경로 제거 — userData/.env로 이동, 구 인스톨러 폐기. **⚠️운영원칙: 클린 배포판 전까지 설치파일 외부 반출 금지.** 제품화 감사 전문=메모리 plan-productization(블로커9·3단계 로드맵·팩 아키텍처).
- **★팀별 허브 UI 재편 Phase1+2**(4073be4·6081858·e0ee5c8): 사장님 지시서(06_UIUX_팀별허브_재편_지시서_260706)+7팀 확정(총무→영업→구매→생산→생산기술→품질경영보증→개발 = resp_dept 1:1). 첫 화면=7팀 컬러 허브(shared/team-theme.ts 토큰, 빨강=경고 예약) → 팀 상세(SQ 항목 번호단계, 배점순) → **단계 펼침: 요구사항→지침 본문(regulation_sections 인라인)→필요 양식(작성가능/등록만)→[작성]=form-builder·[AI 초안]=AiAuthorModal**. TEAM_SUMMARY IPC(team-handlers, computeSqReadiness 재사용 렌즈). 기존 대시보드는 '매일 관리' 방 보존. ⚠️React effect deps 함정(무한로딩) 수정=e0ee5c8.

**추가(오후, 팀 회의 피드백)**: 사장님 "BOM에 다 따놨는데 왜 비어보이나" → 정확한 지적. 팀 상세에 **② 책임 규정·양식 섹션**(ce8e175) 추가 — TEAM_REGS IPC(forms.resp_dept 기준 규정→하위양식 트리) + 지침 펼침(본문 인라인+양식 [작성]/[AI초안]). 총무팀도 규정 26종 표시. + 무한로딩 버그 수정(e0ee5c8, React effect deps 함정).

**▶ 다음 (사무실에서)**:
1. ~~**설치판 재빌드+재설치**~~ ✅ **7/7 완료** — `build:win`+사일런트 설치(설치본 exe 8:38 갱신). 보안검증: 패키지 resources·app.asar·설치경로 전부 .env 0건. userData/.env에 CLAUDE_API_KEY 존재 확인 → AI 복구 전제 충족. **남은 스팟체크(사용자)**: 설치판에서 ✨코파일럿 1회 질문으로 AI 응답 확인.
2. 팀원 피드백 반영 → 지시서 Phase 3(사이드바 5방+SQ⇄IATF 렌즈 토글)·Phase 4(대시보드 다이어트) 판단. SQ 미배정 18항목은 카테고리→팀 폴백 배정 검토(사장님 확정 필요: 생산조건→생산·검사시험→품질·설비/금형→생산기술·자재→구매·품질경영체제→품질).
3. 제품화 '지금 미리' 잔여 — **7/7 오전 3건 완료**: ~~singleInstanceLock~~(설치판만 잠금, dev+설치판 병행 워크플로우 유지, index.ts) · ~~마이그 전 DB 스냅샷~~(migrate.ts VACUUM INTO→userData/backups, 최신5개 보존, 라이브DB 12.4MB=118ms 검증. **자동복구 아님**: 1차방어=마이그 트랜잭션 롤백, 스냅샷=잘못 커밋된 시드·파일손상 대비 수동복구) · ~~AUDIT_DATE→company_profile.auditDate~~(Sidebar 심사일 편집 UI+useDday 반응형 캐시+브리핑 폴백, 마이그 불필요=시드 백필). typecheck+빌드3타깃 OK. **남음**: 0058부터 스키마/데이터 시드 분리 규칙(문서화) · form_approvals 스키마 씨앗. ⚠️설치판은 8:38 빌드라 오늘 오전 3건 미포함 — 퇴근 전 재빌드 권장.

## 🟢 7/03 — 정본 전량적재 + 엑셀형 작성(양식 캔버스) PoC + APQP 실데이터 연동 (최신 · 콜드스타트 1순위)

**완료 (라이브·검증)**:
- **APQP↔실데이터 연동**: APQP_BOARD가 FMEA/MSA/PPAP/ISIR CP 실데이터를 결정론 집계 → 산출물에 🔗증거칩 + 상태 어긋나면 [⚡반영] 원클릭(ApqpEvidenceDto). 자동 덮어쓰기 없음(A레일 철학).
- **정본 전량 적재**(마이그 `0056`, 654KB): 규정 본문 51개 추출·적재 → **57규정 열람 가능**(이전 4개), KB 464→762청크(코파일럿이 전 규정 인용). + 미보유 양식 27개 등록(M-series 22→CP-03 등) → **양식 237/237 전량**. ⚠️본문에 흐름도 병렬컬럼 섞임(열람·검색용 수용). 추출기=scratchpad extract_reg_bodies.py.
- **★양식 캔버스(엑셀형 작성 화면) PoC**: 리서치(워크플로우 17에이전트, 8후보 조사→검증→종합, 상세=메모리 plan-excel-authoring-ux) 결론=자체 캔버스(1순위)+엑셀 체크아웃(2순위·미구현). 구현: `src/main/docgen/render-model.ts`(ExcelJS→RenderModel: 병합·테두리·테마색tint·폭높이px, 세션캐시) + IPC `FORM_RENDER_MODEL`(editCells=출력엔진 norm/ALIASES 브리지 재사용) + `ExcelSheetView.tsx`(colgroup+table 원본재현, 셀맵 좌표만 노란 입력셀, formStore 양방향) + FormCanvas **[입력/엑셀 뷰/문서] 3모드 토글**. 검증=B2100-01 브리지 10/10·시트 35×32·병합 67. 저장/공식출력 무변경(단방향 원본주입).

**▶ 다음**: ① 캔버스 확대 — 자주 쓰는 양식 순회 검수(양식별 렌더 품질)·격자(대장형) 지원 v2·이미지(로고) 표시 검토 ② Phase1 '엑셀로 작성' 체크아웃 채널(시트보호+chokidar, 캔버스 실패 양식 폴백) ③ 규정 본문 정제(흐름도 컬럼 분리) ④ 기각 라이브러리 재검토 금지(메모리 plan-excel-authoring-ux에 이유 기록됨).

**★데스크톱 설치판(같은날 완료, 39292b4)**: NSIS 설치파일(`dist/IATF16949 품질경영시스템 Setup 1.0.0.exe`) + 바탕화면 'IATF16949 QMS' 바로가기 + 방패체크 아이콘(build/icon.ico). **함정수정 3**: userData 고정(index.ts — 설치판·dev 같은 %APPDATA%/iatf16949-manager DB, 데이터 연속성 검증됨)·.env extraResources 번들(AI키)·아이콘 생성. **운영 워크플로우**: 평소=바탕화면 아이콘 / 개발확인=dev 앱(동시 켜도 데이터 안전, 헷갈림만 주의) / 기능 확정 시 `npm run build:win`+재설치(/S 사일런트)로 아이콘=최신 유지. 재빌드 시 dev앱·설치판 종료 후.

## 🟢 7/02 — APQP 여정 + 오늘할일 보드 + 메뉴 4그룹 재편 (최신 · 콜드스타트 1순위)

**완료 (라이브 적용·검증)**:
- **APQP 여정**(마이그 `0055`): 코워크 명세 v5 이식 — apqp_phases(5단계)+apqp_elements(43산출물, core_tool·clause/team FK). 순차 스테퍼 UI(현재단계 하이라이트·단계 진척바·접이식 산출물 테이블: 상태·목표일 인라인) + **Core Tool 딥링크**(FMEA/PPAP/MSA/CP→해당 모듈, SPC=준비중). 파일: apqp-handlers·apqpStore·ApqpView. 사이드바 'APQP 여정'.
- **오늘 할 일 보드**(DAILY_BOARD IPC + DailyBoardCard, 대시보드 최상단): ①정기의무 도래(연체/임박=lead_days) ②SQ 🔴미충족 중 양식작성으로 해결 가능한 것(배점순) ③draft 이어쓰기(클릭→pendingSubmissionId로 작성본 바로 열림).
- **메뉴 4그룹 재편**(Sidebar): 매일 관리(대시보드·불량대책서·일정표·정기의무) / APQP·Core Tool(APQP·ISIR·PPAP·FMEA·MSA) / 심사 대응(SQ준비도·조항커버리지) / 문서·양식(BOM·양식단독·프로세스작업장). 기능 제거 없음(프로세스 작업장=흐름도 등록·AI추출 고유기능 유지).

**아침 재진단(중요 정정)**: "132양식 미적재/필드미정의" 전제는 부정확 — 기존 번호양식(D1100·J3100 등)은 대부분 **이미 작성가능**(0014/0027/0028 시드). 0053의 규정 플레이스홀더를 실양식으로 착각했던 것. **진짜 갭 = 생산 M-series(M-1100/1200/2100/3100/4100) 통째 미적재(격자형=격자 파이프라인 필요)** + E/D-21xx(ISO14001/45001=후순위). 상세=메모리 finding-process-reg-completeness.

**▶ 다음 후보**: ① 생산 M-series 격자 적재(작업일보·관리대장류— 격자 파이프라인) ② FMEA 문서헤더 편집기+새문서 버튼(현재 고객칸만 편집가능) ③ APQP 산출물↔실데이터 자동연동(예: FMEA 문서 있으면 apqp-3-06 자동 완료) ④ SPC=사용자 엑셀 대기.

> **📋 이번 세션(6/25) 핸드오프 보고서+지시서 = [docs/핸드오프_2026-06-25_양식출력엔진.md](docs/핸드오프_2026-06-25_양식출력엔진.md)**
> 양식 출력엔진(방향B) 작업을 이어받을 땐 **그 문서부터** 읽을 것. 완료보고·자산위치·다음지시·함정 전부 정리됨.

## 🟢 7/01 — 프로세스↔규정 정본정합화 + 조항커버리지 + FMEA마무리 (최신 · 콜드스타트 1순위)

> 상세 메모리: [[finding-process-reg-completeness]] · [[finding-pfmea-format]] · [[project-core-tools]].
> ⚠️ 마이그 0049~0053 = 앱 재시작 시 자동적용(이번 세션 이미 라이브 적용·검증 완료).

**이번 세션 완료 (라이브 적용·typecheck+빌드+DB사본/라이브 검증)**:
- **FMEA 마무리**: ① in-app 출력 활성화 — fmea-export가 폼출력엔진 `resolveMastersDir`로 단일통일 + **Sidebar '정본 폴더' 설정 UI**(IPC `COMPANY_PICK_MASTERS_DIR`, company_profile.mastersDir). ② **구판 J1101-01 소프트폐기**(마이그 `0049`, forms.deprecated/replacement_page + FormCanvas 배너·'공정 FMEA 열기' 리다이렉트). ③ **C5 고객명**(마이그 `0050`, fmea_documents.customer + FmeaView 고객 입력칸 + 출력 C5).
- **프로세스 관련규정 전면 정합화**(마이그 `0051`·`0052`·`0053`): 정답지 = **품질환경매뉴얼 REV.7 0.7 조항매트릭스**(TPC-M-07, 74튜플/69규정, `IATF 전체 자료모음_김권표이사_260501\1.품질&환경 메뉴얼_230501\…REV.7…_완료.xlsx`). SP-03 14/14 + **전 9프로세스 정본일치**. 미적재 28규정 문서등록(필드미정의, code=reg_code) + 프로세스 매핑 + `forms.resp_dept`·`iatf_clause` 컬럼·전규정 기입. **CP-03 생산관리 3→22종**(갭지도 지적 M·E·D 대량누락 해소). ⚠️MP라벨슬립 보정(0.7 인적자원=MP-02 → 앱 MP-03; 앱 MP-02=리스크).
- **조항 커버리지 뷰(신규)**: 사이드바 **'조항 커버리지'**(유령 PageId `clause-tree` 재사용) → IATF 4~10장별 규정/프로세스/책임팀 + 빈조항🔴. IPC `CLAUSE_COVERAGE`(process-handlers) + `ClauseCoverageView.tsx`. + BomProcessDetail 관련양식에 **책임팀 배지**(ProcessFormRefDto.respDept).

**▶ 사무실에서 다음 (우선순위)**:
1. **① 규정별 하위양식 셀맵 적재 = "작성·공식출력 가능하게"** (가장 큰 실작업). 등록된 28규정 + 총 ~132 미적재양식은 현재 **"필드 미정의"**(목록만·작성/출력 불가). `scripts/extract-all-full.mjs`로 정본 xlsx 양식시트에서 (라벨·셀·타입) 추출 → `form_fields`+`form_cell_map` 시드 → 앱 작성기+공식 xlsx 출력 활성. **한 번에 X, 실무빈도·심사중요 양식부터 하나씩.** **조항 커버리지 뷰 8장 운용(36개)이 우선순위 지도.**
2. **라이브 스팟체크(사장님)**: ① Sidebar '정본 폴더' 지정 → FMEA '신판 시트 출력' 실출력(C5 고객 포함) ② 구판 J1101-01 진입 시 배너·리다이렉트 ③ '조항 커버리지' 뷰 ④ 프로세스 상세 책임팀 배지.
3. **FMEA 문서 헤더 편집기 부재** — 현재 FmeaView는 고객칸만 편집 가능, 나머지 헤더(FMEA번호·품명·책임·차종·팀·양산일)·새문서 버튼 없음(store엔 createDoc/updateDoc 존재, UI 미연결). 실작성 위해 후속.
4. 앱>정본 초과 프로세스(CP-02 9>8·CP-03 22>21) = 기존 초과분 비파괴 유지 중, 정리 여부 판단.

## 🟢 6/28 — P2 #1 런타임 ISIR 임포트 UI 완료

> 결정: ①6/27 ISIR P1 먼저 커밋(`5bf28d7`) ②임포트는 **TS 포팅(ExcelJS)** — python 런타임 의존 0.

**완료 (커밋 `feat(isir): P2 #1 런타임 임포트`)**:
- **순수 파서** `src/main/ingest/isir-parser.ts`: ingest-isir.py 충실 포팅. `parseIsirWorkbook(wb)→ParsedIsir`(표지 part메타+26종, 검사협정 IRE/QA/rev, 관리계획서을 행그룹핑). DB·electron 의존 0(독립 테스트 가능).
  - ⚠️**병합셀 함정**: ExcelJS는 세로병합 마스터값을 슬레이브 셀까지 채움 → 항목NO(H)가 연속행마다 채워져 110→211 오분리. `cell.isMerged && cell.master.address!==cell.address` 로 슬레이브 빈값화(openpyxl read_only 복제)해 해결. S()도 \r·\n 순차치환으로 원본과 바이트 일치.
- **임포트** `src/main/ingest/isir-import.ts`: `importIsirFromFile(db, path)` — ExcelJS read→parse→트랜잭션 적재→reindexKb. **동일(part_no,rev_code) 재임포트=교체(멱등)**, 0042 UNIQUE 호환. 고객 추정(경로 토큰→조부모폴더 "NN. " 제거→수요자), plant 기본 '2공장', 빈값→NULL(nz).
- **IPC/UI** `PARTS_IMPORT_ISIR` 채널 + isir-handlers(파일다이얼로그) + ipc-types(IsirImportResult) + partsStore(importIsir/importing/importNotice) + PartsView('ISIR 적재' 버튼 + 성공/실패 배너).
- **검증**: 파서=0041 골드 **바이트 일치**(헤더·26종·110항목/31공정·연속행 결합). Python sqlite 등가성 = 런타임적재 **== 마이그 0041** (parts/packages/문서26/관리항목110 행단위 일치 + 재임포트 멱등 1/26/110). typecheck(node+web)+빌드 3타깃 OK.
- **⚠️ 남은 스팟체크(사용자)**: 런타임 UI 클릭 — '품번/ISIR' 우상단 'ISIR 적재' → xlsx 선택 → 목록갱신·배너·상세 표시. (헤드리스 검증은 끝, 실클릭만 남음.)

**▶ 다음(6/28 이어서) — P2 #2~**: 아래 6/27 블록의 P2 리스트 #2부터(다품번 배치 적재 — 이제 파서가 TS로 있으니 폴더 일괄선택 multi-import 로 확장. submit_type 추정강화·forward-fill·latestPackage rev_date정렬 등 이월결함 함께).

## 🟢 6/27 — ISIR/관리계획서 척추 P1 완료 (최신 · 콜드스타트 1순위)

> 도메인 통찰(사장님): **ISIR 서류(검사협정+관리계획서) = 모든 통제의 근간/통칭** — 고객과 "이렇게 관리하겠다" 협의한 문서묶음. SQ 공정감사가 이 ISIR을 근간으로 진행. 2nd 핵심 = 불량 개선대책서. 실 ISIR xlsx(현대위아 28237-2MAA1, 24시트 = 한 부품 통제패키지 전체, 모든 핵심시트가 ISIR제출용/공정감사용 쌍)를 까서 모델링. 플랜 = `C:\Users\ASUS\.claude\plans\encapsulated-greeting-lark.md`.

**라이브 완료 (레퍼런스 1품번 28237-2MAA1, end-to-end)**:
- **스키마** `0040_isir_spine.sql`: parts / isir_packages / isir_documents(26종 체크리스트) / control_plan_items(관리계획서 을 라인아이템). `0042`: isir_packages(part_no,rev_code) UNIQUE 방어.
- **적재** `scripts/ingest-isir.py`(원본 읽기전용 파싱→표지26종+검사협정IRE+관리계획서을 행그룹핑) → `0041_isir_28237_2maa1.sql`. 결과 = ISIR 1·관리항목 110/31공정·불량 1(QC-2026-0001 자동조인).
- **AI/IPC** `src/main/ai/isir.ts`(computeIsirCompleteness 결정론[설변기준 필수26중 미제출2=공정감사평가표·4M변경]/explainIsirGap AI) + `ipc/isir-handlers.ts`(PARTS_LIST/PART_DETAIL/AI_ISIR_COMPLETENESS/AI_ISIR_EXPLAIN) + `kb.ts` reindex 확장(control_plan110+part1 색인, FTS "포밍 외경"→28237-2MAA1#2 그라운딩OK).
- **UI** 사이드바 '품번/ISIR'(PartsView/PartDetail/partsStore) + 대시보드 IsirCompletenessCard.
- **검증**: typecheck(node+web)+빌드+실DB사본 통합+라이브 재시작(마이그 적용·396청크). **검수(독립 2에이전트+직접) 후 실결함 수정**: partsStore race가드(loadingDetail/동일품번 재선택 깜빡임), 0042 UNIQUE, 죽은 db param/cell()/글리프.

**▶ 내일(6/28) — P2 (우선순위순)**:
1. **런타임 임포트 UI** — 앱 안에서 xlsx 선택→파싱→DB 적재(빌드타임 python 마이그레이션 대체). ingest-isir.py 로직을 TS 포팅 or 파이프 호출.
2. **다품번 배치 적재** — 현대위아/삼보 승인본 다수. ★이때 검수 이월결함 해결: ingest-isir.py를 **argv화**(SRC 하드코딩 제거)·**submit_type 추정 강화**(align 휴리스틱이 agreement 오분류·동률편향)·**공정/설비 forward-fill 견고화**(cur_equip 누수)·**분담 글리프 일반화**·**latestPackage를 rev_date 정렬로**(현재 id DESC).
3. **cases ↔ parts FK** — 불량 이력 정합(현재 part_no 문자열 조인, 표기 정규화 미보증).
4. **FMEA 상세표** — isir_fmea_items(공정FMEA 시트, RPN).
5. **E1/E3 재정의** — 심사예측·모의심사를 일반양식(65/1000)이 아닌 **Control Plan 준수도** 기준으로.
6. **정기검사(1번 폴더) 연결** + **관리항목 주기 vs 성적서 freshness 부재감지**(F1 ISIR판 심화).

**검수 이월(저위험, P2에서 같이)**: PartsView 전체구독→셀렉터, IsirCompletenessCard PARTS_LIST 이중호출→store 단일화, PartDetail `it.special` truthy 명시화, 불필요 `as` 캐스팅. (zustand 무한루프 함정은 이번 추가분에 없음 확인.)

## 🤖 6/26 저녁 — AI 레이어 A→E1 전부 완료

> 설계서 `05_AI레이어_구현플랜`(claude.ai 제공) 채택, 전제교정(pack 뺌·기존 src/main/ai 흡수·벡터→FTS5). origin HEAD `6c8e479`.

**한 세션에 AI 레이어 "첫 2주" 전부 라이브** (마이그 0037~0039, 파일 `src/main/ai/{drafts,tools,gateway,kb,briefing,author,readiness}.ts` + `ipc/ai-handlers.ts`):
- **A 레일** (0037 ai_drafts/ai_actions): AI 쓰기는 ai_drafts에만 → 사람 approve로만 공식테이블 commit → ai_actions 감사. 게이트웨이(gateway.ts tool-use루프·캐싱·재시도·비용로깅) + 도구레지스트리(tools.ts read4/draft5, zod).
- **B 그라운딩** (0038 kb_chunks+kb_fts trigram): reindex 285청크(규정·SQ·양식·케이스·프로세스). searchKnowledge 하이브리드(FTS+다중어LIKE). 기동 시 reindex.
- **C1 코파일럿**(GlobalCopilot, TopBar ✨버튼): 우리 데이터 자연어 Q&A, 근거칩, read-only.
- **C2 브리핑**(BriefingCard, Dashboard): 마감초과/임박·심사Dday·SQ증빙누락 신호등 + AI요약.
- **D 캡처→초안→결재**(AiAuthorModal, TopBar 🪄버튼): 메모→AI가 B2100-01 등 초안→검토→승인(form_submission). raw_captures(0039).
- **E1 SQ 심사예측**(ReadinessCard, Dashboard): 결정론 점수(현재 65/1000)+손실항목+AI진단·우선순위.

**검증**: 전부 typecheck node+web + 헤드리스 end-to-end(실제 Claude+실제 데이터, 환각0) + 라이브 기동(0037~0039 적용·285청크). **⚠️ 런타임 UI 클릭(코파일럿·브리핑·AI작성·심사예측 4버튼)은 사용자 스팟체크 남음.**

**다음 후보**: 시크릿 회수 — **#4 부재감지 일반화(갭지도→expectedSet 룰엔진=F1, 추천1순위)** / #2 수용률 플라이휠 UI(ai_drafts.edit_diff 집계) / G 제품화(라이선스·비용대시보드) / 또는 양식 더 표준화해 실제 SQ점수↑. 상세=repo `05_AI레이어_구현플랜.md`(있으면) + 아래 정본PDF 블록(미반영분 #2 연결고리·#3 빈규정27 양식수집은 여전히 유효).

---

## 🌅 (이전) 정본 PDF 학습 + "쉬운 작성" 전환

**맥락(사용자 피드백)**: 뷰어로는 좋은데 **실제 작성 도구로는 불편/부족**하다. 핵심 니즈 =
① 정본 PDF를 읽어 도메인 정확도·연결고리 업데이트 ② 작성 편하게(노션/엑셀 복붙) ③ MES 자료 복붙→CSV→항목 자동분배 ④ 중간 이미지 삽입.

**새 자료**: `D:\IATF16949,SQ 자동작성 봇\IATF16949 캡쳐본 모음\` = 정본 PDF 캡쳐본 **36개**(메뉴얼23/프로세스9/규정4=A-1100만). 규정(B~M)·프로세스 나머지는 순차로 더 들어올 예정.

**오늘 한 것 = CP-03 생산관리 1장 읽고 앱과 대조(데모)**. 발견(중요):
- 앱 `process_forms`의 CP-03 = **L-1100 설비(24)+L-1200 치공구(11)=35개뿐**. 정작 정본 CP-03 흐름의 핵심(작업지시 M-1100/M-1200·자주검사 L-2100·완성품검사·4M변경 J-3100·시정조치 B-2100·완성품관리 M-3100)은 미반영. → 앱은 "양식의 소유 프로세스"만 모델링(검사 L-2100→SP-02). **"프로세스가 사용하는 양식 흐름(many-to-many)" 차원이 없음** = "연결고리"의 정체.
- **성과지표(KSSI)**: `process_revisions`에 kpi/formula/cycle/owner 컬럼 있으나 **CP-03은 0/7 전부 비어있음**. 정본 표지/개정표에 8종(생산계획달성율·설비가동율·공정불량율·입고불량율·완성품불량율·납기율·일인당생산성·조치율) 산출식·주기·책임자 다 있음 → 그대로 채울 수 있음.
- **빈 규정(양식 0개)**: A-2100·M-1100·M-1200·M-3100·M-4100 → 폴더3 규정 PDF가 채울 대상.
- **자주검사 체크시트**: 정본 C/A 핵심 산출물인데 **앱에 없음**(L-2100엔 수입·인수검사만). → MES 복붙 파일럿 후보로 딱.

**✅ 6/26 진행**: (C) 정본 9개 프로세스 + 0.6/0.7 매트릭스 전부 읽고 대조 → **갭지도 완성** = `docs/정본_연결고리_갭지도_2026-06-26.md`. 이어서 갭지도 §5 **#1 KSSI 일괄반영 완료**(0036, 위 미적용 참고). origin HEAD 20a2eaf.
**남은 우선순위(갭지도 §5)**: #2 연결고리 차원(`process_form_flow`, PDCA 단계별 양식 흐름) / #3 빈 규정 27개 양식수집(M-시리즈 생산본연 우선, 규정 PDF 더 필요) / #4 자주검사 체크시트 MES 파일럿.

**참고 — 당초 선택지(완료/이관)**:
- ~~(A) 자주검사 체크시트 파일럿~~ → 갭지도 #4로 이관(MES 자료 대기)
- ~~(B) CP-03 성과지표 반영~~ → (1) KSSI 일괄반영(9개 전체)으로 확장 완료
- ~~(C) 프로세스/규정 PDF 읽기~~ → 완료(갭지도)

**미해결 빌드 갭**: photo 필드는 입력칸만 있고 **공식 엑셀 출력에 이미지 주입은 미구현**(셀맵에 photo 타입 없음) → ④ 이미지 니즈 = 새 빌드 필요.

**콜드스타트**: 이 블록 → 그다음 핸드오프 "진행 갱신 2"(어제 h/f/g). 정본 대조 스크립트 참고는 scratchpad(세션밖, 재작성 가능): cp03_crosscheck.py.

## ⚠️ 미적용 마이그레이션 — 다음 앱 재시작 시 자동 적용
- **0036_seed_process_kssi.sql** ((1) KSSI 일괄반영): 정본 9개 프로세스 개정이력+성과지표를 process_revisions에 시드. 8개 프로세스 개정이력 6행 INSERT(KPI 초기행) + CP-03 기존행 KPI UPDATE. KPI 채워진행 18. 생성기 `scripts/seed-process-kssi.py`. 코드변경 없음(ProcessCoverDocument가 이미 렌더). 오프라인 시뮬 검증 완료.
  - ⚠️BEGIN/COMMIT 없음(migrate.ts가 db.transaction()으로 감쌈).
  - **적용 후 스팟체크**: 문서BOM 프로세스 상세 → 문서모드 → 개정이력 표에 성과지표/산출식/주기/책임자 채워졌는지(예 SP-02 4종, CP-03 3종).

> 0026~0035 은 적용 완료(0034 라벨·0035 개정관리 = 6/26 앱기동 시 적용됨).

## ✅ 2026-06-25 완료 (이 세션): B2100-01 증명 + 도메인지도 + 갭2 교정
- B2100-01 end-to-end 증명(ExcelJS 보존 무손실 + 값주입 + Excel COM PDF). 매핑=CSV(가) 확정.
- 도메인 지도: 정본 IATF전체자료 72xlsx/561시트, 앱↔정본 구조일치(프로세스9·regA~M·부서7). 고객=삼보+ZF. TPC 13만개=참고서.
- 갭2 정합성: 앱 process_forms ↔ 정본 0.6 = 40%일치. 분해(미수집27 + 오배치~10 + 분류차이~5) → 0026으로 오배치·분류 교정. 미수집27(E·M·D 양식부재)은 갭1에서 수집.
- **갭1 범위·설계 확정**: 72파일 323양식시트 → 폼형 **267**(83%, 자동화 타겟) / 격자 14 / 대장·매트릭스 42. 폼형 분포 검사L-2100(43)·설비L-1100(21)·자격F-2100(14) 집중.
  - **핵심 통찰**: 앱은 B2100-01만 s1~s10 수동정의, 나머지 262 폼형은 필드 미정의. → **셀맵 추출(label,cell,type)이 곧 field 정의**이므로 손정의 불필요. ①이 ②를 흡수.
  - **갭1 설계**: ①267폼형 셀맵 대량추출(=field+셀매핑) ②범용 출력엔진(fill-b2100 범용화: form+셀맵+값→엑셀→PDF) ③앱 IPC통합(작성→공식엑셀 출력 버튼). ③이 앱 코드변경(DB/IPC/UI) 신중작업.
  - 도구: scripts/extract-all.mjs(다중시트 셀맵), classify-forms.mjs(유형분류), fill-b2100.mjs(주입), probe-b2100.mjs. _demo_output/양식유형분류.csv·B2100_전체_셀맵.csv.
  - **✅ 갭1① 완료(이 세션)**: extract-all-full.mjs로 267폼형 셀맵 대량추출 → **3520건**(평균13.2). 품질분포 5건+ 70%(186)/1-4건 16%(43)/0건 14%(38실패). 샘플검증 B1100-01 **10/10정확**(발행번호K3·LOT E6·품번O6·발생일자E5·작성자O4·불량수량O5...), B2100-01 9/10. **셀맵 label이 앱 분배필드와 연결됨**(작성일자↔h2·발생일자↔i1·LOT↔i4·품명↔i3) = ②의 다리. 산출물 `_demo_output/전체_폼형_셀맵.csv`. 보강대상=실패38+부분43(후순위).
  - **다음 세션 1순위 = 갭1②③ 구현**(②fill-b2100 범용화: form+셀맵+값→엑셀→PDF / ③앱 IPC통합: 작성→공식엑셀 버튼) 또는 0026 적용검증 먼저.

## 🎯 내일(6/25) 1순위 — 제품 방향 전환 결정됨 (B: 작성 대체)

**결정(2026-06-24)**: 이 앱은 *뷰어*가 아니라 **여기서 모든 양식을 작성 → 공식 엑셀로 출력 → (점차) 앱으로 직접 심사**받는 도구다. 엑셀로 도망가지 않는다.

**핵심 깨달음 — 렌더러로 양식 똑같이 그리기(막혔던 것)는 버린다. 대신:**
> 앱에선 데이터만 입력 → 출력은 **원본 양식 엑셀(.xlsx)에 값을 꽂아서 생성**(ExcelJS read+write). 결재란·갑지/을지·레이아웃 전부 원본 그대로 = 100% 공식 양식. 입력 UI는 단순해도 됨. 양식별 "필드→엑셀 셀" 매핑표만 있으면 됨.

**내일 바로: 1개 양식 end-to-end 증명** — `B2100-01 시정/예방조치 요구서`로:
1. 원본 엑셀 위치: `IATF 전체 자료모음_김권표이사_260501/3.IATF16949 규정&지침_230501/B-2100 시정조치 규정...xlsx` 의 `(시정·예방)조치 요구서 (B2100-01)` 시트.
2. 앱 입력값(form_submissions.values_json: s1~s10) → 그 시트의 해당 셀에 ExcelJS로 write → `.xlsx` 저장/내보내기.
3. 셀 매핑(s6=부적합사항 → 어느 셀 등)은 시트 구조 까서 1:1 정의. 되면 "이 길이 맞다" 확정 → 218개로 확장.

**확장 로드맵(B)**: ① 엑셀 원본→양식 필드 대량 자동생성(손으로 깎지 말 것) ② 채운 값→공식 엑셀 출력 엔진+양식별 셀맵 ③ 심사 신뢰성(결재/승인 흐름·개정관리·작성자/일시 추적·접근통제). 분배엔진(한 번 입력→여러 양식)이 B의 심장. 기존 BOM/필드/AI추출/사업부분류 전부 재료로 쓰임.

## 현재 상태 (콜드스타트 1순위)
- 브랜치: `feature/v5-soft-reset`, origin 동기화. 최신 HEAD = 검수수정 커밋(아래 참조)
- 앱 실행: `cd iatf16949-manager && npm run dev` (끌 땐 창 닫기 = WAL 안전)
- DB: `%APPDATA%/iatf16949-manager/iatf16949.db` (점검은 `python sqlite3 file:...?mode=ro`)
- 설계 단일출처: `docs/사건중심_8D흐름_분배맵_설계.md`

## 최근 완료 (2026-06-20~21)
**SQ 평가 백본 + 사건중심 8D + 분배엔진** — 코워크 SQ Ver4 백본(6대·42항목·1000점)을 앱에 통합.
forms.reg_code ↔ sq_reg_map 으로 218개 양식이 SQ 항목에 자동 연결.
- **SQ 준비도**(사이드바): 42항목 신호등(🟢충족/🟡진행/🔴미충족/⬜미해당). 0012 + sq-handlers + sq-readiness/*.
- **사건 작업**(사이드바): 고객 불량 통보 → 8D 단계 → 선별(사내재고=생산/고객사=품질) → 원인·대책.
  0013 + case-handlers + case-work/*.
- **분배 엔진**: 케이스 공유사실 → B1100-01·B2100-01 작성본 자동생성(case_id 연결) → SQ 작성본수 롤업.
- **PPT 본문 추출 시드**: `scripts/seed_defect_cases.py` (대책서 PPTX 라벨 파싱, 데모 12건, owner='[샘플]').
- **전체 검수(멀티에이전트) + 수정 반영**: SQ green 규칙 재정의(도달가능+gray), 마이그레이션 원자성,
  채점 submissionId 경합, nextSerial 중복제거(database/serial.ts), 분배 작성일자 자동, '하헌'/'1000점' 하드코딩 제거.

## 다음 할 일
- [x] ~~**B-2100→2_9(리크/리워크) 매핑 도메인 확인**~~ (2026-06-24 완료): 진단 결과 B2100-01「시정/예방조치 요구서」가 2_9를 **거짓 green**(분배엔진 자동작성본) 점등. 실제 증거인 리크/리워크 검사양식은 forms에 0건. 도메인 결정=매핑 제거 → `0019_unmap_b2100_leak.sql`로 B-2100→2_9만 삭제(6_7은 유지). 2_9는 전용 양식 생길 때까지 gray(측정불가)=정직신호. 향후 전용 리크/리워크 양식 신설 시 sq_reg_map에 그 reg_code→2_9 추가하면 실증거 점등.
- [x] ~~**218 forms 마이그레이션화**~~ (2026-06-23 완료): `0014_seed_all_forms.sql` 로 forms 218 + process_forms 218 시드 고정.
  생성기 `scripts/dump-forms-seed.mjs`, 검증기 `scripts/verify-migrations.mjs`. 클린설치 재현 = live와 parity(forms/pf/fields/layout 일치, orphan 0).
- [x] ~~**8D 흐름 양식 표준화**~~ (2026-06-24 완료): B2100-03(개선대책서,`0020`,IMP) + B1100-05(봉쇄 작업표,`0021`,CTN) + H3200-02(고객불만 대응결과,`0022`,CCR) 표준화+분배. 표준화 양식 2→5개. H3200-01은 다행 관리대장이라 단일레코드 폼 모델 부적합 → 스킵. (렌더러 2D매트릭스/사진전후 미지원 → 실용 선형 필드셋 방침). 라이브는 다음 앱 재시작 시 0021/0022 자동 적용.
- [~] **PPT→필드 AI 추출** (파이프라인 완성, 적재 교체 대기): 2단 — `scripts/dump-defect-texts.py`(PPTX→원문 텍스트 JSON, 무과금) → `scripts/ai-extract-cases.mjs`(@anthropic-ai/sdk 구조화 추출, OS temp 출력). 42건 코퍼스, 5건 샘플 품질 정규식 압도(원인·근본대책이 완결 문장으로 정제). 남음: 전체 42건 배치(과금) + AI JSON → cases 적재(seed_defect_cases.py를 AI소스로 교체, DB 변경). env-loader가 electron 결합이라 generate() 직접 import 불가 → 동일 SDK 직접 호출로 우회.
- [x] ~~**LOT 입력 경로**~~ (2026-06-24 완료, `eb7a26b`): 접수폼에 'LOT NO' 필드 + CASE_CREATE가 case_facts.lot upsert + 상세 LOT FactBox. 분배 B1100-01 i4 갭 해소. CaseIntakeInput.lot 타입 추가.
- [~] **로그인/작성자**: 작성자 설정 ✅완료(2026-06-24, `b10ac80`) — Sidebar 인라인 편집으로 company_profile.defaultAuthor 변경(양식 자동채움/분배 즉시 반영). 단일사용자 내부툴이라 풀 로그인은 보류(과잉). 남음: (선택) 풀 로그인 시스템, 다른 양식 복제, AI프롬프트 내 인명 컨텍스트 동기화.
- [~] **양식 공통/사업부별 분류** (1단계 완료, `ba7896f`): 프로세스 관련양식이 사업부마다 공통/전용 갈리는데 모델에 차원 없던 것. `forms.scope`(common/division, 0023) + FORM_SET_SCOPE + BomProcessDetail 배지(🔵공통/🟠사업부별) 클릭 토글. 기본 전부 common, 사업부별만 flip. **2단계(향후)**: 사업부 테이블 + division_forms(사업부×양식 적용 매트릭스) — 공통=전 사업부, 전용=일부. 용접팩→타업종 일반화 기반. 라이브는 다음 앱 재시작 시 0023 자동 적용.

## 주의/메모
- SQ평가 = 삼보(HKMC) Ver4 브레이징 단일표준 가정(시드 하드코딩). 다업종 확장 시 백본 JSON 교체.
- 데모 시드/‘샘플 채우기’ 버튼은 dev 편의용 — 출하 시 정리 방침 필요(단일사용자 내부툴이라 현재는 low).
- AUDIT_DATE = `2026-12-31` 데모 임시값(useDday.ts).
- 핵심 파일: ipc/{sq-handlers,case-handlers,form-handlers}.ts, database/{migrate,serial}.ts,
  presentation/components/{sq-readiness,case-work}/*, stores/{formStore,uiStore}.ts,
  resources/migrations/0012_sq_backbone.sql, 0013_cases_8d.sql
