-- ============================================================
-- Migration 0023: forms.scope — 전사 공통 / 사업부별 분류 (1단계 경량 태그)
--
-- 배경: 각 프로세스의 관련 양식이 사업부마다 겹치는 것(공통)과 별개인 것(전용)이 섞임.
--   현 모델엔 사업부/공통 구분 차원이 전무 → 단일 사업부(AM) 플랫 세트.
--   1단계로 양식 단위 binary 태그를 도입해 BOM 관련양식에서 분류·표현한다.
--   (사업부 × 양식 적용 매트릭스는 다음 단계 — 본 태그를 기반으로 확장 가능)
--
-- 기본값 'common': 규정 원본이 '1,2,3공장 공통적용'으로 설계돼 대다수 시스템 양식이
--   전사 공통이므로, 전부 common 으로 두고 사업부별 전용(용접·도금 등 특화)만 flip 한다.
--   scope ∈ {'common'(전사 공통), 'division'(사업부별 전용)}.
-- ============================================================

ALTER TABLE forms ADD COLUMN scope TEXT NOT NULL DEFAULT 'common';
