-- ============================================================
-- Migration 0141: 작업지시 상태 전이 감사 열 (P2″ — 8/11 검수 서버 Minor) (2026-08-12)
-- [스키마 전용 마이그레이션 — 데이터/시드 없음]
--
-- 발견(8/11): workOrderUpsert 갱신 경로가 무주체 '취소' 전이 허용 — 주체·사유 요구 검토.
-- 검토 결정(8/12 처분): 지시는 계획 앵커(가역 상태 전이 — 기록 5종의 append-only 와 다른 축)
--   → 사유 강제는 과잉, **전이 주체·시각 각인**으로 결정. 사유가 필요한 취소는 note 로 남긴다.
--   렌더러 확인창(8/12 MA-9)이 오클릭 방어를 이미 담당 — 여기는 "누가 언제"의 감사 축.
-- ⚠️BEGIN/COMMIT 없음(migrate.ts 트랜잭션 래핑).
-- ============================================================

ALTER TABLE work_order ADD COLUMN status_by TEXT;   -- 마지막 상태 전이 주체(STAMP)
ALTER TABLE work_order ADD COLUMN status_at TEXT;   -- 마지막 상태 전이 시각
