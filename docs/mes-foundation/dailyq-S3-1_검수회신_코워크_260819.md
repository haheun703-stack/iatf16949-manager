# S3-1 표준팩 SQ층 — 검수 회신 (2026-08-19 저녁, 코워크) — **조건부 합격**

메커니즘·누출축은 실측 통과, ④-4 재작성 **품질**만 결과 텍스트 미도달로 조건 1건.

## 1. ④-3 편차(67건) — 코워크 판정: **67 유지 권고 (제외 추가 불요)**

- 실측: 레거시 의무 73건 제목 전수 판독 → 봇 제외 6건 확인(브레이징 조건·스포트 팁·MES 정합·MES 다운로드·중금속 MS201-02·내식성 MS611-15) → **유지 67건에 TPC 고유 식별자(현대/기아/모비스·MS코드·품번·티피씨) 잔존 = 0**.
- 유일한 경계 항목 "고객특별요구(CSR) 갱신 확인" = **업종중립 개념**(어느 공급사나 고객 CSR 추적 의무 존재, 특정 고객명 없음) → 유지 정당.
- 판단: 67은 **ⓑ의 과잉이 아니라 충실한 이행**입니다(조직 무관 일상업무가 실제로 많았을 뿐). 판매판에서 풍부한 기본 의무 캘린더는 **기능**(고객은 불요분을 지우면 됨 — 없는 걸 새로 쓰는 것보다 쉬움). assignee·도래일 전부 NULL(역할 기반)이라 개인정보·TPC 일정도 안 실림. → **추가 제외 없이 67로 확정.**

## 2. 레거시 무해 — 독립 재현 통과

- :8081 pid 36292·copy:true, `forms 302`·`obligations 73` 불변, SQ 심사 뷰/자체평가 정상 렌더. 팩 6파일 OR IGNORE no-op 실물 확인. TPC 운영 상태(assignee·cp 상태·kpi 목표) 유지 = 레거시 축 무영향.

## 3. ④-4 재작성 품질 — **조건(결과 텍스트 미도달, 러버스탬프 회피)**

- 브레이징 재작성분(sq_items 4·guides 11)의 **결과 텍스트는 팩 파일/클린설치에만** 존재. 레거시 복사본(:8081)은 원문(TPC)만 보이고, 클린설치 서버(:8082·8083)는 미기동(연결 거부 실측)이라 **코워크가 재작성 후 텍스트에 도달할 경로가 없습니다.** 안 본 텍스트를 합격 처리하지 않겠습니다.
- **품질 기준(재작성이 지켜야 할 것)**: ①SQ 심사 요지 보존 — 심사원이 무슨 증거를 보는지(예: "접합부 조건 기록의 존재·주기·이탈 시 조치")가 남아야 함 ②공정 고유성 제거 — "브레이징/스포트" → "특수공정/접합공정" 등 일반화하되 심사 체크가능성 유지 ③가짜 중립화 금지 — 내용을 비우고 이름만 바꾸면 반쪽.

## 4. ★코워크 제안 — S3 내내 **표준팩 클린설치 검수 서버 상설화**

- S3-1 ④-4, S3-2(규정 뼈대 **안내문 신규 집필**·forms 302 중립판)까지 **판매판 실제 콘텐츠 품질**은 전부 클린설치에서만 보입니다. 매번 before/after를 repo에 붙이는 것보다, 봇이 **표준팩 클린설치 서버를 고정 포트(:8083 권장)·검수 표지·E2E봇 시드로 상설 기동**해 주면 코워크가 레거시(:8081)와 나란히 대조하며 실물 검수할 수 있습니다.
  - 조건(42호 D 소견 연동): 이 클린 서버도 `IATF_REVIEW_COPY=1` 계열로 **검수 표지+E2E봇 한정**, 실기록 금지 fail-closed.
- 즉시 대안(서버 상설 전): ④-4 15행 **before/after를 검수요청에 첨부**해 주시면 이번 건만 텍스트 검수 후 종결.

## 5. 판정 요약

- **④-3 = 67 확정**(코워크 판정, 사장님 도장 불요 — 도장 ⓑ 범위 내 이행 확인). **레거시 무해·⑥⑦ 0행·⑩ GREEN = 접수.**
- **④-4 = 조건부**: 클린설치 도달(서버 상설화 or 15행 첨부) 후 품질 종결. 그때까지 S3-1 전체는 **조건부 합격**(메커니즘 합격, 콘텐츠 품질 1건 보류).
- 다음(S3-2)도 콘텐츠 검수가 핵심이니 **§4 서버 상설화가 M1 마무리(8/27) 순항의 관건** — 채택 권고.
