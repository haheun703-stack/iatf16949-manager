# dailyq-G1 — 수집함 착수 배치 검수요청 (2026-08-05, 사무실 봇)

> **첫 줄 하네스**: E2E `scripts/e2e-g1-inbox.mjs` **27/27 통과**(검증 서버 :8081·라이브 복사본) ·
> verify-migrations **클린설치 133개 전체 적용·FK 위반 0** · typecheck node+web 통과 ·
> build(렌더러)+build:server 통과. 라이브(:8080) 무접촉 — 복사본에만 임시비번 시드(7/29 선례).
>
> **근거**: 29번 §8-⑥ 도장(8/4) + §10-1(코워크 재지적) + `M1_선행분_견적서_260728.md`(코워크 합격).
> 견적 2.5일(B-1 0.5 + B-2 1.5 + B-3 0.5) 범위 전체를 한 배치로 이행.

## ★검수 항목 1호 (코워크 8/4 재지적 이행) — §10-1 필드 포함 여부

**포함 완료 — 마이그 `0136_g1_receipt_inbox_and_revision.sql`.** 쓰기 경로 스키마가 처음 굳는
본 배치에 반영(기록이 쌓인 뒤의 소급 문제 봉쇄):

| §10-1 | 조치 | 구현 | E2E 단언 |
| --- | --- | --- | --- |
| ⓐ insp_spec revision 축 | 테이블 재생성(SQLite 제약 변경 불가) — `revision INTEGER DEFAULT 0` + `rev_date` 추가, **UNIQUE(item_code, insp_kind, insp_item, revision)** → 개정 = 신규 행·구판 불변 | 0136 §① | `unique=[item_code,insp_kind,insp_item,revision]` ✓ |
| ⓑ insp_record 3필드 | `spec_revision`(기록 시점 스냅샷 — 참조 금지) + `sample_phase`(초품/중품/종품 CHECK) + `confirmer`/`confirmed_at`(확인자 2단) | 0136 §② | 4컬럼 존재 ✓ |
| ⓒ 확인자 ✓는 사람 | 컬럼만 선반영 — 확인 액션 채널은 PC 단계(검사기록 쓰기 채널과 동시) 명세. 대필 경로 없음(현재 어떤 코드도 미기입) | — | 기존 행 NULL = 선-리비전 시대(소급 기입 없음) |

- 재생성 안전 근거(실측): insp_spec·insp_record 는 0101 이후 **어떤 코드도 참조하지 않음**
  (grep 전수 — 마이그 SQL 뿐). 기록 0행인 지금이 마지막 안전 창.
- ⚠️B-1 의 mat_receipt.capture_id·receipt_class 는 **0106 기적용**(7/28 선행분) 실측 확인 —
  0136 은 §10-1 몫만. (착수 시 0136 에 중복 포함했다가 클린설치 게이트가 잡아 정정 — 게이트 실증.)

## 이행 내역 (B-1 → B-2 → B-3)

### B-2 태깅 화면 + 채널 (M1 §2 설계 그대로)

- **화면** = `ReceiptInboxView.tsx`(신규, PageId `receipt-inbox`) — 템플릿 C: 좌 380px 목록
  (탭 = 숫자 밴드 클릭: 미분류·오늘 입고·오늘 출하·전체) + 우 사진 뷰어/태깅 폼.
  사이드바 "입고 사진함(disabled)" 예약석을 실화면으로 교체. 풋터 DB v0101→v0136 갱신.
- 폼 = 유형(입고/출하 정정 가능)·전표일자·거래처(마스터 datalist, 유형 표시)·입고 구분
  (partner_type 초안 자동)·**[품목 행 추가]**(1:N)·업체LOT(공란 허용 명기)·비고(단가·금액 금지 명기).
  미선택 시 = 요약 카드(미분류 n·오래된 미분류 TOP 5 — 규칙④).
- 업로드 = [입고 전표]/[출하 전표] 버튼 2개(M1 §3 1차 판별) → 캔버스 다운스케일(긴 변 2000px·
  JPEG 0.85 — 서버 8MB JSON 한도 안).
- **채널 5종**(semimes-handlers 편승): `captureList`·`captureCreate`·`captureImage`·`captureTag`·
  `itemSearch`. **견적 2~3개 → 실측 5개**(정직 고지): 웹에서 사진 표출은 정적 노출 없이 채널
  (data URL)로 — 서버 파일 라우트 신설 회피가 +1, 품번 검색 경량 채널 분리가 +1.
- 서버 가드 3곳: STAMP(captureCreate·captureTag → createdBy 세션 강제) + REQUIRED(3채널) +
  PROTECTED 무추가(= 로그인만 — form:submissionCreate 등 기존 쓰기 채널과 동일 급. 이견 시 반전).
- 태깅 계약(핸들러 검증): 거래처·품번 마스터 실존 강제 / 수량 > 0 / 업체LOT 공란 허용 /
  **재태깅 거부**(append-only — 정정 = 취소+재등록, 취소 채널은 후속 검수 트랙) /
  출하 = mat_receipt 0행(content 만 — 15번 경계) / content = JSON 명세 v1 그대로(M4 판독 계약).
- INLOTUSE: item_master.inlotuse=1 이고 업체LOT 기입 시에만 internal_lot 승계 — 자체 발번은
  PC 단계 lotIssue 소관(선점 안 함).

### B-3 수입검사 데이터 트리거 (M3 확장 — 신규 엔진 0)

- `trigger-engine.ts` 의 예약석 `insp_due` 활성화 = **T4**: 태깅 수불(capture_id 有) 발생일 →
  "[수입검사 도래] {입고일} 입고분 — 이력카드 당일 기입(SQ 2_2)" 품질팀 할 일 발행.
  중복 방지 = UNIQUE(트리거, 'receipt-insp', 입고일) — M1 §4 키 그대로. lazy 창 14일
  (설치 첫날 과거 폭탄 방지 — T3 선례).
- 해소 = 당일 insp_record(수입) **또는 L2100-07 작성기록** 감지 시 해소표시만 — ✓는 사람(M3 규칙②).
  원천 정정(수불 부재)만 자동 취소(사유 기록).
- 보드 합류: T4 제목 분기 + **당일 = due**(overdue 아님 — "당일 기입" 의무의 정직한 상태) 1줄.

## 검증 실물

| 게이트 | 결과 |
| --- | --- |
| E2E 27/27 (`e2e-g1-inbox.mjs`) | §10-1 스키마 6단언 → 업로드 → **STAMP 위조 무시 실증**(촬영자·태깅자 모두 세션명) → 다품목 태깅 = **mat_receipt 2행**(사진 1:N) → 재태깅·무등록 거래처/품번·수량 0 거부 → **T4 발행(당일 due) → L2100-07 기입 → 해소표시** → 출하 0행 |
| verify-migrations | 클린설치 133개 전체 적용 · forms 302 · FK 위반 0 |
| typecheck node+web / build / build:server | 전부 통과 |
| 라이브 안전 | 검증 전 과정 복사본(:8081) — 라이브 무접근. 임시비번 시드 복사본 한정 |

- 정직 고지 ①: E2E 사진은 512B 더미 바이트(데이터 흐름 검증용) — **전표 실물 4장
  (`입고출하전표_샘플_260728/`) 태깅 리허설 + 화면 캡처는 코워크 재현 검증 몫**으로 남김
  (M1 §5 "전표 4장이 시드" — 자기검수 회피).
- 정직 고지 ②: 채널 수 견적 초과(2~3 → 5) 사유 상기 — 서버 표면 무확장이 교환 조건.

## 판정 요청 (사후 확인형 — 이의 시 해당 항만 반전)

1. **§10-1 필드 구현 방식**: sample_phase 값 = '초품/중품/종품' 3값 CHECK · confirmer 단일 컬럼
   (검사자 inspector 와 2단) · spec_revision INTEGER — 3차 노트 문법 번역이 이대로 맞는가.
2. **PROTECTED 무추가**(수집함 쓰기 = 로그인만) — 태깅을 특정 role 로 좁힐 필요가 있는가.
3. **T4 해소 신호 2종**(insp_record 수입 ∨ L2100-07 작성) — L2100-07 하드코드 대신 양식 매핑
   테이블로 넓힐지(현행 P1b 커버리지 매핑과 동형이라 하드코드 채택).

## 다음

- 코워크: 본 검수(★1호 = §10-1 포함 여부) + 전표 4장 실물 태깅 재현 + 캡처.
- 봇: PB2 홈 재편(30번 v2 정본, 0134·0135) 병행 착수 — 파일 겹침 없음 확인(G1 = semimes/ingest,
  PB2 = 홈/메뉴/테마).
- ⚠️W4 설치판 재빌드 범위 **0136+**. ⚠️라이브 웹서버 = 다음 재기동 시 0136 자동 적용.

---

## 추기 (8/5) — 코워크 문서 검수 회신 접수 + 재현 대비 마스터 대조

**문서 검수 판정 접수(코워크, 8/5)**: ★1호 **합격**(revision UNIQUE 축 + spec_revision 스냅샷 =
소급 봉쇄 정확 · 기록 0행 시점 재생성 근거 타당). 판정 3건 = ①§10-1 구현 방식 **승인**
②PROTECTED 무추가 **조건부 승인** — 조건: **W4 비번 정책 결정 테이블(29번 §8-④)에서 태깅
채널 role 축소 여부 재상정** (PA 배치 등재) ③T4 L2100-07 하드코드 **현행 채택 동의**(해소
신호 양식 3종 초과 시 매핑 테이블화). 잔여 = 전표 4장 화면 재현·캡처(Chrome 확장 연결 후).

**재현 대비 마스터 대조(봇, 복사본 실측)** — 태깅은 품번·거래처 마스터 실존 강제 계약이므로 선대조:

| 전표 | 거래처(코드·유형) | 품번 실존 | 재현 방침 |
| --- | --- | --- | --- |
| 입고1 세일에스앤티 | 1301 · **고객** | **5품목 전량 마스터 부재**(24451\*·22452\*·24411\* 계열 0건) | **"품번 미등록 거부" 실증 사례로 채택**(거부 화면 캡처 = 마스터 실존 강제 계약의 증거). 1:N 다품목 재현은 입고3으로 |
| 입고2 SW테크 | 1302 · **고객** | 28236-2MAA0 ✓ · 25457-2MHE0 ✓ | 2품목 1:N + 업체LOT 공란 재현. 입고 구분 = **수동 '외주재입고'**(하단 소견) |
| 입고3 휴먼테크 | 1713 · **고객** | 45264-3D900-O2 ✓ · 28237-2MAA1-P1 ✓ · 28236-2MAA0-4 ✓ | 3품목 1:N + 전표일자 7/27(과거일) 재현. 입고 구분 수동 |
| 출하1 TPC→SW테크 | 1302 | 28236-2MAA0-4(EYE-JOINT) ✓ | 출하 = mat_receipt 0행 검증 |

**소견 2건(정비 후보 — 본 배치 밖)**:

1. **partner_type 불일치**: 전표상 공급자/외주처인 세일에스앤티·SW테크·휴먼테크가 마스터에
   전부 '고객'으로 등록 → receipt_class 자동 초안이 안 걸림(수동 선택으로 진행 가능, 기능 무영향).
   partner_type 정비 동선(M0 기준정보 트랙) 편승 후보.
2. **AS/니쁠류 품번 마스터 부재**: 입고1 계열(24451\*·22452\*·24411\*)이 item_master 0건 —
   M0 적재 범위(pop_item/BOM) 밖 품번으로 추정. 실입고가 계속되는 품목이면 마스터 등록
   동선 필요(임의 시드 금지 — 기준정보 오너 확인 후).
