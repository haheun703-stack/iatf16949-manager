# P1 ⓒ 품번×공정 매트릭스 — 마일스톤 검수요청 (2026-07-31, 사무실 봇 → 코워크)

ⓐⓑ 당일 이어 **ⓒ 완료** — R1 확정분(빌더 확장 +0.5일) 실행. 신규 채널 1
(`mesRecords:partProcess`)·신규 테이블 = **사이드카만**(subul_monthly — 앱 DB 무변·마이그 0,
M1 §2.5 계약 유지)·P1 예약 마이그 0125+ 소진 0 유지.

## §1 구현

- **빌더 확장**(`scripts/mes_records_build.py` Pass 3): POP_JEPUM_SUBUL(624,781행) →
  사이드카 `subul_monthly`(month×pno×iogbn → qty·rows·last_ymd + 인덱스 2 + meta
  subul_stats). 미래 일자 제외 동일 규율. **자재(POP_JAJE_SUBUL 335만행)는 R1 §2-3 범위 밖
  유지** 명기. 재빌드 실증 = 51.2초·subul_monthly **9,452키**·기존 수치(sqc_daily 111,564 등)
  전량 동일 재현·사이드카 11MB.
- **채널** `mesRecords:partProcess`({ymd?, limit?}): 정렬 = subul_monthly IOGBN='SO'
  **데이터 있는 최신 월**(달력월 아님 — 미반입 기간 왜곡 방지) 상위 N. 셀 = sqc_daily
  품번×공정(ⓑ와 동일 WRKCTR 매핑·수입검사 I 직귀속) 기준일 검사 항목 수 + 마지막 기록일.
  구 사이드카(subul_monthly 없음) 호환 = 정렬 비활성 정직 안내.
- **렌더러** `PartProcessMatrix.tsx` — ⓑ 아래 배치. 행 = 품번 상위 8(품명 동반), 셀 =
  ●N(기준일) / 마지막 기록일(흐림) / '·'(이력 없음), 끝열 = 기준월 SO 수량(정렬 근거 노출).

## §2 소견 2

1. **목업 B의 "오늘/진행 LOT" 끝열 → "월 수불량(SO)" 대체**: 진행 LOT 원천(lot_registry)이
   M1/P3 게이트 뒤라 — 가짜 표기 대신 정렬 근거를 그대로 노출. LOT 대장 가동 시 추가(범례 명기).
2. 실측 신호: 상위 품번 셀이 **조립·압입 공정에 집중**(자주검사가 조립 라인 J* 중심) —
   여타 공정 커버리지 공백이 시각화됨(커버리지 모드의 목적 그대로).

## §3 실증

- **CDP 실행 캡처** `captures/cap_p1c_part_matrix.png`(라이브): 기준월 2026-07 · 상위 =
  45264-3D900 TUBE ASSY-FEED 37,050 EA → D28350-2M100 27,540 → **28236-2MAA0 22,770**
  (SQ 심사 품번이 실수불 상위 실증) → 28237-2MAA1 19,900 → 45272-4G100 … · 셀 = 조립·압입
  7/16(마지막 기록일) 표출.
- **E2E(`e2e-p1b.mjs` 확장) 9/9** — ⓑ 3단 + ⓒ 2단(열 7 계약·사이드카 부재 시
  available=false·rows 0 정직). 검증 웹서버 **149채널** 재번들 편입 확인.
- typecheck 전판 · 빌더 재빌드 로그 실측치 일치.

## §4 다음

ⓓ 트리거 보드 승격 + ⓔ 의무 보드 하단 탭 이동(정보 손실 0) — PortalHome 재배치(채널 0)
→ **P1 완료 게이트**(28화면 회귀 + grid 8종 + 캡처 일괄 + 코워크 재현 검수).

⚠️새 덤프(P0) 반입 시 같은 빌더 명령으로 sqc/mac/subul 일괄 갱신(운영 편승 그대로).
⚠️W4 재빌드 = 0116~0124 + build:server(149채널) + 렌더러(P1 진행분).
