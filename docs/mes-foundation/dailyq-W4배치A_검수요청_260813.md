# W4 배치A 검수요청 — 서버 보안 재검 (2026-08-13)

> **첫 줄 하네스**: 신규 `scripts/e2e-w4a.mjs` **9/9** + 회귀 **118/118**(d35 12·pc1 27·g1 27·
> b3 24·b4 28 — **전부 E2E봇 계정 로그인** = STAMP 분리 실증 겸) + typecheck node+web + build.
> 대상 = :8081 **새 복사본**(qms-e2e-260813b · 단독 구성 픽스처 · 배치A 서버 코드로 기동 —
> 검수 시작 전 기동 완료, 검수 중 재기동 없음). 로그인 = 서규하/2222.
> **37호 도장(8/13)**: ①~④ 전건 승인 — 배치A = ④health + 편승 2건 + PROTECTED 점검.

## 1. health 무인증 슬림화 (37호 ④ — P1 보류 청산)

- 비로그인 `GET /api/health` = **`{ok:true}` 만**(DB 경로·양식/사용자 수·런타임 제거 —
  사내망 무인증 정보 노출 금지). 세션 시 상세 복원 + **copy 플래그** 동봉.
- 런처/자동시작(ServerAlive)·E2E 는 status 200 만 봄 — 무영향(실측: 기동·E2E 전 통과).

## 2. PROTECTED 확장 — 관리성 3종 (전수 점검 결과)

- **manager+ 로 격상**: `semimes:workCalendarSave`(조업달력 = 공유 분모) ·
  `kpi:indicatorSave`(KPI 기준) · `semimes:ppmTargetSave`(PPM 목표). member = 403.
- **격상하지 않은 것(근거)**: 현장 쓰기(실적·검사·LOT·지시) = member 유지 · 설비/금형 등록 =
  생산팀 주체(8/12 판정 ③) · 기준정보(품목/거래처/SPEC)는 화면×7종 정밀 통제가 정답 자리라
  **배치B(#19)로 이관**(채널 이중 잠금 회피).

## 3. 편승 ⑴ — E2E봇 계정 (8/13 오인 사고 후속)

- 복사본 시드가 'E2E봇'(manager·검수) 계정 보장 — **E2E 는 이 계정으로만 로그인**.
  기록 STAMP 가 'E2E봇'으로 각인돼 실무자 이름 혼입 0(양품 589 사건 재발 차단).
  이번 회귀 118건이 실증(모든 E2E 기록 주체 = E2E봇).

## 4. 편승 ⑵ — 검수 복사본 화면 표지

- IATF_DATA_DIR 지정 구동(=복사본 서버) → 타이틀 줄에 **적색 표지
  "검수 복사본 — 실기록 입력 금지"**(툴팁: 실기록은 :8080). 라이브/데스크톱 = 표지 없음.

## 5. 검수 동선 (코워크 — 36호 채널)

1. 무인증: `curl http://127.0.0.1:8081/api/health` → `{"ok":true}` 만인지.
2. :8081 로그인(서규하/2222) → 타이틀 줄 **적색 표지 육안 1회**.
3. member 계정(예: 김초연/qms1234)으로 조업달력 저장 시도 → 거부 확인(403 안내문).
4. `__invokeRate()` 등 기존 도구 정상(참고).

## 6. 라이브 반영 (판정 — 코워크 의견 요청)

- 서버 코드 변경(index.cjs)이라 **라이브 재기동 필요**. 오늘 이미 재기동 1회 소진 —
  **배치B 완료와 묶어 1회 재기동 제안**(그때까지 라이브 health 노출은 사내망 한정 지속).
  보안 우선으로 조기 반영이 낫다고 보면 회신에 명기 — `restart-qms-server.bat` 1회면 됨.
