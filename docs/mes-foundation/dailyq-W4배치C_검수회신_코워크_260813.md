# W4 배치C 검수 회신 (2026-08-13, 코워크) — **합격 (문서 검수 · 실물 관문 = M-1)**

- ① 설치판 산출물: 코워크 원격 접근 불가(dist는 브리지 연결 범위 밖) — **빌드 exit 0 · 마이그 141파일 소스 1:1 실측 · 코드 무변 패키징** 봇 자기 실측 신뢰로 접수. 실물 검증은 M-1이 겸함(신 Setup을 실제 설치·실행하는 행위 자체가 산출물 검증).
- ② 데스크톱 경로 상설 하네스(e2e-w4c-desktop 14/14 — 주체 빈 값 전부 거부 + DB 쓰기 0 검산): **8/6 교훈의 올바른 상설화** — 접수. IATF_DATA_DIR 게이트(라이브 보호)도 적절.
- ③ 정정 자기 신고(142→141개, 0054 결번): 3축 일치 소명 정직 — 접수. 이후 문서 표기 = 141 통일.
- ④ **W4 종결 조건 = M-1 사장님 확인 1회** (절차는 코워크가 사장님에게 직접 안내 — 아래 절차 그대로). 통과 시 W4 게이트 종결, 배포는 37호대로 별도 지시.

## M-1 절차 (사장님 30초 — 코워크 안내분)

1. `iatf16949-manager\dist\IATF16949 품질경영시스템 Setup 1.0.0.exe` 실행 → 설치(기존 앱 덮어씀).
2. 설치된 데일리Q 데스크톱 앱 실행.
3. **사용자를 선택하지 않은 상태**로 생산실적 등록(또는 작업지시) 화면에서 저장 시도.
4. **"사용자를 선택하세요" 거부 문구가 뜨면 통과** — "M-1 통과" 한마디로 종결 기록.
