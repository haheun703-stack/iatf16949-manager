# S3-1 ④-4 재작성 15행 — before/after 전문 (2026-08-19, 봇 · 기계 생성)

- before = 마이그 체인 원문(레거시=:8081 과 동일) · after = :8083 표준팩 클린설치 실물(팩 적용 결과 — 파일이 아니라 **설치된 DB에서 추출**).
- 코워크 품질 기준(회신 §3): ①심사 요지 보존 ②공정 고유성 제거 ③가짜 중립화 금지.

## A. sq_items 4행

### 1_5

**before(제목)**: 주요조건 기준수립 및 현장 관리 상태 비교검증 실시

**after(제목)**: 주요조건 기준수립 및 현장 관리 상태 비교검증 실시

**before(요구사항)**:
```
(로 브레이징)
   ▷ 가 용접 : 전류.전압.유량
   ▷ 주요조건 : 사양별 소재 장입량, 온도, 시간(콘베어속도). 용재량(동링.솔더)확인
   ▷ 로내 구간별 온도 실측관리    - 온도 타점기록계 관리 / 온도 프로파일 관리
   ▷ 로내 질소가스 비율 관리 기준 설정 및 준수상태
(수동 브레이징 용접)
   ▷ 용접 표준서 게시 (온도. 한도견본)
   ▷ 용접 작업자 인증 . 용접 검사로 보증
   ▷ 겹침 모재 적정틈새 유지 관리
   ▷ 용접조건 기준관리
    → 용접 조건 관리항목 : 가스 유량 경보장치 설치 미설치시 관리방안 수립 및 기준준수, 실행상태

※ 작업자 착용장갑에 의한 제품 오염(실오라기 등)으로 용접부 리크 발생이 없도록 점검
```

**after(요구사항)**:
```
(특수공정 보유 시 — 열처리·용접·도장·성형 등)
   ▷ 주요조건 : 사양별 조건(온도·시간·속도·전류·전압·유량·투입량 등) 기준 수립
   ▷ 구간별 실측관리 — 자동 기록계(타점·차트) 관리 / 조건 프로파일 정기 측정 관리
   ▷ 보조매체(가스·용재·약품 등) 기준 설정 및 준수상태
(수작업 공정)
   ▷ 작업표준서 현장 게시 (조건·한도견본)
   ▷ 작업자 인증 · 후속 검사로 보증
   ▷ 작업 조건 기준관리
    → 이상 감지장치(경보 등) 설치, 미설치 시 관리방안 수립 및 기준준수·실행상태

※ 작업자 착용구(장갑 등)에 의한 제품 오염으로 품질 결함이 발생하지 않도록 점검
```

### 2_8

**before(제목)**: 완제품 검사 기준 설정 및 준수상태

**after(제목)**: 완제품 검사 기준 설정 및 준수상태

**before(요구사항)**:
```
- 외관, 치수, 취약부 절단면 검사, 상대 물 조립 성 검증 등 (샘플 실측 요망)
    - 최종 검사 기준설정 및 시행상태, 검사이력 관리 (1회 이상 /3개월)
    → 브레이징 용접 : 용접부 용입성 평가 (절개 평가) 및 외관 크랙 및 LEAK 검사 진행
```

**after(요구사항)**:
```
- 외관, 치수, 취약부 검사, 상대물 조립성 검증 등 (샘플 실측 요망)
    - 최종 검사 기준설정 및 시행상태, 검사이력 관리 (1회 이상 /3개월)
    → 특수공정 보유 시 : 접합·처리부 파괴 평가(절개 등) 및 외관 결함·기밀(LEAK) 검사 진행
```

### 3_4

**before(제목)**: 작업전 설비 일상점검 항목 설정 및 실행상태 확인

**after(제목)**: 작업전 설비 일상점검 항목 설정 및 실행상태 확인

**before(요구사항)**:
```
- 용접기: 팁 교환. 노즐청소. 와이어 공급기. 케이블. 전류 전압.
    - 브레이징 로 : 로 내부온도 . 콘베어 속도 실측 . 가스유량 .메쉬망
    - 로봇 원점관리 : 로봇 용접 시작원점 일치 확인. 궤적 확인
     (시업전 원점확인/ 사진 전산관리 or 경보작동 : 원점확인 수치화 어려움)
```

**after(요구사항)**:
```
- 가공·조립 설비: 공구/소모부 교환 기준. 급유·청소. 케이블. 전류 전압.
    - 처리로(爐)·챔버형 설비 : 내부온도 . 이송 속도 실측 . 유량 . 소모부(메쉬 등)
    - 자동화 설비(로봇 등) 원점관리 : 시작원점 일치 확인. 궤적 확인
     (시업전 원점확인/ 사진 전산관리 or 경보작동 : 원점확인 수치화 어려움)
```

### 3_5

**before(제목)**: 브레이징 로 점검 항목 설정상태 및 시행 여부

**after(제목)**: 특수공정 설비(로 등) 점검 항목 설정상태 및 시행 여부

**before(요구사항)**:
```
- 브레이징 로내 온도 구간별 실측점검
    - 온도 프로파일 관리
    - 로 콘베어 메쉬 속도 실측점검
```

**after(요구사항)**:
```
- 특수공정 설비 내 조건 구간별 실측점검
    - 조건 프로파일 관리
    - 이송 장치(콘베어 등) 속도 실측점검
```

## B. sq_guides 11행

### 1_3|examples|1

**before**:
```
교육기록: `2026-06-10 응웬반A(베트남) | WS-12 로브레이징 작업표준 교육(베트남어 통역) | 숙지확인: 이상 발생 시 정지→호출 절차 문답 OK | 교육자 서명`
```

**after**:
```
교육기록: `2026-06-10 외국인 작업자A | WS-12 핵심공정 작업표준 교육(모국어 통역 병행) | 숙지확인: 이상 발생 시 정지→호출 절차 문답 OK | 교육자 서명`
```

### 1_5|evidence|1

**before**:
```
(로 브레이징) 가용접 전류·전압·유량 기준표 / 사양별 소재 장입량·온도·콘베어속도·용재량(동링/솔더) 기준 / 로내 구간별 온도 실측기록 + 타점기록계 차트 / 온도 프로파일 정기 측정 성적서 / 질소가스 비율 기준·점검기록
```

**after**:
```
(특수공정 예 — 로(爐)형) 주요조건(온도·시간·속도·유량 등) 기준표 / 사양별 투입·조건 기준 / 구간별 실측기록 + 자동 기록계 차트 / 조건 프로파일 정기 측정 성적서 / 보조매체(가스·용재 등) 기준·점검기록
```

### 1_5|evidence|2

**before**:
```
(수동 브레이징) 용접표준서 현장 게시(온도·한도견본) / 작업자 인증 기록(6_5 연계) / 겹침 모재 틈새 기준·점검 / 가스유량 경보장치 or 대체 관리기준
```

**after**:
```
(수작업 공정) 작업표준서 현장 게시(조건·한도견본) / 작업자 인증 기록(6_5 연계) / 작업부 관리 기준·점검 / 이상 감지장치(경보 등) or 대체 관리기준
```

### 2_8|evidence|3

**before**:
```
브레이징 특화: 용접부 용입성 절개평가 기록 + 크랙·LEAK 검사 기록
```

**after**:
```
특수공정 특화: 접합·처리부 파괴 평가(절개 등) 기록 + 외관 결함·기밀(LEAK) 검사 기록
```

### 2_9|examples|1

**before**:
```
`RW-0715-03 | 리크 NG 0.8cc/min 1EA (LOT B0715-1) | 재브레이징 후 재검사: 리크 0.3cc/min 적합 + 외관·용입 확인 | 판정 합격 | 리워크자/재검사자 서명 상이`
```

**after**:
```
`RW-0715-03 | 기밀 NG 1EA (LOT B0715-1) | 재작업 후 재검사: 측정값 기준 내 적합 + 외관·처리부 확인 | 판정 합격 | 리워크자/재검사자 서명 상이`
```

### 3_1|examples|1

**before**:
```
`브레이징로 #1 일상점검 07-15 | 에어압 0.52MPa(0.4~0.6 녹색범위) | N₂유량 12㎥/h(10~15) | 알람 테스트 ○ | KEY-LOCK 시건 ○ | NG마스터 검출 ○ | 점검자 서명 08:00`
```

**after**:
```
`핵심설비 #1 일상점검 07-15 | 에어압 0.52MPa(0.4~0.6 녹색범위) | 보조매체 유량 기준 내 | 알람 테스트 ○ | KEY-LOCK 시건 ○ | NG마스터 검출 ○ | 점검자 서명 08:00`
```

### 3_4|evidence|2

**before**:
```
브레이징 로: 내부온도·콘베어 속도 실측·가스유량·메쉬망 점검 기록
```

**after**:
```
처리로(爐)·챔버형 설비: 내부온도·이송 속도 실측·유량·소모부(메쉬 등) 점검 기록
```

### 3_5|examples|1

**before**:
```
`로 #1 프로파일 (2026-07-01, 프로파일러 TP-03 교정有) | 예열 620℃/브레이징 752℃/냉각 320℃ — 기준곡선 대비 편차 ±5℃ 이내 적합 | 차트 첨부 | 측정자/검토자`
```

**after**:
```
`설비 #1 조건 프로파일 (2026-07-01, 측정기 교정有) | 구간별 실측 — 기준곡선 대비 편차 허용범위 내 적합 | 차트 첨부 | 측정자/검토자`
```

### 5_1|examples|1

**before**:
```
모의 역추적: `완제품 LOT B0710-1 → 브레이징 07-10 로#1 (작업일보) → 파이프 LOT P-0708A·동링 C25-0702 (투입기록) → 수입검사 성적서 #IR-471/468 — 역추적 소요 8분, 실시자 서명`
```

**after**:
```
모의 역추적: `완제품 LOT B0710-1 → 핵심공정 07-10 설비#1 (작업일보) → 원자재 LOT P-0708A·부자재 C25-0702 (투입기록) → 수입검사 성적서 #IR-471/468 — 역추적 소요 8분, 실시자 서명`
```

### 6_10|examples|1

**before**:
```
`리워크 기준표 | 리크 미세누설: Rework 가능(고객협의 QM-25-112) — 재브레이징+재검사 | 크랙: 금지 → 폐기 | 치수 NG: 금지 → 폐기`
```

**after**:
```
`리워크 기준표 | 경미 결함: Rework 가능(고객협의 문서번호 기재) — 재작업+재검사 | 구조 결함(크랙 등): 금지 → 폐기 | 치수 NG: 금지 → 폐기`
```

### 6_11|how_to_write|1

**before**:
```
브레이징 예: 동링 사용량과 브레이징 완료 수량을 월 대사 — `동링 소진 10,250EA vs 생산 10,180EA, 차이 70EA(불량 폐기 65+낙하 5) 소명` 식으로 차이 원인까지 기재하면 우수 요소.
```

**after**:
```
부자재 대사 예: 주요 부자재 사용량과 완성 수량을 월 대사 — `부자재 소진 10,250EA vs 생산 10,180EA, 차이 70EA(불량 폐기 65+낙하 5) 소명` 식으로 차이 원인까지 기재하면 우수 요소.
```

