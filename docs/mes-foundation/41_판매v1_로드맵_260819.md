# 41호 — 데일리Q 판매 v1 로드맵 (2026-08-19, 봇 · 사장님 "계획이 없다" 지적에 대한 답)

- 목표 정의: **판매 v1 = 남의 회사 PC에 설치해서, 그 회사 이름으로 뜨고, 현장이 폰으로 기록을 넣고, SQ 심사 준비를 돌릴 수 있는 상태.**
  기준 = 29호(2·3차용 미니MES·모바일 퍼스트) + 33호(기본 = 미니MES+SQ 내장 · IATF = 라이선스 애드온) + 39·40호(TPC 종속 분리).
- 브랜드 = **데일리Q**(자체) · TPC 자료는 표준팩의 **구조·양식 원형**으로만 활용(40호 ④ 도장 — 코드 체계·양식 42종 채택), 규정 본문·실명·품번은 TPC팩으로 격리.

---

## 1. 지금 어디까지 왔나 (사실)

| 축 | 상태 | 근거 |
|---|---|---|
| 웹 타입 전환 | **완료** — 사내 웹 서버(:8080) 상시 운용, 로그인·권한(W4)·화면별 권한 매트릭스, 검수 서버 이중화, E2E 9종 170건 | 14호(7/23)~37호(8/13) |
| 미니MES 화면 | **완료** — 1차분 6 + 2차분 17 + 전광판 = 24화면, 툴바·KPI·조업달력·추적, 기록 쓰기 PC-1(direct 모드) | 32·34·35호 |
| SQ 내장 | **완료** — SQ Ver4 42항목 백본·가이드층·미니멀 팩 45양식·수집함 G1 | 0012·0064·0132~0135 |
| IATF 애드온 스위치 | **골격만** — `license.iatf_addon` 플래그 존재, 언락 UI·라이선스 키 발급 절차 없음 | 33호 |
| 회사 종속 제거 | **절반** — S1(코드 하드코딩 0)·S4(클린 설치 게이트 RED 478행) 완료, **S2·S3 미착수**(오늘 ④ 도장으로 착수 조건 충족) | 39·40호 |
| 현장 폰 입력 | **미착수** — 입력 화면(검사·실적)은 웹 화면으로 존재, 폰 최적화·스캔 UX·시료 복수·서명 = PC-2 | 29호 PB/PC |
| 설치 경험 | **없음** — 설치 마법사(회사 프로파일·관리자 계정·팩 선택) 미구현, 설치판(NSIS) 8/17 후순위 판정 | 38호 ④ |
| 판매 패키지 | **없음** — 데모 DB·사용 설명서·가격표 |

솔직한 자평: 8/6~8/17 두 주는 TPC 라이브 검수·처분(A~D군·M-9)에 썼습니다. 결함 봉합은 제품 자산이지만 **판매 마일스톤을 하나도 앞당기지 못했습니다** — 39호 §0에서 자기정정한 그대로이고, 오늘부터는 아래 표 순서로만 갑니다.

---

## 2. 마일스톤 (근무일 기준 · 8/20 수 착수)

| # | 마일스톤 | 내용 | 기간 | 완료 판정(기계) |
|---|---|---|---|---|
| **M1** | **클린 설치 GREEN** | S2 신규 설치 경로(팩 태깅 스킵·혼합 마이그 처리안) 2일 + S3 표준팩(시드 재편·가이드 중립화·의무 선별·양식 xlsx 42종 프로파일 치환·미추출 30종 추출·뼈대 템플릿) 4~5일 | 8/20~**8/27(수)** | `e2e-clean-install` **8/8**(TPC 0·실명 0·안내문 0) |
| **M2** | **설치 첫날 경험** | 설치 마법사(회사 프로파일 입력·관리자 1계정·팩 선택 SQ/IATF) + 설치판(NSIS 재개, 웹서버 모드 동봉) + 라이선스 플래그 언락 UI | 8/28~**9/2(화)** | 빈 PC 설치→"우리 회사"로 로그인 E2E 1종 |
| **M3** | **현장 폰 입력(PC-2)** | 폰 셸(`/m`) 최소판: 검사·실적 2화면 [조회]·[저장] 문법 + 카메라 스캔 + 시료 복수 + ⑥-1 서명 | 9/3~**9/8(월)** | 폰 실기 스톱워치(스캔→저장 3초) + E2E |
| **M4** | **판매 v1 패키지** | 표준팩 데모 DB · 사용 설명서(설치·첫날·현장·심사 4장) · 라이선스 키 발급 절차 · 가격표 초안(기본/IATF 애드온) | 9/9~**9/11(목)** | 사장님 시연 리허설 1회 |
| — | 버퍼(코워크 검수·재검) | 각 마일스톤 뒤 반나절 | | |

→ **판매 v1 내부 시연 목표 = 9/12(금).** 이후 = 첫 고객사 파일럿(9월 3~4주).

## 3. 전제 (안 지켜지면 위 날짜가 밀립니다)

1. **도장 창** — 마일스톤 착수 시 1회씩(M1 착수 도장 = 이 문서). 중간 판단은 코워크 채널로.
2. **라이브 운영 개입 최소** — TPC 라이브 이슈는 "멈춤·손실"급만 즉시, 나머지는 주 1회 묶음.
3. 코워크 검수 회신 **하루 이내**.
4. API 콘솔 자동 충전 유지(오늘 켜짐) — 앱 AI 경로 검수 가능.

## 4. 오늘 도장 요청

| # | 항목 | 판정 |
|---|---|---|
| ① | 판매 v1 정의(§0)와 M1~M4 순서 | ☐ 승인 ☐ 조정 |
| ② | 날짜(9/12 시연) — 전제 4건 포함 | ☐ 승인 ☐ 조정 |
| ③ | M1 즉시 착수(8/20) — S2 상신은 착수 첫날 오전에 올리고 오후부터 구현 | ☐ 승인 |
