# 2차분 배치⑶ 검수요청 — 지표·달력·추적 4화면 (2026-08-12)

> **첫 줄 하네스**: E2E `scripts/e2e-d34-b3.mjs` **24/24 통과 ×2회(멱등)** + 회귀 pc1 **26/26** ·
> g1 **27/27**(전부 :8081 오늘자 복사본) · typecheck node+web · build · build:server ·
> 클린설치 **139개 전체·FK 0**. 라이브(:8080) 재기동 완료 — 0139 적용·채널 186(+5) 실측.
> **대상**: 34호 대조표 배치⑶ = **#18 조업달력(승격) · #15 성과 지표 · #12 생산현황 차트 ·
> #16 추적 공정 흐름 밴드**. 검수 = :8081(서규하/2222 — 오늘 재설정분).

## 1. #18 조업달력 (그림31 · 골격) — 배치 선두 사유 = 분모의 원천

- **마이그 0139** `work_calendar`: 1일 1행(ymd PK) · work_type CHECK('조업'/'휴무') ·
  기입 주체 각인(updated_by — 서버 STAMP 세션 강제). **행이 없는 날 = '미등록'** —
  가짜 가동일 금지. 정정 = 같은 날 덮어쓰기(마스터 성격 — append-only 계약 아님을 명기).
- 화면: 월 캘린더(요일색·오늘 표식) + 선택일 등록 폼(조업/휴무+메모) + 월 요약 칩
  (조업/휴무/미등록) + **미등록 평일 일괄 조업**(확인창 — 기존 행 불변) + 엑셀.
- **모순 표식**: 휴무로 적힌 날에 실적 기록이 있으면 ⚠기록 배지(숨기지 않는다 — 정정 유도).

## 2. 분모 정밀화 (코워크 조건1의 본체 — 3곳 공유)

| 소비처 | 종전 | 이후 |
| --- | --- | --- |
| 홈 도넛 %(processLive) | 창 내 "어느 공정이든 기록 있던 날" 프록시 | 창에 달력 등록이 있으면 **분모 = 조업일 · 분자 = 조업일 교집합** |
| SQ 심사 뷰 ●◐×(auditMatrix) | 동일 프록시(opDays) | 동일 규칙 — ● 판정 기준이 달력 가동일로 |
| 성과 지표/생산현황 | (신규) | 조업일수 = 달력 등록분만 · 미등록 = null/'미등록' 정직 |

- **미등록 구간은 종전 프록시 유지**(이중 작업 없음) + DTO `denomSource: 'calendar'|'proxy'`
  로 화면 범례에 원천 명기(도넛 헤더·심사 뷰 기간 칩). 휴무일 특근 기록은 %에서 제외되고
  모순 표식이 달력 화면에서 그 날을 짚는다.

## 3. #15 성과 지표 (그림19~25 · 골격 — 공통 프레임 1 + 지표 2종)

- 채널 `semimes:perfIndicators`: **양품률**(양품/(양품+불량)) · **수입검사부적합 PPM —
  건 축**(불합격 건/검사 건, 시료 수량 원천 부재라 수량 축은 만들지 않음 — 화면 명기).
- 계약: 월 12칸 고정 · 실적 없는 달 = **null**(가짜 0/100 금지) · **연간 = 분자·분모
  재합산**(월평균 아님 — E2E 산식 재현 단언) · 표에 분자·분모 동봉(산식 검증 가능).
- 콤보차트 = 바(분모 규모)+라인(값) 자체 SVG. 목표선·부호 착색은 KPI 기준정보(#13,
  배치⑷) 후 연결. OEE 3분해 = 가동시간 원천 확보 후(대조표 명기 — 미구현 정직).

## 4. #12 생산현황 차트 (그림61 · 골격 — 생산수량 축 우선)

- 채널 `semimes:prodChart`: 일별 양품/불량 스택 바(취소 제외 — E2E DB 실측 대조) +
  품번별 상위 30 + 기간 조업일수(달력 등록분만 — 미등록 = '미등록' 표기).
- **UPH/가동률 축 없음** — 표준 UPH·가동시간 원천 부재(만들지 않는다, 화면 명기).

## 5. #16 추적 공정 흐름 밴드 (그림26·27 · 전면)

- 채널 `semimes:traceBand`: 지시번호 → **지시수량 → 라우팅 공정별 양품/불량 열산 밴드**
  + 공정 카드 클릭 = **공정별 LOT 그리드**(실적일·LOT·수량·작업자·취소 흐림).
- 정직 계약: 기록 없는 공정 = "기록 없음"(0 날조 없음) · **라우팅 밖 공정 기록 =
  offRoute 경고 밴드**(공정 미지정 포함 — 숨기지 않음) · 취소 행 = 그리드 표기·합산 제외 ·
  **판매수량 열 = 원천 부재로 미표기**(15번 §2 확장 금지·돈 경계) · 자재 LOT 소급은
  기존 mes-trace 바로가기로 위임(중복 구현 0).
- E2E 자기일관 단언: 밴드 합(라우팅+밖) = 작업지시 진척 합(okSum) 일치.

## 6. 동반 조치 2건

- **P2″ 조기 봉합 — `ymdOk` 실재 일자 검증**(2026-13-45류 통과하던 것): 조업달력이 분모
  원천이라 가짜 월(2026-13) 행 유입을 막아야 해 앞당김. 형식+실재 왕복 검증으로 교체 —
  기존 쓰기 채널 전부 동일 혜택(회귀 pc1·g1 전건 통과로 무영향 실측).
- **공용 `ymdAddKST`** 를 date-kst 로 승격(슬러지 "3중 복제" 해소 경로 — 신규 화면 2곳 사용.
  기존 3곳 치환은 P2″ 일괄 때).

## 7. 배선·검증

- 메뉴: 생산관리(생산현황 차트·추적 공정 흐름·조업달력) + 경영정보(성과 지표). PageId 4종.
- E2E `e2e-d34-b3.mjs` 24/24 ×2(멱등 — 픽스처 초기화 동반: 달력은 마스터 성격이라 복사본
  한정 초기화 무해 명기) + 회귀 pc1 26/26·g1 27/27 + 클린설치 139개·FK 0.
- 라이브 반영: **서버 변경 배치**라 빌드=재기동 세트 이행(:8080 재기동 — 0139 적용·채널 186).
  ⚠세션 초기화 — 재로그인 1회. :8081 검수 서버 = 새 번들·오늘자 복사본·서규하/2222.

## 8. 판정 요청 (사후 확인형)

1. **조업달력 도입 동선**: 매월 초 관리자가 "미등록 평일 일괄 조업" 후 휴무만 손대는 운용을
   기본으로 제안 — 승인 여부.
2. **수입검사부적합 PPM = 건 축** 정직 표기 유지 vs 시료 수량 원천(검사 성적 값 행 수) 근사
   사용 — 봇 권고 = 건 축 유지(근사 분모 금지 원칙).
3. 휴무일 특근 기록의 도넛 % 제외(분모 = 조업일 교집합) — 유지 여부(대안 = 특근일 자동
   편입은 "사람이 적는다" 원칙 위배로 비권고).
