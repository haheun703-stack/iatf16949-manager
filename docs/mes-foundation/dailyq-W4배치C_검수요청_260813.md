# W4 배치C 검수요청 — 설치판 재빌드 + 데스크톱 경로 단언 (2026-08-13)

> **첫 줄 하네스**: 신규 `scripts/e2e-w4c-desktop.mjs` **14/14**(데스크톱 경로 주체방어 —
> 8/6 교훈 상설화) + 설치판 빌드 exit 0 + **산출물 마이그 141파일 = 소스 1:1(말단 0142)**.
> 회귀는 금일 저녁 실측 그대로(w4b 23/23 + 127/127 — 이번 빌드는 코드 무변 패키징).
> **착수 근거**: 사장님 "1번부터 해라 순차적으로"(8/13 저녁 = 배치C 진행 지시).

## 1. 설치판 재빌드 (37호 배치C — 0101 세대 → 0142 세대)

- 산출물 = `dist\IATF16949 품질경영시스템 Setup 1.0.0.exe` (**279.1MB · 8/13 20:43**).
  electron 31.7.7 · better-sqlite3 네이티브 리빌드 · **마이그 0139~0142 동봉**(work_calendar·
  설비/금형 마스터·WO 전이 감사·권한 매트릭스 — 7/27 구판 이후 41개분 전부 포함).
- 신규 설치 PC = 클린 체인 전량 적용 · 기설치 PC = pending 분만 적용(_migrations 스킵 —
  서버와 같은 체인이라 충돌 없음). 코드 서명 없음(현행 관례 유지). **배포는 별도 지시**(37호).

## 2. 데스크톱 경로 E2E 단언 (8/6 교훈 "웹 경로만으로는 C-3류 못 잡음"의 상설화)

- `e2e-w4c-desktop.mjs` = bridge(전 핸들러) **직접 호출 = 서버 STAMP 미주입 경로**(데스크톱
  IPC 등가)에서: 기록·정비 쓰기 12채널 + inspConfirm + WO 상태 전이(0141 축) —
  **주체 빈 값 전부 거부** + 거부 전량 = **DB 쓰기 0 검산**(prod·wo·lot 행 수 불변).
- 대상 = 복사본 한정(IATF_DATA_DIR 필수 게이트 — 라이브 보호). M-1 수동 확인의 기계 짝.

## 3. M-1 — 사장님 30초 수동 확인 1회 (W4 종결의 남은 관문)

- 재빌드된 Setup 실행·설치 → 앱 실행 → **사용자 미선택 상태**로 생산실적(또는 작업지시)
  저장 시도 → **"사용자를 선택하세요 / 작성자가 없습니다" 거부가 뜨면 통과**.
  (§2가 같은 방어선의 기계 단언 — M-1은 실물 앱 육안 1회. 세부 절차는 코워크 안내 예고분.)

## 4. 정정 1건 (자기 신고)

- 금일 아침 배치B 검수요청의 "클린설치 **142개**" = 오기. 정확치 = **마이그 파일 141개**
  (체인 결번 0054 실측 · 말단 0142) = 복사본 적용 141 = 산출물 동봉 141 — 3축 일치.
  FK 0·전 마이그 적용 사실은 무변.

## 5. 검수 동선 · 판정

1. 산출물 확인: `dist\win-unpacked\resources\migrations` 말단 4개 = 0139·0140·0141·0142.
2. `e2e-w4c-desktop.mjs` 재현(복사본): 14/14 · 거부 문구가 화면 안내와 동일 어휘인지.
3. **W4 종결 = M-1 확인 1회(사장님) + 본 검수** — 이후 남는 큰 산 = PC-2(현장 스캔 UX).
4. 설치판 각 PC 배포 시점·대상 = 별도 지시 대기(37호 그대로).

---

## 추기 (8/13 밤) — M-1 통과 · W4 게이트 종결 (후속 1건 등재)

- **M-1 통과(사장님 실행 + 봇 프로브, 21:4x)**: 재빌드 실물(win-unpacked — Setup 동봉
  바이너리와 동일)을 **사장님이 직접 기동**(`m1-launch.bat`) → 봇이 진단 포트 접속 전용
  프로브(`m1-attach-check.mjs`)로 실측 — **5/5**: 사용자 미선택 작업지시 저장 = "작성자가
  없습니다 — **사용자를 선택하세요**" · 생산실적 저장 = "작업자 이름이 없습니다 — 기록주체
  없는 실적은 저장할 수 없습니다" 거부 + 실행 증거 `captures/M1_desktop_reject_260813.png`.
  전부 거부 경로 = 라이브 DB 쓰기 0. 데스크톱 상설 하네스 14건 + 실물 확인으로 이중 봉인.
- **⚠발견 1건(후속 등재) — Setup(NSIS) 래퍼 크래시**: 사장님 설치 시도에서 옵션 화면
  "다음" 직후 조용히 종료 — 이벤트 로그 **APPCRASH 3건 실측**(NSIS System.dll ·
  21:22~21:24 · 설치 폴더 빈 상태). 앱 본체(payload)는 위 M-1 로 무결 실증 — 결함은
  **NSIS 래퍼 한정**. 처분 = **배포 전 필수 해결 과제**(재빌드·타 PC 재현·백신 간섭 확인
  — 배포는 어차피 37호 별도 지시라 W4 기능 축 종결에는 무영향).
- **M-1 이행 방식 기록(절차 정직)**: 원안 = Setup 설치 후 확인이었으나 래퍼 크래시로
  win-unpacked 동일 바이너리 실행으로 대체(코워크 회신 ①의 "실물 검증 = M-1 겸함" 취지
  충족 — 설치 과정 검증만 위 후속 과제로 이월). 앱 실행 = 사장님 손, 봇은 접속 프로브만.
- **∴ W4 게이트 종결** — 보안(A)·권한(B)·데스크톱 방어선(C) 전 축 라이브/실물 실증 완료.
  잔여 = NSIS 래퍼 후속 1건(배포 전) · 다음 트랙 = **PC-2**.
