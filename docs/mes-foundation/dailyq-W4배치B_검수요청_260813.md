# W4 배치B 검수요청 — #19 화면별 권한 매트릭스 + 편승 2건 (2026-08-13)

> **첫 줄 하네스**: 신규 `scripts/e2e-w4b.mjs` **22/22 ×2(멱등)** + 회귀 **127/127**(w4a 9·
> pc1 27·g1 27·b3 24·b4 28·d35 12 — 전부 E2E봇 로그인) + typecheck node+web + build +
> build:server + 클린설치 **142개·FK 0**.
> 대상 = :8081 **새 복사본**(qms-e2e-260813c · 배치B 서버 코드로 기동 — 검수 중 재기동 없음).
> 로그인 = 서규하/2222. **37호 도장(8/13)**: ②권한 축 = 팀+개인 ③단가 = 전원 잠금 시드.

## 1. 마이그 0142 — screen_permission (그림33 골격)

- **주체 축** = 권한그룹(부서 = `app_users.team_dept`) + 개인(`app_users.id` — 그 화면의
  팀 규칙을 **통째로 대체**) × **화면**(uiStore PageId) × **7종 비트**(읽기·쓰기·수정·삭제·
  엑셀·프린트·단가).
- **단가 = 스키마 CHECK 하드락**(`can_price = 0` — 37호 ③ "자리만 만들고 켜지 않음"의
  코드 실장). 핸들러 친절 거부 + 스키마 거부 이중 방어 — 개방은 후속 마이그 1건(도장 관문).
- **IATF 라이선스 플래그** = app_config `license.iatf_addon` = 'off' 1행(32호 상품 구조 —
  자리만, 뷰 언락 배선은 후속).
- **시드 0행** — 규칙 부재 = 현행 허용(적용 직후 동작 변화 0). 매트릭스는 관리자 화면에서
  채울 때만 효력. executive = 그림33 [그룹] 최종관리자 — **매트릭스 무시 전권**.

## 2. 서버 디스패처 강제 (서버가 정본)

- `SCREEN_GUARD`(index.cjs): 채널→(화면, 축) 매핑 — **쓰기 14·수정 3·삭제 1(recordCancel
  kind 분기)·엑셀 3 = 21채널**. 규칙 있으면 403(개인>팀·executive 전권), 없으면 현행.
  PROTECTED role 가드는 그대로 **바닥**(두 관문 모두 통과해야 실행).
- **매핑 원칙 = REQUIRED_FIELDS 와 동일**: 화면 귀속이 명백한 채널만(추측 금지·오차단 방지).
  **읽기·프린트 축은 렌더러 보조(메뉴 숨김)만** — 조회 채널은 여러 화면이 공유(합성 조회)라
  단사상이 아니어서 서버 차단 시 무관 화면이 깨진다. 양식/케이스 등 잔여 채널 확장은
  채널→화면 대조표 작업으로 후속(§7-②).
- perm 채널 3종: `perm:list`(로그인) · `perm:save`(**manager+** · STAMP updatedBy ·
  단가/모순 규칙 저장 거부) · `perm:effective`(세션 사용자 자신 — STAMP userName).

## 3. 관리자 화면 — '화면별 권한관리' (기준정보 메뉴 · manager+ 표출)

- 그림33 재현 범위: 주체 탭([권한그룹(부서)]/[개인]) + 메뉴 트리(모듈>화면 체크 선택) +
  7종 체크 패널(단가 = 잠금 표시·비활성) + **"선택메뉴 권한설정 저장"** + 주체별 규칙 표
  (규칙 해제 = 현행 복귀). 규칙 있는 화면은 트리에 배지 표식.
- 화면 숨김(보조): 로그인 사용자의 병합 규칙으로 메가바·2차 탭에서 읽기 0 화면 숨김.
  숨겨도 서버 정본이 최종 방어선(딥링크 등) — 보조막임을 화면·코드 주석에 명기.

## 4. 편승 ⑴ — E2E 지시 발번 `E2E-WO-` 접두 (8/13 질의 후속)

- 발번 주체(세션)가 E2E봇이면 `E2E-WO-YYMMDD-nn` — 실지시 번호대(WO-)와 **충돌 자체가
  없음**. 카운트도 접두별 분리(LIKE 접두 앵커 — 상호 불침범). 실측: E2E 2발 발번 전후
  실번호대 카운트 불변(w4b 5단), STAMP = E2E봇.

## 5. 편승 ⑵ — 'E2E'/'E2E봇' 표기 통일

- **기록 주체 표기 = 'E2E봇' 단일**(계정명과 동일): d35 직접 시드의 created_by/worker 'E2E'
  전량 정리 → 복사본 주체 'E2E' 잔재 **0** 실측(w4b 6단).
- 구픽스처 **비고 칸 'E2E' 마커는 유지** — 값 칸의 데이터 표지(주체 아님)라 혼동 축이
  다르고, 배치 1~8 expect 단언과 얽혀 있어 정리 이득 0. 규약: 주체 = 'E2E봇' · 표지 = 'E2E'.

## 6. 검수 동선 (코워크 — 36호 채널)

1. :8081 로그인(서규하/2222) → 기준정보 > **화면별 권한관리** 진입(그림33 골격 육안).
2. 권한그룹 [관리팀] · '조업달력' 선택 · 쓰기 해제 → 저장 → 서규하로 조업달력 저장 시도
   = **403 안내문**("권한 매트릭스: …") 재현 → 규칙 해제 → 저장 복귀.
3. 개인 [김초연] 읽기 해제 저장 → 김초연/qms1234 로그인 = 메뉴에서 해당 화면 숨김(보조) 확인.
4. `perm:save` 를 member 로 직접 호출 = 403 · 단가 켜기 = 거부 안내문(원하면 curl).
5. 끝나면 만든 규칙 전부 해제(규칙 0행 = 현행 복귀 — E2E 는 자체 정리 완료 상태로 인계).

## 7. 판정 요청 (사후 확인형)

- **① perm:save 권한 축**: 현행 = manager+(사용자 관리 appUser 계열과 동일 축 — 일관성).
  권한을 나눠주는 채널이라 **executive 전용**으로 좁히는 게 맞다고 보면 회신에 명기(1줄 수정).
- **② SCREEN_GUARD 확장 범위**: 명백 귀속 21채널로 개시(오차단 방지). 양식 제출/케이스/
  FMEA 행 등 잔여 쓰기 채널의 채널→화면 대조표 확장을 후속 배치로 둘지, 배치C 전에 당길지.
- **③ 라이브 재기동**: 배치A 회신 동의대로 **A+B 묶음 1회**(restart-qms-server.bat —
  재로그인 1회). 합격 회신 시 사장님 "재기동 해라" 한마디로 이행. ⚠현재 라이브 = 신 렌더러
  (정적 즉시 반영)+구 서버 → 권한 화면 진입 시 '조회 실패' 정직 표기(효력 0·무해),
  재기동으로 완성.

---

## 추기 (8/13 저녁) — 합격 접수 · 소견 조치 · 판정 확정 · 라이브 반영 완료

- **합격 회신 접수** = `dailyq-W4배치B_검수회신_코워크_260813.md`(강제 전 주기 실물 실증 ·
  소견 1건 = 재기동 조건 · 판정 권고 3건).
- **소견 조치(403 "통신 오류" 오인 표기)** = 코워크 권고대로 **비2xx 공용 분기**:
  폴리필(web-api)이 비2xx 에 이름표(403 = PermissionError · 401 = AuthError) →
  신규 `lib/errText.invokeErrText` 가 서버 안내문("권한 매트릭스: … 쓰기 권한이 없습니다 —
  관리자 문의"·"로그인이 필요합니다")을 그대로 표기, **진짜 통신 실패만 폴백 문구**.
  쓰기 catch **12화면 22지점** 교체(조업달력·KPI기준·PPM·SPEC·품목·거래처·설비·금형·
  KPI그리드+엑셀·작업지시·생산실적 발번/저장/취소·검사 저장/확인/취소·홈 완료/KPI·권한 화면).
  스토어 경유 엑셀(FMEA 등) 잔여는 SCREEN_GUARD 확장(§7-②)과 같은 후속 축.
- **판정 ① 확정(사장님 8/13)** = `perm:save` **executive 전용**(서버 PROTECTED 1줄 +
  메뉴 execOnly + 화면 안내문). manager 확대는 안정 후 재론. 판정 ② = 21채널 유지(동의) ·
  판정 ③ = 아래 이행.
- **재검증** = e2e-w4b **23/23**(판정 ① manager 403 체크 추가) + 회귀 **127/127** +
  typecheck·build·build:server. :8081 재기동(검수 종결 후 — 규약 준수) 동일 코드.
- **라이브 재기동 실측(사장님 "재기동 해라")** = restart-qms-server.bat: 구판 PID 20924
  종료 → 신판 기동 — **0142 적용 로그·채널 197·health 무인증 슬림 {ok}** 실측.
  라이브 DB 읽기 전용 대조: screen_permission **0행**(규칙 전무 = 현행 그대로) ·
  `license.iatf_addon` = off · E2E- 지시 0건. ⚠직원 재로그인 1회 발생(고지 문구 그대로).
- 검수 잔여물(조업달력 18일 1건)·w4b 검증분(14·15일)은 **복사본 한정** — 라이브 무관.
