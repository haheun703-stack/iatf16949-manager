# dailyq — 2차분 배치⑵ 검수 회신 (2026-08-11, 코워크)

- **대상**: 배치⑵ 기준정보 5화면 — #1 품목코드관리 · #2 거래처코드관리 · #3 BOM조회(정/역전개) · #4 코드관리 · #5 BOM관리 배선 (커밋 e2d9d20)
- **검수 방식**: :8081 직접 조작(서규하) — 골격 대조(그림6·9·14·3·12) + **거래처 유형 정비 파급 실물 재현** + 값 소거 계약 재현. 캡처 6장.

## 총평: **합격** — G1 검수 소견 1호(세일에스앤티 오분류)가 이번 배치로 화면에서 닫힘

## 1. 골격·명세 대조

| 화면 | 정본 | 판정 | 확인 |
| --- | --- | --- | --- |
| #1 품목코드관리 | 그림6~8 | ✅ | 300/2,308건 페이징 · **단가 열 없음(돈 경계) 헤더 명문** · 검사구분 3종 = 표시 전용("의미 확정 전 편집 금지") · 유형 콤보(반제품/완제품/원자재) · 인라인 수정 · 정직 '—' |
| #2 거래처코드관리 | 그림9·10 | ✅ | 유형 5종 콤보 · 헤더에 "유형이 수집함 입고 구분 초안의 원천 — 오분류 정비 동선(G1 소견 1)" 명문 · 비활성 포함 토글 · 인라인 수정 |
| #3 BOM조회 정/역전개 | 그림14 | ✅ | 정전개 L1~L9 트리(모품번/자품번/품명/유형/소요량) · **역전개 시 열 의미 전환(사용처(상위)/기준 품번)** · 헤더 정직("활성 간선만 · 깊이 10 · 조회 전용 — 정정은 갱신 파이프라인이 정본") |
| #4 코드관리 | 그림3~5 | ✅ 골격만 | [골격만] 배지 + "2단 코드 스키마 미보유 — 조회 표출·편집은 스키마 확보 후" 정직 명문 · 좌 4그룹(불량유형Q 250·공정D 66·품목유형P·거래처유형M — 파생 그룹은 편집처 안내) · 편집 버튼 흐림 |
| #5 BOM관리 배선 | 그림12·13 | ✅ | 기존 품번 트리(반-MES) 재사용(32호 §5 무변경 방침 그대로) · 2차 탭 줄 편입 · 검색 하이라이트·3색 범례·상단 통계 6종·임포트 스탬프 정상 |

## 2. 핵심 시나리오 — 거래처 유형 정비 파급 (G1 소견 1호 해소 실증)

1. **정비**: 거래처코드관리에서 1301 세일에스앤티 [수정] → 유형 고객→**외주처** → 저장 → 배지 [고객](분홍)→[외주처](파랑) 즉시 반영.
2. **파급**: 수집함 → 입고 전표 태깅 폼에서 거래처에 **"1301" 입력 → 입고 구분 초안이 "외주재입고"로 자동 제안** — "거래처 유형으로 초안" 라벨 그대로 작동. 코드 입력만으로 인식(코드·이름 모두 인식 — G1 소견 D 해소분도 재확인).
3. 오분류였던 '고객' 상태였다면 초안이 구매입고 계열로 잘못 잡혔을 것 — **마스터 정비 → 현장 기록 초안까지 한 줄로 이어지는 동선**이 이 화면의 편입 근거(34호 #2)였고, 그대로 실증됨.
4. 태깅은 **저장하지 않고 닫음**(파급 확인만) — 수불 기록 미생성. 유형 정비는 올바른 분류로의 정정이므로 복사본에 유지. **라이브 반영 시 봇이 동일 정비 1건 수행 필요.**

## 3. 값 소거 계약 재현 (봇 프로브 COALESCE 수정 확인)

- 품목 1140306101 품명 "BOLT" → **소거 후 저장 → 목록에 '—'로 저장됨**(구값 되살아남 없음) → 재수정 "BOLT" 복원 저장 정상. 소거→복원 양방향 확인.

## 4. 소견 (경미 — 차기 검토)

1. **유형 변경 확인창 부재**: 거래처 유형은 수집함 초안·입고 통계에 즉시 파급되는 값인데 인라인 저장이 확인창 없이 완료됨. 품목·거래처 마스터 중 **파급 큰 필드(유형)만 ConfirmDialog 1단** 검토 제안(전 필드는 과잉).
2. 검수 중 수집함에 테스트 사진 잔여(미분류 2건 — 코워크 주입 1건 포함, 서규하 각인, 복사본 한정) — 정리 봇 재량.

## 5. 다음

- 봇: 회신 접수 → **"배치⑶ 가자"(지표·달력·추적 — #15 성과지표 · #18 조업달력[승격분] · #16 추적 흐름 밴드 · #12 생산현황 차트)**. 배치⑶은 조업달력 승격 조건(34호 조건 1 — 분모의 원천)이 처음 실물화되는 배치이므로, 도넛 %·심사 뷰 분모가 조업달력 기준으로 바뀌는지가 검수 핵심이 될 것.
- 봇: 라이브 반영 목록에 **세일에스앤티 유형 정비 1건 추가**(§2-4).
- 사장님: 퇴근 시간대 라이브(:8080) 재기동 창 — confirm/접근성 묶음 + 배치⑴⑵ 일괄.
