# dailyq-PB2 — MES 스타일 홈 재편 견적 (2026-08-04)

> **근거**: 29번 §11 — "겉이 MES, 속이 SQ" 사장님 확정(8/4 "이 방향 맞다") + 테마 지시
> ("밝은 테마 + 파스텔, 시연성"). 설계 정본 = 루트 **30번 목업 v2(파스텔)**.
> 배지 3색(SQ #2467b3 · IATF #a03d7c · 갭 #9a6200)은 CVD 검증기 통과, 상태는 색+기호(●◐×)+라벨 3중.

## 범위 (ⓐ~ⓓ)

| # | 내용 | 재료(재사용) | 규모 |
| --- | --- | --- | --- |
| ⓐ | **상단 MES 모듈 메뉴바** — 기준정보·자재·생산·품질·설비·금형·경영정보 드롭다운(문법노트 3차 §1 트리) + 하위 항목 SQ·IATF 배지 + 갭 ＋행 | 기존 PageId 라우팅 재매핑(uiStore), pack_forms.sq_item_code | 1일 |
| ⓑ | **홈 = 공정 실황 도넛 카드** — 커버리지·LOT연결·최근시각. 기존 관제탑 요소는 하단·탭 재배치(**정보 손실 0 확인표** 동봉) | `mesRecords:processLive` 채널 그대로, ProcessLiveMatrix 데이터 로직 | 1~1.5일 |
| ⓒ | **SQ 심사 뷰 신설** — SQ 항목(팩 45종 축) × 공정 7, 셀 = ●◐× + 최근일. 신규 채널 `sq:auditMatrix` 1개 | mes_records 커버리지 + mes_trace LOT 연결 + form_submissions 작성 기록 + pack_forms | 1.5~2일 |
| ⓓ | **파스텔 라이트 테마 토큰** — v2 목업의 검증된 잉크·필을 토큰화 | 기존 토큰 체계(17번 리프레시 선례 — 표피만) | 0.5일 |

**합계 4~5일 · 신규 채널 1 · 마이그 2**(0134 pack_forms에 iatf_clause 컬럼[스키마] ·
0135 IATF 조항 매핑+갭 행 시드[데이터] — 시드분리 규칙 준수).

## 원칙

- 기존 규율 전량 승계: 정보 손실 0 · 라이브 무손상 검증 · 첫 줄 하네스 · 가짜 숫자 금지
  (LOT 연결·커버리지 전부 실측만, 데모값 금지) · 별도 엔진 금지(psi 채널 재사용 우선).
- 사이드바는 유지하되 반-MES 그룹이 상단 메뉴로 승격되는 만큼 축소 재편(확인표에 명시).
- ⓒ의 "IATF 추가 요구 ＋행"은 v1에서 예방보전(8.5.1.5)·작업준비 검증(8.5.1.3) 2건으로 시작 —
  조항 확장은 데이터(0135) 행 추가로만.

## 검수 게이트

28+화면 회귀 캡처 · 도넛/심사 뷰 E2E(기록 주입 → 셀 반영) · audit 무손상 · 코워크 재현 검증.
⚠️W4 설치판 재빌드 범위 0134+.

## 추기 (8/4 저녁) — ⓔ 증권 HTS 문법 반영 (문법노트 4차, 사장님 지목)

| # | 내용 | 규모 |
| --- | --- | --- |
| ⓔ | **부호 착색 2단**(빨강=+초과·파랑=−부족, 숫자+파스텔 히트맵 — ⚠MES 구화면과 반대라 축 분리 확정: 부호=빨강/파랑, 판정=신호등, 4차 노트 §1) + **색상 ON/OFF 토글** + **기간 칩** + **★중점관리 별** + **하단 엑셀형 그리드**(병합 헤더·인그리드 편집·저장 + 엑셀 다운로드 — "엑셀처럼 적고 저장" 요구) + 프리셋 버튼 줄 | +1~1.5일 |

**합계 갱신: 5~6.5일.** 탭 세트 저장·크로스헤어 툴팁은 2차분(후속).

## 착수

방향·테마는 기도장(29번 §8·§11). **본 견적 수치(5~6.5일) 확인만 받고 즉시 착수** — 이의 시 범위 조정.
G1 수집함(2.5일)과 병행 가능(파일 겹침 없음 — G1은 ingest/사진, PB2는 홈/메뉴).
