# W4 배치A 질의 소명 — WO-260813-01 (2026-08-13, 봇)

> 판정: **ⓐ 확정 (무해)** — 정확히는 ⓐ보다 깨끗함. 라이브 작업지시가 **0건**이라
> "우연히 실지시와 같은 번호"조차 아님(충돌 대상 부재). ⓑ 역방향 오염 배제.

## 실측 근거 (양쪽 DB 읽기 전용 조회 · 무변경)

- **복사본 시각열**: qms-e2e-260813b DB 생성 **16:33:36** → :8081 기동 **16:33:50** →
  문제의 지시 생성 **16:34:25** (회귀 실행 중). 즉 복사본 구성 "후" 내부에서 생성된 행.
- **지시 실체**: work_order id 6 = WO-260813-01 · 28236-2MAA0 · 500 · 진행 ·
  created_by=**E2E봇** = 회귀 **pc1 8단**(`workOrderUpsert` 500 → 진행 → 실적 50)이
  **API 경유 발번**한 픽스처. 관측된 "진행 50/500"과 완전 일치.
- **STAMP 방증**: pc1 은 createdBy:'위조' 를 클라이언트에서 보내지만 기록은 E2E봇 —
  서버 세션 STAMP 가 클라이언트 값을 무시(배치A ③ 재실증).
- **라이브 대조**: 라이브 work_order **총 0건** — WO-260813-01 없음 · 오늘자 `WO-260813-%`
  0건 · E2E/E2E봇 유래 0건. 작업지시 발번은 라이브에서 아직 미사용 기능.
- 복사본 나머지 5건 = 전부 `E2E-TV-*`(d35 직접 시드) — 라이브 유래 행 0.

## 권고 2건 — 수용, 배치B 편승 제안 (착수 도장 대기)

- **번호 공간 분리**: pc1 8단은 서버 발번 규칙(WO-YYMMDD-nn) 자체를 검증하는 단계라
  픽스처 측 접두 강제 시 발번 경로 검증이 소실 → **서버 측: 세션 사용자가 E2E봇이면
  발번 접두 `E2E-WO-`**(발번 로직 검증 유지 + 실번호 충돌 원천 차단)로 반영 제안.
  pc1 체크 정규식 동기 수정.
- **표기 통일**: 구픽스처 created_by 'E2E'(d35-tv 직접 시드 5건) → 'E2E봇' 일괄 정리.

소명은 본 문서로 종결. 배치B(#19 권한 매트릭스 + 위 편승 2건)는 사장님 착수 지시 대기.
