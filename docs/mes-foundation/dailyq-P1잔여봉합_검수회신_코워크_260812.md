# dailyq — P1″ 잔여 봉합 검수 회신 (2026-08-12, 코워크)

- **대상**: P1″ 잔여 21건 봉합(커밋 6b1a160) 중 실물 재현 가능 4건 — Major ②③ + 주목 봉합 2건
- **검수 방식**: :8081 직접 조작(서규하) — 사장님 실관람 하에 진행. 캡처 3장 + 네트워크 실측.

## 총평: **조건부 — Major ③(LOT 이중 발번) 재봉합 필요.** 나머지 3건은 합격

| # | 봉합 항목 | 판정 | 실증 |
| --- | --- | --- | --- |
| 1 | Major ② 작업지시 '취소' 확인창 | ✅ | WO-260812-01 [취소] 클릭 → 커스텀 확인창 "취소된 지시는 진척 집계에서 제외됩니다" + 지시번호 명시 + [그만두기]/[취소 처리] 무중의 버튼. [그만두기]로 원복 |
| 2 | **Major ③ LOT 발번 더블클릭** | ❌ **재봉합** | 연속 2클릭 → `semimes:lotIssue` POST **2발 모두 200**, 응답 본문 **`-13`·`-14` 서로 다른 차수** (fetch 계측 실측). 연타 가드·서버 멱등 둘 다 미작동 — asyncGuard가 이 버튼 미적용으로 추정. E2E 63/63 통과였으므로 프로브가 연타 경로를 실제로 안 때리는 것 |
| 3 | 수집함 품번 없는 행 정지 | ✅ | 2행에 수량 5·업체LOT만 기입 후 저장 → **"2행 품번이 비었습니다 — 품번 없이는 저장되지 않습니다(수량·업체LOT만으로는 수불이 성립하지 않음)"** 행 번호 짚는 정지 배너 · 행 보존 · 저장 차단(무기록) |
| 4 | SPEC 개정 확인창 서버 재조회 | ✅ | '외경' REV 1 등록 후 기준치 12.6으로 개정 저장 클릭 → **확인창 표출 전 `semimes:specList` 재조회 발사 실측** → 확인창 "REVISION 2 신규 행 생성 · 구판(REV 1) 불변" 예고가 서버 기준. 취소로 미저장(기전 확인 목적 달성) |

## 재봉합 지시 (Major ③)

- 클라: 발번 진행 중 버튼 disabled(공용 asyncGuard 적용) — 이 화면 누락분.
- 서버: 미사용(실적 미연결) 발번 LOT 존재 시 신규 발번 대신 기존 번호 반환(멱등).
- E2E: 연타 프로브를 실제 연속 2클릭(요청 2발) 경로로 수정 — 이번 오탐의 재발 방지.

## 검수 중 사고 1건 (규약 소견)

- 검수 도중(13:2x) **:8081이 예고 없이 재기동·복사본 재생성** → 코워크 세션 사망·재기동 전 검수 증거(발번 -8) 소실·사장님 재로그인 3회째.
- 규약 요구: ①코워크 검수 중 :8081 재기동 금지(불가피 시 사전 고지) ②복사본 재생성 시 서규하 비번 **2222 유지**(e2e-g1 초기화 재발 — 오늘 아침 사장님 직접 재설정으로 해소).

## 부수 관측

- **실사용 개시 확인**: 오늘 라이브 복사본에 직원 실기록 — 생산 2건(양품 100)·검사 2건·2단 확인 대기 2건·수불 4건·작업지시 WO-260812-01(진척 50/500). 좋은 신호.
- 발번 503 당시 오류 문구 "발번 실패 — 통신 오류"는 정직 표기로 적절(서버 다운 순간이었음 — 결함 아님).
- 검수 잔여물(복사본 한정, 정리 재량): 미사용 LOT 4건(-11~-14 추정), SPEC 외경 REV 1(28236-2MAA0·자주), 수집함 미분류 테스트 사진 1건. 봇 E2E 잔여물(P1더블프로브 REV 4)도 그리드에 병존 관측.

## 다음

- 봇: **Major ③ 재봉합 → 코워크 재검(발번 더블클릭 1회 재현으로 종결)** → 배치⑶ 착수는 사장님 신호대로.
- 사장님 대기: 라이브 자동 시작 등록 가/부(코워크 의견: **가**).
