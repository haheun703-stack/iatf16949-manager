# M2 설치 첫날 경험 — 1차: 설치 마법사 + 복사본 단일 스위치 — 검수요청 (2026-08-23 일, 봇 · 41호 M2 착수 도장 "M2 가라")

- 근거: 41호 M2("설치 마법사(회사 프로파일·관리자 1계정·팩 선택) + 설치판(NSIS) + 라이선스 플래그 언락 UI", 완료 판정 = **빈 PC 설치 → "우리 회사"로 로그인 E2E 1종**) · 42호 D(`IATF_REVIEW_COPY` 단일 스위치 — 코워크 소견 채택).
- ⚠ 라이브 무접촉(:8080 pid 9776 구 코드 그대로 — 서버 코드 변경분 반영은 도장 창). :8083 = 새 코드 + 플래그로 재기동 **pid 6636**(copy=true·E2E봇 OK·forms 294). :8081(pid 27496)은 구 코드 유지(코워크 S3-2 검수 중 — 36호 4항).

## 1. 산출물

| 파일 | 내용 |
|---|---|
| **`server/index.cjs`** | ① **`IATF_REVIEW_COPY=1` 단일 스위치** — `IS_COPY_SERVER = 플래그`. "IATF_DATA_DIR 지정 = 복사본" 추론 제거(고객사가 경로만 바꿔도 표지·E2E봇이 열리던 구멍 봉합). 플래그 ON + 라이브 DB = **기동 거부**(fail-closed). ② **E2E봇 서버측 관문** — `auth:login` 에서 `E2E봇` 은 복사본에서만(고객사·라이브 = 401 "검수 복사본 전용 계정") — 종전엔 하네스(lib/e2e.mjs) 클라이언트 가드뿐이었음. ③ **설치 마법사** — `setupNeeded()`(활성 사용자 0명) 일 때만 `/setup` 개방·`/login`→`/setup` 유도. `POST /api/setup:complete` = 트랜잭션 1개(company_profile 5키 upsert · app_users 관리자 1명 executive·must_change_pw 0 · `license.iatf_addon` on/off · `install.setup_done` 시각) → 세션 발급(자동 로그인). 입력 검증(회사명 2자↑·관리자명 2자↑·`E2E봇` 금지·비번 4자↑). 사용자 1명 생기면 **409/redirect 로 영구 폐쇄**(운영 중 재실행 불가). 쓰기는 `authDb`(쓰기 핸들) |
| **`server/setup.html`** | 마법사 1페이지(로그인과 같은 결·유리 카드): ① 회사(회사명*·약칭·대표·주소·전화) ② 관리자(이름*·부서·비번*·확인) ③ 사용 범위(기본 SQ 고정 체크 + **IATF 16949 애드온 켜기** 토글). 완료 → `/`. 이미 설치된 시스템에서 열면 `/login` 으로 |
| `scripts/start-standard-review.bat` · `e2e-console-kill` · `e2e-cprime-gates` | `IATF_REVIEW_COPY=1` 반영 |
| **`scripts/e2e-setup-wizard.mjs`** | 완료 판정 E2E(§2) — 서버를 직접 띄우고(:8097·mkdtemp) 내림, 라이브 무접촉 |
| `scripts/README-e2e.md` §5-1 | 복사본 규약 변경 등재(:8081 수동 기동 시 플래그 필수) |

## 2. 실측 — `e2e-setup-wizard.mjs` **22/22**

빈 폴더 클린 설치 → `/login`→`/setup` 302 → 마법사 HTML → 검증 3건 400 → 완료 200(role executive·쿠키) → health copy=false·**forms 294**·users 1 → `/api/brand` = 입력 회사명 → 프로파일(대표·약칭) 저장 → 재실행 **409** · `/setup`→`/login` → 관리자 재로그인(mustChangePw false) → **플래그 없음: E2E봇 401 "검수 복사본 전용"** → DB `license.iatf_addon=on`·`setup_done` → 같은 폴더 `IATF_REVIEW_COPY=1` 재기동: **copy=true** · E2E봇 관문 통과(계정 부재 사유로만 실패) · `/setup` 닫힘.

기존 게이트 무영향: `e2e-clean-install` 13/13 유지(데이터층 변경 없음 — 서버 코드만).

## 3. 코워크 확인 요청

1. **마법사 화면 육안** — 빈 폴더로 직접 띄우기: `set PORT=8097 & set IATF_DATA_DIR=%TEMP%\\wiz & set IATF_INIT_DB=1 & set IATF_INSTALL_PACKS=standard & set ELECTRON_RUN_AS_NODE=1 & node_modules\electron\dist\electron.exe server\index.cjs` → 브라우저 `http://127.0.0.1:8097/` → 자동으로 `/setup`. 문구·순서·필수 표시.
2. **42호 D 조건 충족 판정** — "플래그 하나가 ①표지 ②E2E봇을 여는 유일 스위치 + 고객사 설치 = 표지 없음·E2E봇 불가" (§2 ⑰·⑳·㉑). 라이브 DB + 플래그 = 기동 거부는 코드 판독으로(실행은 라이브 접촉이라 안 함).
3. **판단 요청 ①** 관리자 계정 비번 `must_change_pw=0`(마법사에서 본인이 정한 비번이라 강제 변경 생략) — 이견 시 1 로.
4. **판단 요청 ②** 마법사 부서 기본값 '경영' · role executive 고정 — 화면별 권한 매트릭스(0142)와 맞는지.

## 4. M2 잔여(다음 슬라이스) — 9/2 목표

| # | 항목 | 상태 |
|---|---|---|
| a | **라이선스 플래그 언락 UI** — `license.iatf_addon` 은 0142 부터 존재하나 **읽는 코드 0**(실측). 마법사가 값을 쓰므로 다음은 ①렌더러가 플래그를 읽어 IATF 화면(조항·문서BOM·내부심사·규정) 표시/잠금 ②설정 화면에서 키 입력·언락 | 착수 전 — **IATF 화면 목록 확정 필요**(33호 상품 구조 기준 봇이 초안 상신) |
| b | **로고** — 마법사에 로고 파일 업로드 + 출력 엔진 `{{logo}}` 자리 삽입 | S3-2 후반 판단 ⓑ 대기 |
| c | 데스크톱(Electron) 모드 마법사 | 웹 서버 모드 우선(사내 웹 운용 확정 38호 ④). 데스크톱은 기존 로그인 유지 — 필요 시 후속 |
| d | **NSIS 설치판 + 웹서버 모드 동봉** | 감축 1순위(41호 부속) — M3 뒤 여유 시 |
| e | 라이브 반영(서버 코드 = 재기동 세트) | 도장 대기(라이브엔 무영향 변경 — users>0·E2E봇 없음·플래그 없음) |
