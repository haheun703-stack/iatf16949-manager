# P1 ⓐ 데이터 파이프라인 밴드 — 마일스톤 검수요청 (2026-07-31, 사무실 봇 → 코워크)

P1 발효(track7_b2_final §3) 당일 착수 — **ⓐ 완료**. 견적 그대로 **신규 채널 0·신규 테이블 0·
마이그 0**(렌더러 조합만). 밝은 테마 유지·달성률 미표시(커버리지 원칙).

## §1 구현

- **신규 컴포넌트** `src/renderer/.../home/PipelineBand.tsx` — 클러스터 5:
  ①[MES 수신] 신선도 신호등(회색 미가용 / 초록 ≤1일 / 앰버 2~6일 / 빨강 7일+ "다운로드
  필요") — `mesRecords:status` 재사용 ②[기준정보] 품목·간선·라우팅·공정 4종 —
  `semimes:summary` 재사용(신규 로드 1) ③[오늘 수신] 검사계열별 마지막 수신일(자주/수입/
  패트롤/설비 — 덤프 끝 기준 정직 표기) ④[데이터 할 일] 보드 파생 dataCount(미완 트리거
  이슈+장기 공백) — 클릭 = 미이행 필터 on + 보드 스크롤 ⑤[정합 신호] 미래 일자 오입력 행
  제외 고지(futureRows>0일 때만).
- **PortalHome 통합**: 헤더 밴드 바로 아래 삽입. (구)헤더 MES 칩 2종은 밴드 ①로 이동 —
  **정보 손실 0**(동일 정보 + 신호등·계열별 수신일 보강). 죽은 헬퍼 2(daysBetweenYmd·
  shortYmd)·미사용 아이콘 2 제거.

## §2 실증 (사무실)

- **typecheck 전판 통과**(node+web).
- **실행 캡처 = `captures/cap_p1a_pipeline_band.png`**(CDP 파이프라인, dev 실기동):
  라이브 데이터 표출 — MES 수신 🔴15일 지남·기준정보 품목 2,308/간선 2,759/라우팅 1,042/
  공정 66·오늘 수신 4계열 7/16·데이터 할 일 38건·정합 신호 미래 일자 6행 제외.
- **부수(정직 고지)**: dev 기동으로 라이브 DB에 0115~0124 자동 적용됨(검증 완료 체인 —
  사전 스냅샷 백업 `backups/pre-0115_*` 자동 생성 확인). 다음 사장님 실행 시 이미 최신.
- 28화면 회귀·grid 8종·커버리지 3단 E2E는 **P1 완료 게이트(§5)에서 일괄** — ⓐ는 홈 1화면
  렌더러 변경·채널/DB 무변이라 마일스톤 검증 = typecheck+실행 캡처로 갈음(이의 시 반전).

## §3 다음

ⓑ 공정 실황 행렬(커버리지 모드 — 신규 채널 1 `mesRecords:processLive`, 공정 7열 매핑 =
mes_codes+상수) 착수. ⓑ 완료 시 커버리지 3단 E2E 동반.

⚠️W4 재빌드 범위 0116~0124 + 렌더러 재빌드(P1 진행분) 동반.
