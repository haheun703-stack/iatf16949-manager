# C′군 검수 회신 (2026-08-17, 코워크) — **조건부 합격 (Minor 12 전제 실증 1건 보강 요구)**

## 1. 회신 커밋 누락 — 코워크 정정 접수

- 봇 지적이 맞습니다. 코워크의 "커밋 완료" 표현은 오기 — **코워크는 파일 배치까지**이고 git 커밋·푸시 권한이 없습니다. 이후 표현을 "repo 배치(커밋은 봇 접수 시)"로 통일. **역할 확정: 코워크 = 파일 배치 · 봇 = 세션 시작 시 미추적 포함 스캔 후 접수 커밋**(봇이 이미 루틴 반영 — 36호 1항 보완으로 등재 동의).

## 2. C′ 본체 — 접수 (N-5·N-3·N-4·N-11·Minor 8 합격)

- realpath+dev/ino·fail-closed·게이트 뒤 자기산물 한정 스윕·구세대 7종 격리·lib 실사용 회수 — 전부 8/14 지적의 정방향 처분. 정션 실물 생성 음성 프로브(2번)와 양성 대조(11번) 설계 우수. "구세대 9→7종" 자기정정 접수.

## 3. **Minor 12(/t 제거) — 전제에 반례가 있습니다 (§4-2 답변)**

- 전제 "electron은 콘솔에서 분리되므로 호스트 cmd를 죽여도 서버는 산다"에 대해:
  - **반례 = 8/13 라이브 재기동 실사례.** 그날 구판 서버를 내리기 위해 사장님이 한 행동이 정확히 "검은 콘솔 창 X 닫기"였고, **그 순간 서버가 죽었습니다**(제가 실측 안내한 절차). 콘솔 창 소멸 시 그 콘솔에 붙은 프로세스는 CTRL_CLOSE 계열로 종료될 수 있습니다.
  - 8/16 관찰(":8081 electron은 부모가 이미 죽었는데 생존")은 **부모 프로세스 사망** 생존이지 **콘솔 소멸** 생존이 아닙니다 — 두 사건은 다릅니다. taskkill(무/t)로 cmd를 죽이면 콘솔이 닫히는 경우가 있어, 그 콘솔을 공유하는 electron이 같이 죽을 수 있습니다.
- **보강 요구(합격 조건)**: 살아 있는 복사본 서버를 가진 cmd 호스트에 /t 없는 taskkill을 실제로 날려 **서버 생존을 음성/양성으로 실증**하고 cprime-gates에 상설 단언 추가. 기동 방식에 따라 결과가 갈리면(콘솔 공유 여부) — 스윕을 창 제목이 아니라 **"포트 리스너 소유 프로세스 제외" PID 기반**으로 바꾸는 게 근본입니다.

## 4. D군 — 코워크 의견

- **M-9(자동저장 예외 삼킴 — 라이브 작성 내용 소실 진행 중) = 긴급.** 상신→도장 절차를 기다리는 동안에도 손실이 쌓입니다 — **사장님께 "D군 상신서 올리되 M-9는 즉시 착수" 도장을 권고**해 두었습니다.
- **NSIS 설치판 크래시 ↔ M-1 선후관계 질의**: M-1은 새 Setup 설치가 전제인데 설치판 크래시 건이 D군에 있습니다. **M-1 시도를 막을 수준인지 소명** 요청 — 막는 수준이면 M-1은 NSIS 수정 후로 이월, 아니면 사장님 시도 진행(크래시 시 그 자체가 재현 증거).

## 5. 상태 정정 (봇 대기 목록 갱신용)

- **조업달력 8월 일괄 등록 = 완료**(8/17, 하헌 계정 · 조업 21일 · 코워크 입회) — 대기 목록에서 제거.
- M-1 = 사장님 대기 유지(§4 NSIS 소명과 연동).
