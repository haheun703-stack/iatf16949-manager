# S3-1 완전 종결 접수 + 서버 3기 복구 + ★정본 폴더 경로 경보 (2026-08-19 저녁, 봇)

- 접수: `dailyq-S3-1_④-4종결+서버경보_코워크_260819.md` — **④-4 합격 → S3-1 완전 종결**(④-3 67 확정 + 레거시 무해 + ⑥⑦⑩ + ④-4). 예시 수치 소견(2_9·3_1·3_5 범용 수치 1개 유지)은 **S3-2 예시층 이월** 접수.
- 지시 이행 순서: ① 서버 3기 재기동(라이브 우선) → ② :8083 3_5 실물 1줄(코워크) → ③ S3-2(forms 302 중립판).

## 1. 서버 3기 복구 — 완료 (19:14)

| 서버 | 상태 | pid / startedAt | 기동 방식 | 비고 |
|---|---|---|---|---|
| 라이브 `:8080` | **UP** | **19880 / 19:14:04** | `start-qms-server.bat`(호스트 cmd 최소화 — restart bat 3/4 단계와 동일 형태) | `copy=false`·채널 197·DB=`%APPDATA%\iatf16949-manager`. 3기 전부 죽어 있어 kill/스윕 단계(1~2/4) 불필요 — 36호 5항 사정권 0 |
| 검수 복사본 `:8081` | **UP** | **32484 / 19:14:21** | 같은 복사본 `%TEMP%\qms-e2e-260818` · 현재 HEAD 코드 · 창 제목 `QMS Review 8081` | `copy=true`·로그 `qms-review-8081.log` |
| 표준팩 상설 `:8083` | **UP** | **29936 / 19:14:22** | `start-standard-review.bat` | `copy=true`·`install.packs=standard`·DB=`%LOCALAPPDATA%\iatf-standard-review` |

- 원인 확인: 스크립트 3종 전부 `%~dp0` 기반 — **폴더명 변경(`IATF16949,SQ 자동작성 봇` → `0. IATF16949,SQ 자동작성 봇`)에 무관하게 정상 기동**. 단순 미기동(재기동 누락)이었음.
- :8083 봇 선확인(코워크 1줄 대조 전): `sq_items 3_5 = "특수공정 설비(로 등) 점검 항목 설정상태 및 시행 여부"`(before/after 문서 after 와 일치) · `forms 0 · obligations 67` · sq_items 브레이징 잔재 0.

## 2. ★경보 — 폴더명 변경이 라이브 DB 설정값 3종을 깨뜨림 (도장 요청)

복사본 DB(= 라이브 8/18 상태) 전 테이블 스캔: 옛 절대경로 `H:\…\IATF16949,SQ 자동작성 봇\…` 를 담은 행 **7건**.

| 위치 | 건수 | 영향 | 조치안 |
|---|---|---|---|
| `company_profile.mastersDir` | 1 | **★양식 xlsx 출력 실패** — `resolveMastersDir` = env → DB 값(존재 안 함) → 번들 `resources/forms`(xlsx 0) → **"정본 폴더가 설정되지 않았습니다" 에러**. 기록 입력·열람은 무영향 | 사장님이 **사이드바 '정본 폴더'에서 새 경로 재지정**(UI 1회) 또는 봇이 라이브 DB UPDATE 1줄(도장 시). 새 경로 실재 확인: `…\0. IATF16949,SQ 자동작성 봇\IATF 전체 자료모음_김권표이사_260501\3.IATF16949 규정&지침 _230501`(xlsx 58) |
| `app_config mes.drop_dir` | 1 | 앱 코드 소비처 없음(python spike 전용) — 무영향 | 같이 갱신(선택) |
| `isir_packages.source_path` | 5 | ISIR 원본 파일 링크 — 열기 시 경로 없음 | 같이 갱신(선택) |

- 봇은 **라이브 DB 무접촉 유지**(반영 = 별도 도장 관례). 도장 시 UPDATE 3종 1회로 해소 — 복사본(:8081)은 검수용이라 그대로.

## 3. 다음 = S3-2 착수 (오늘 저녁~)

- 1차 축: forms 302 중립판 → :8083 `forms 0 → 29x` · 2차: 레이아웃 4테이블 + xlsx 42종 · 3차: ④-5 규정 뼈대 템플릿 → `e2e-clean-install` ⑪ GREEN.
- ⚠ S3-2 선행 사실: 원본 양식 xlsx 폴더(`mastersDir`)는 새 경로에 **실재(58종)** — "미추출 ~30종 추출·클리어" 작업 가능.
