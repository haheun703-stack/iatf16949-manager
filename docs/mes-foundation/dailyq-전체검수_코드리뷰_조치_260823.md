# 전체 검수(코드·로직·버그·슬러지) — 7관점 코드 리뷰 결과와 조치 (2026-08-23 밤, 봇)

- 범위: 8/19 `65ff601` 이후 11커밋(287파일 · +12,828) — S3-2(표준팩 데이터·xlsx)·M2(마법사·스위치·라이선스)·M3(폰 셸)·속 채우기(예시·설명·가이드)·메뉴 버그.
- 방법: 리뷰 에이전트 7종(단순화·효율·재사용·라인 스캔·구조·삭제 행동·교차 파일) 병렬 → 봇이 검증·수정 → E2E 전 세트 재실행.
- 결과: 지적 33건 → **수정 19건 · 의도적 보류 8건 · 비해당 6건**. 수정 후 `clean-install 13/13 · setup-wizard 28/28(+3) · license 12/12(+2) · mobile 19/19 · templates 11/11`.

## 1. 보안·정합성 (수정 완료)
| # | 지적 | 조치 |
|---|---|---|
| S1 | 설치 마법사 체크박스만으로 `license.iatf_addon=on` — 유료 애드온이 키 없이 열림 | 마법사에 키 입력란(체크 시 표시). **키가 맞을 때만 on**, 없으면 off + 안내(`addonSkipped`). E2E ⑧-1(틀린 키 400) |
| S2 | `setupNeeded()` = 활성 사용자 0명 → 전원 비활성이면 `/setup` 재개방(외부에서 관리자 생성 가능) | **사용자 1명이라도 있으면(비활성 포함) 폐쇄** |
| S3 | `PUBLIC_PREFIX` startsWith → `/setup/anything`·`/setupx` 로 비인증 SPA 전체 노출 | 정확 일치 4경로(`PUBLIC_PATHS`). E2E ⑧-2 |
| S4 | 애드온 게이트가 렌더러(AppShell)에만 — curl 로 `bom:*`·`clause:*`·`iatf:*`·`process:*` 데이터 그대로 | **서버 디스패처 게이트** `403 ADDON_LOCKED`(`server/license.cjs isAddonChannel`). E2E ②-1/⑦-1 |
| S5 | 라이선스 키 계산식 3벌 복사(index.cjs·gen-license-key·e2e) + 죽은 `module.exports.licenseKeyFor` | **`server/license.cjs` 단일 출처**(verifyKey·licenseState·isAddonChannel) — 서버·발급기·하네스 3곳 require. `license:state` 라우트(호출자 0) 삭제 |
| S6 | 080/081/090 OR IGNORE = 레거시에서 운영자가 지운 양식·문서가 팩 재적용으로 되살아남 | 080~095 전부 **클린 설치 표식(`_migrations.status='snapshot'`) 있는 DB 만 적재**(insertBlockIfClean). 020~070(코워크 무해 실증분)은 유지. 하네스 ⑫ 레거시 DB 에 `_migrations applied` 시드 |

## 2. 버그 (수정 완료)
| # | 지적 | 조치 |
|---|---|---|
| B1 | **작성 화면에 `{{companyName}}` 글자 노출** — 토큰 치환이 export 에만 있고 render-model(미리보기) 없음 + 세션 캐시 | `render-model` 에 `applyProfileTokens` 적용 + 캐시 키에 프로파일 서명(변경 즉시 재구성). 실측: renderModel 토큰 0·회사명 치환 ○ |
| B2 | 토큰 치환이 6키 하드코딩·수식 캐시 결과 미치환(L3101-02 R48~Y48 `{{companyName}}` 잔존) | company_profile **전 키** `{{key}}` 치환 · 문자열/리치텍스트/**수식 텍스트+캐시 결과** 모두 |
| B3 | 폰 `Stepper`/`Chip` 이 렌더 안 선언 → 숫자 입력마다 리마운트·포커스 이탈 | 모듈 스코프로 승격 |
| B4 | 스캔 `setInterval`+async detect 겹침 → 같은 QR 로 [조회] 2~3회 발사 | 자기 예약 루프(비행 1건) + `scanningRef` |
| B5 | 탭 전환 중 getUserMedia 도착 시 카메라 트랙 미해제(LED 켜진 채) | `aliveRef` — 언마운트 뒤 도착 스트림 즉시 stop |
| B6 | `/m` 비로그인 → `/login` → 로그인 후 `/`(PC 화면) | 서버 `?next=%2Fm` + login.html 화이트리스트 + 폰 로그아웃도 next. E2E ⑧-3 |
| B7 | `/^\/m(\/\|$)/` 가 `/m/` 허용 → 상대 자산 경로 깨짐(빈 화면) | 정확히 `/m` 만 |
| B8 | 폰 부팅이 `loadUsers()`(사용자 명단·활성 사용자 동기화) 호출 | `/m` 은 `refreshSession()` 만 |
| B9 | `neutralize-xlsx` `\b인발\b` — `\b` 는 한글에 안 먹힘(규칙 사문) | 한글/영문 경계 lookaround |
| B10 | 수식 텍스트 안 리터럴(`="㈜티피씨 "&B3`) 미중립화·미검사 → 재계산 시 회사명 부활 | 수식 텍스트도 중립화·게이트(`cellStrings`) |
| B11 | 잔재 게이트에 고객사명 없음 → **HKMC 240셀·삼보 30셀이 표준팩에 실려 있었음**(강화 규칙이 적발) | `CUSTOMER_RULES`(HKMC·현대·기아·위아·모비스·삼보 → 고객사) + 게이트 추가. 238종 재생성 |
| B12 | 메가바 `flex-wrap` — 좁은 창에서 2행 위로 드롭다운이 덮임 | `flex-nowrap`(overflow visible 유지) |
| B13 | `'/api/\:channel'` 무의미 이스케이프(읽는 사람 오해) | `'/api/:channel'` 원복 |

## 3. 정리 (수정 완료)
e2e-clean-install ⑬ 의 `TPL_RE` 복사본 → `XLSX_RESIDUE_RE` import · `cellString` 재구현 → 공용 `cellStrings` · `e2e-standard-templates` 죽은 변수 `tplHasToken` 제거 · setup/login/index.cjs CRLF 혼재 → LF.

## 4. 의도적 보류 (이유)
| 지적 | 보류 이유 |
|---|---|
| AppShell 이 전체 페이지 스위치를 감싸 들여쓰기 없이 중첩 | 서버 게이트(S4)가 정본이 되어 화면 관문은 보조막 — 리팩터는 M4 이후 |
| App.tsx lazy 가 AppShell 도 lazy 로 해야 폰 번들 경량 | 폰 실기(도장 ⓐⓑ) 뒤 측정하고 결정 — 지금은 /m 청크만 분리 |
| `insp KINDS/PHASES` 등 데스크톱·폰·서버 3벌 | 공용 모듈 추출 = 기존 화면 3개 동시 수정 — M3 2차에서 |
| MobileShell `/api/brand` fetch vs IPC · 로그아웃 store 화 | 동작 정상, 구조 정리는 M3 2차 |
| 팩 적재 정책을 SQL 에 박지 말고 매니페스트로 | 러너(migrate-core) 변경 = S2 설계 변경 — 코워크 협의 후 |
| ⑬ 238파일 ExcelJS 순차 파싱(30~60초) | 게이트는 하루 수 회 실행 — 허용 |
| 생성기 dense 셀 복사·워크북 캐시 메모리 | 1회성 생성기(수 분) — 허용 |
| 키 v1 오프라인 HMAC(salt 동봉) · 회사명 변경 시 재검증 없음 | 41호 대로 **M4(정식 발급·회수)** 에서 서명/온라인 검증으로 |

## 5. 비해당
`insertBlockIfEmpty` 동작·`session` 스코프·`'\:'` 이스케이프 유효성·guide 배열 정규화 — 리뷰어 자체 검증으로 이상 없음 확인.
