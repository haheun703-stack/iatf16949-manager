# S3-1 회신 접수 + :8083 표준팩 검수 서버 상설화 (2026-08-19 저녁, 봇)

- 접수: S3-1 조건부 합격(④-3 = **67 확정** · 레거시 무해 재현 · ④-4 = 결과 텍스트 도달 후 종결).
- 코워크 §4 제안 **채택 — 즉시 이행**(사장님 "추가부분 지금 진행" 지시).

## 1. :8083 표준팩 클린설치 검수 서버 — 가동 중

| 항목 | 값 |
|---|---|
| 주소 / pid | `http://127.0.0.1:8083` / **37244** (16:13 기동) |
| DB | `%LOCALAPPDATA%\iatf-standard-review\`(안정 경로 — Temp 청소 비대상) · **표준팩 클린설치**(`install.packs=standard`) |
| 표지 / 게이트 | `health.copy=true` → 빨간 "검수 복사본" 표지 + E2E봇 로그인 허용(36호 6항). 실명 계정 0(app_users = E2E봇 1) |
| 로그인 | **E2E봇 / qms1234** (`seed-local-passwords.cjs` 1회 시드 — 복사본 전용 가드 통과) |
| 실측 | 로그인 성공 · health = `forms 0 · obligations 67 · users 1 · copy true` · `/api/brand` = `""` |
| 재기동 정본 | **`scripts/start-standard-review.bat`** 신설 — 창 제목 `QMS Standard Review 8083`(라이브 스윕 `IATF QMS Server*` 사정권 밖) · 포트 점유 감지 [FAIL] · 로그 `%TEMP%\qms-standard-8083.log` |
| 상설 한계 | 재부팅 시 소멸(자동 시작 등록은 41호 자동기동 규약 판단과 묶여 있어 **미등록** — 죽어 있으면 bat 1클릭 또는 봇 호출) |

- 42호 D 소견(`IATF_REVIEW_COPY` 유일 스위치) 연동: 현행 판정(IATF_DATA_DIR≠라이브)으로도 이 서버는 표지+E2E봇이 정상 작동. **명시 플래그 전환은 M2에서 일괄**(그때 이 bat 도 플래그 1줄 추가).

## 2. ④-4 즉시 대안 — before/after 15행 첨부 완료

- `dailyq-S3-1_④-4_before_after_260819.md` — **기계 생성**(before = 마이그 체인 원문 / after = :8083 설치 DB 실물 추출 — 파일 아닌 설치 결과라 "팩 적용 후 실제 보이는 텍스트"임이 보장).
- 검수 경로 2택: ⓐ 문서로 15행 텍스트 검수(빠름) ⓑ :8083 SQ 심사 화면에서 :8081 과 좌우 대조(실물). 둘 다 열려 있습니다.

## 3. 접수 정리

- ④-3 = 67 확정(생성기 EXCLUDE 6건 그대로 동결) · 레거시 무해·⑥⑦·⑩ = 접수.
- ④-4 = 코워크 §3 기준(요지 보존·고유성 제거·가짜 중립화 금지)으로 15행 판정 대기 → 합격 시 **S3-1 완전 종결**.
- 다음 = **S3-2**(8/20): forms 302 중립판 → :8083 `forms 0` 이 302로 바뀌는 것까지가 1차 확인축.
