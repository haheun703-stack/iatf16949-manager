# 42호 보안 점검 — 봇 실측 회신 (2026-08-19)

- 접수: 코워크 42호(바이브코딩 16항목 대입). 급소 3건 + 발견 A~C 를 코드·실측으로 종결.
- 방법: 현재 HEAD(라이브와 동일 코드)로 :8081 재기동(pid 24768) 후 무인증 타격 + 코드 판독. **라이브 무접촉**.

## 1. 급소 3건 판정

| 게이트 | 판정 | 실측 근거 |
|---|---|---|
| **발견 A — 복사본 무게이트 원인** | **스테일 확정 · 백도어 아님** | 인증 미들웨어(`server/index.cjs:177`)는 `/api/*`(health 제외)·`/download/*` 전부 세션 필수 — `copy` 예외 코드 **없음**(전문 판독). 현재 코드 :8081 재기동 후: `appUser:list`·`company:profileGet`·`company:profileSave` = **전부 401**. 코워크가 본 무인증 200 = 재기동 전 옛 빌드. → 복사본 재생성/재기동으로 해소(완료) |
| **항목 8 — 파일 저장소(수집함 사진)** | **안전(치명 실패 없음)** | 사진은 정적 서빙 아님 — 오직 `GET /api/capture-image/:id`(게이트 뒤)와 일렉트론 채널(data URL, HTTP 무). 실측: `/api/capture-image/1·2` = **401**, `/download/{임의}` = **401**, `/process-images/*`·`/uploads/*` = SPA HTML(실파일 미노출). T앱형 "공개 버킷" 구조 부재 |
| **비번 해시 + 배포 TLS** | **해시 안전 / TLS = M2** | `server/auth.cjs`: **bcrypt(bcryptjs, cost 10)** · `password_hash` NULL = 로그인 차단(N-8) · 세션 토큰 = `crypto.randomBytes(24)`. TLS = 현재 사내망 `127.0.0.1` 평문 — 판매 설치판에 "localhost 바인드 유지 or TLS" 명문화는 M2(설치 첫날) 항목 |

## 2. 발견 B·C

- **B(복사본 health DB 경로 노출)**: 사실. 라이브는 슬림(무관). 복사본 통일 = 저위험, M2 정리 대상으로 접수.
- **C(코워크 유발 프로파일 공란 12필드)**: **복구 완료** — 라이브(0144 권위값)에서 복사본 12필드 전량 원복
  (phone·revision·processes·products·plant·mastersDir·companyNameEn/Short·auditDate 등). 복사본 RW 1회, 라이브 무접촉.

## 3. 잔여 결정 항목 (판매 전, 치명 아님)

1. **사진 접근 = 순번 id + 인증 사용자 전원 열람** — 무인증은 막혔으나(위), 로그인한 직원은 누구나 `:id` 로 타 전표
   사진 열람 가능(행 소유자 검사 없음). 사내 MES 통념상 허용이나, 판매판 정책 결정 필요(부서 스코프 or 전원 열람 유지).
2. **TLS 배포 정책**(위 3번) — M2.
3. **health.copy 의미**(S2 §4 발견): 고객사 설치가 "검수 복사본" 표지를 다는 문제 — M2 설치 마법사에서 명시 플래그 분리.

## 4. 16항목 대입 — 봇 확인분 종결

| # | 항목 | 판정 |
|---|---|---|
| 1 | .env 깃 커밋 | **안전** — `git log --all -- **/.env` = 0건, `.gitignore` 7~9행 등재(.env·.env.*·!.env.example) |
| 2·3·4·5 | 프론트 키·재발급·기간 | **안전** — 프론트 0건(코워크) · 키 = 서버 .env(번들 제외) · 오늘 콘솔 키 분리로 기간·한도 설정 |
| 5(DB)·6·7 | 인증 게이트·마스터키·CRUD 분리 | **안전** — 401/403 게이트 + 권한 매트릭스 21채널(기검수) |
| 8 | 파일 저장소 | **안전**(§1) + 결정 1건(§3-1) |

## 5. 코워크 권고 이행 — M1 게이트에 보안 단언 흡수

- 권고 채택: `e2e-clean-install`(또는 신설 `e2e-security-smoke`)에 **"미인증 데이터 채널 = 거부"·"사진/다운로드 URL 미인증 = 거부"** 단언 추가.
- 구현 메모: 현 게이트는 서버 없이 DB 만 검증 → 보안 단언은 임시 서버(`IATF_INIT_DB=1`·랜덤 포트) 기동 후 무인증 프로브가 필요. **M1 잔여 작업으로 등재**(S3 표준팩과 병행). 완료 시 판매판이 매 회귀에서 이 선을 기계로 지킴.

→ **판매 관점 결론(봇·코워크 합치): 프론트 키·무인증 DB·파일 저장소 = 라이브 안전. 남은 건 결정 3건(§3)과 M1 보안 단언 흡수 — 전부 M1~M2 안에서 닫힌다.**
