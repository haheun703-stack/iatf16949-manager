# 2차분 배치⑷ 검수요청 — 설비·금형·리포트·KPI 6화면 (2026-08-12)

> **첫 줄 하네스**: E2E `scripts/e2e-d34-b4.mjs` **28/28 통과 ×2회(멱등)** + 회귀 pc1 27/27 ·
> g1 27/27 · b3 24/24(전부 :8081 오늘자 복사본) · typecheck node+web · build · build:server ·
> 클린설치 **140개 전체·FK 0**. 라이브(:8080) 재기동 완료 — 0140 적용·채널 193(+7).
> **대상**: 34호 대조표 배치⑷ = **#9 설비등록 · #10 설비일상점검내역 · #11 금형 2종 ·
> #13 KPI기준정보+배선 · #17 문서형 리포트 · #8 X BAR R**. 검수 = :8081(서규하/2222).
> ⚠고지: 본 검증에 :8081 재기동 2회 있었음(검수 회신 접수 직후 — 검수 비겹침 확인 후 진행).

## 1. #9 설비등록 (그림46·47 · 골격 + 0140 스키마)

- **0140 equipment_master** 신설 — 설비 마스터 원천 부재(덤프 line_no 는 관측치)라 행은
  **사람이 등록**(추정 시드 0 — 빈 목록 + 안내가 초기 상태 정본). line_no 연결 = 선택
  (관측 라인 datalist 참고). UPSERT 정정·STAMP·설치일 실재 검증(ymdOk)·비활성 강하(삭제 없음).

## 2. #10 설비일상점검내역 (그림48 · 전면 — 원천 2축 병렬)

- **앱 축**(L1100-07 작성 기록 — 실시간·양식 직행 링크) + **MES 축**(mac_daily — 일자×라인
  점검 항목/점검자/확인 항목/확인율). 두 축을 **정직 구분**해 병렬 표기(합산·재해석 없음).
  사이드카 부재 설치 = "MES 축 원천 없음" 명기(복사본 실측 그대로).

## 3. #11 금형 2종 (그림51·53 · 골격 — 26번 A4 계약 착지)

- **0140 mold_master** + 마스터등록/타발수조회 2탭. **타발수 = 연결 품번 생산수량(취소
  제외) ÷ 캐비티(올림)** — 캐비티·품번 미기입 = '—'(추정 금지, 툴팁 사유 명기). 잔여 수명 =
  보증 − 타발수, **≤0 = 빨강+▲**(IATF 8.5.1.6). 연결 품번 = 마스터 실존 강제 · 캐비티/보증
  = 1 이상 정수만(0 대입 금지). 화면에 "실적 연동 근사"임을 명기 — 샷 카운터 원천 확보 전
  정직. 한계 도달 알림(T계열)은 후속. E2E = 타발수 150÷2=75 실측 재현.

## 4. #13 KPI 기준정보관리 (그림63 · 전면 + 배선)

- kpi_indicators 편집(신설·정정·비활성 강하 — 삭제 없음·측정값 이력 보존). **목표·방향이
  KPI 그리드 부호 착색·성과 지표 목표선의 원천**임을 화면에 명기 + 양방향 배선 링크.
  빈 목표 = null(미설정 정직·0 대입 금지) · 비수치 거부 · 익명 정비 거부(STAMP).
  unit 은 NOT NULL(0066) — 미전달 시 현행 유지(E2E가 위반 적발 → 당일 수정).

## 5. #17 문서형 리포트 (그림17 · 골격 — 일일 실적현황 1종)

- **결재란(담당/검토/승인)** + 회사명 헤더 + 품번별 소계 + 상세 명세(취소 행 = 흐림 병기·
  합산 제외) + 인쇄 CSS(화면 = 지면 미리보기). **판매금액 열 제외(돈 경계) 지면에 명기**.
  원천 = prodList(신규 채널 0). 월별 생산판매(그림18)는 후속.

## 6. #8 X BAR R 관리도 (그림37 · 골격)

- 서브그룹 = 검사기록 1건(같은 항목 시료 2~10) · **n<2 군 제외 + 제외 수 정직 표기**
  ("시료 1개 기록 n건 제외 — 군 성립 불가"). 관리한계 = **군 크기 최빈값으로 통일**(A2·D3·D4
  — n 혼합 시 섞지 않음) · 군 2개 미만 = 한계 미계산("가짜 한계선 금지" 명기). X̄/R 2단
  차트(이탈 = 빨강+▲) + 군 표. E2E = UCL 산식 재현(CL+A2·R̄ 소수 4자리 일치). Cpk 스파이크
  이식은 후속 검토(대조표 그대로).

## 7. 배선·검증

- 메뉴: 설비관리(설비등록·일상점검 내역) · 금형관리(마스터·타발수 — soon 예약석 '금형
  타발수 대장' 실화로 대체) · 품질관리(X BAR R) · 생산관리(일일 실적현황) · 경영정보(KPI
  기준정보). PageId 6종.
- E2E 28/28 ×2(멱등 — 픽스처 = E2E 자기 잔여물 정리, 복사본 한정) + 회귀 pc1·g1·b3 78/78 ·
  클린설치 140개·FK 0. 서버 변경 배치 = 빌드·재기동 세트(:8080·:8081 — **재로그인 1회**).

## 8. 판정 요청 (사후 확인형)

1. **금형 타발수 산식** — 실적 연동 근사(생산수량÷캐비티·올림) 승인 여부. 샷 카운터 실측
   원천 확보 시 교체 전제(화면 명기 완료).
2. **설비 마스터 초기 등록 주체** — 현장(생산팀) vs 관리(품질/총무) 어느 쪽 동선으로
   안내할지(화면은 중립 — 권한은 W4 매트릭스 몫).
3. **일일 실적현황 결재란 3단(담당/검토/승인)** — 실제 결재 단계와 일치하는지(다르면
   칸 수만 조정).
