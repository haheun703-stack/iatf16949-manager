# P1 홈 개편(동아式 관제탑) — 완료 게이트 검수요청 (2026-07-31, 사무실 봇 → 코워크)

**P1 ⓐ~ⓔ 전 마일스톤 + 완료 게이트(25번 §5) 전 항목 통과 — P1 종결 검수 요청.**
재가 발효(track7_b2_final §3) 당일 완주: 견적 5.0~6.0일분을 마일스톤 4커밋으로.

## §1 게이트 결과 (25번 §5)

| 게이트 | 결과 |
| --- | --- |
| ① 28화면 회귀 | **PageId 28종 전수 캡처 스윕 전량 성공**(`captures/p1gate/gate28_*.png` 31장 — 렌더 실증. capture.mjs `--config` 전수 + `scrollY` 옵션 신설) |
| ② grid 8종 스모크 | **8/8 통과**(smoke-grid-export — ★오늘 재설계분 J3100-03 신양식·경보강 L1100-15 3섹션[영선 A7·명일 A18·보전 A29] 포함 = 7차 트랙 회귀 동반 실증) |
| ③ 커버리지 3단 E2E | **9/9**(e2e-p1b — 기록 주입→행렬 반영→공백 트리거 발행 + ⓒ 계약 2단) |
| ④ 캡처 일괄 | 31장: 28화면 + **홈 스크롤 3컷**(ⓓ 트리거 보드·ⓔ 탭+보드 — 정직 고지분 보강 완료) |

## §2 하단 존 실증(ⓓⓔ — 전 회신 정직 고지분 해소)

- `gate28_home_trigger_board.png`: **ⓓ "데이터 할 일 — 시스템 발행"** 실데이터 —
  [데이터] 증거 공백 MES 미수신(2026-07-17부터·연체 14일)·[데이터] 문서화 갭 규정 원문
  15종(기한 8/7), 각 [✓ 처리] 버튼(사람 몫).
- `gate28_home_trigger.png`: **ⓔ 하단 탭**[오늘 할 일 보드|이행 매트릭스] + 캡션("사람 의무
  존 — 반-MES 실황 아래 상시·탭 전환·삭제 없음") + 팀별 보드 전량([데이터] 배지 행 포함).
- capture.mjs `scrollY` 옵션 신설(main 내부 스크롤 대응) — 이후 검수 캡처 공용.

## §3 P1 총괄 (마일스톤 4커밋 + 게이트)

- **ⓐ** 파이프라인 밴드(54f8290 — 채널 0) · **ⓑ** 공정 실황 행렬(5eb4dc3 — 채널 1
  processLive·실측 7공정·3단 E2E) · **ⓒ** 품번×공정 매트릭스(a040497 — 채널 1
  partProcess·subul_monthly 9,452키·SO 정렬) · **ⓓⓔ** 트리거 보드 승격+하단 탭(21ffd05 —
  채널 0·정보 손실 0).
- 합계: 신규 채널 **2**(견적 2±1 적중) · 신규 테이블 **0**(사이드카만) · **마이그 0**
  (예약 0125+ 미사용 — 반납 여부 코워크 판단 요청) · 밝은 테마 유지 · 달성률 미표시(커버리지).
- 코워크 재현 검수 = ①~④ 전부 스크립트/설정 재실행 가능(p1_gate_caps 설정은 재작성 쉬움 —
  PageId 28 + scrollY 2컷·capture.mjs --config).

## §4 남은 동선 (P1 종결 후)

- 외부 대기: 레거시 3차(91종) 재가/회람 · 규정 원문 36종 · W4 3결정(+게이트 3 Critical) ·
  새 덤프(P0 — 반입 시 빌더 1명령 갱신).
- 후속 개발 후보: P2(/tv 다크)·P3(현장 원버튼 수집함) = 종전 게이트 유지 · 2단계 동아式
  완성(MES 작업지시 연동 시 계획·달성률 행).

⚠️**W4 설치판 재빌드 최종 범위 = 마이그 0116~0124(9건) + build:server(149채널) + 렌더러
(P1 ⓐ~ⓔ 전체)** — 이 회신 검수 통과 시 P1 종결·재빌드 1회로 전부 반영.
