# 검수 처분 B군 검수요청 — 403 무통지 · 정직 표기 · 권한화면 가드 · perm:save 위생 (2026-08-14)

> **첫 줄 하네스**: 신규 `scripts/e2e-cr13b.mjs` **8/8**(perm:save 위생) + cr13a **20/20** 재확인 +
> 회귀 **164/164** — 전부 **A+B 통합 코드** 기준. typecheck node+web + build + build:server.
> **:8081 재기동 사전 고지 이행**: A군 검수요청 §6 예고대로 :8081 을 A+B 통합 번들로 1회
> 재기동함(qms-e2e-260814 복사본 그대로 · 서규하/2222). **A군 검수는 이 서버에서 그대로 유효.**
> 정본 = `docs/code-review-2026-08-13.md` §7-B + Minor — TODO 정본 8/13 도장분.

## 1. M-10 — 403 무통지 3곳 → `invokeErrText` 경유

- `KpiBatchEntryModal`(kpi:save-batch — **W4-A 에서 403 이 새로 생긴 채널인데 catch 자체가
  없던 자리**): catch 신설 — 권한/세션 안내문 표출, 스피너 무한 오인 소거.
- `UserManageModal` 비번 재설정: `.catch` 신설(403/401 무통지 → 서버 안내문).
- `ObligationPage` 도래일 재설정(obligation:resetDue): catch 신설 — 동일.

## 2. M-2/M-4 — 정직 표기 (막는다고 보이는데 안 막히던 것)

- **"표시 전용" 배지**: 매트릭스 49화면 중 서버 SCREEN_GUARD 강제 밖 화면(= 규칙이 메뉴
  숨김·표기로만 동작)에 배지 + 설명문 명시. 강제 목록 정본 = 신규
  `src/shared/screen-perm-pages.ts` `SERVER_ENFORCED_PAGE_IDS`(16) — **서버 SCREEN_GUARD 와
  1:1 동기 계약** 주석 양쪽 명기.
- **프린트 비트 = "미배선" 잠금 표기**(단가 선례): 소비처 0(서버 ACT_COL 밖·렌더러 미참조 —
  실제 인쇄는 window.print 직행)인데 켜고 끌 수 있어 보이던 것 → disabled + 배지 + 툴팁.
- 설명문에 엑셀 강제 범위(트리 내 KPI그리드·FMEA 2화면) 명시 — M-3(클라 CSV 구조 한계)의
  오해 방지 문구 겸용.

## 3. M-11 — 권한관리 화면 role 가드

- `AppShell`: `screen-perm` 진입을 **role=executive 조건부**로 — 메뉴 숨김(execOnly)만으로는
  사용자 전환·딥링크 열람(perm:list = 로그인만)을 못 막던 구멍. 비executive = 전용 안내
  카드(정직 문구). 저장은 종전대로 서버 403 이 정본.

## 4. Minor 묶음

- **DEFAULT_BITS 동선**: "읽기 차단"(최빈 동선) 시 나머지 비트 자동 해제 — 종전엔 서버 모순
  거부로 1차 저장이 반드시 실패했다. 읽기 꺼짐 동안 나머지 체크박스 disabled.
- **저장 성공 메시지 덮임**: `load()` 실패 문구가 성공 메시지를 덮던 것 → 저장 결과를
  마지막에 확정(+새로고침 실패 시 병기).
- **전체 규칙 뷰(고아 점검 — M-5 동반)**: 부서 개명·퇴사 뒤 남는 규칙은 주체 선택으로는
  안 보여 "통제 중" 착각(M-5) → 전량 조회 + **고아/비활성 배지** + 행 단위 해제.
- **perm:save 위생(서버 축 — cr13b 실증)**: ①pageIds 화이트리스트(목록 밖 ID 거부 — 유령
  규칙 예방·메뉴↔목록 어긋남은 dev 콘솔 tripwire) ②subject_key 정규화('007'→'7' 단일 표기)
  ③**remove 는 존재 검사 제외** — 고아 규칙도 해제는 가능(정리 동선 보장).
- `permStore.canRead()` 죽은 API 제거(슬러지 조기 소진).

## 5. 검증 총괄

| 축 | 결과 |
|---|---|
| 신규 e2e-cr13b(화이트리스트·정규화·고아 해제·잔여물 0) | **8/8** |
| cr13a 재확인(A군 — A+B 통합 코드에서) | **20/20** |
| 회귀 | **164/164**(w4a 9·w4b 23·w4c 14·pc1 27·g1 27·b3 24·b4 28·d35 12) |
| typecheck / build / build:server | 전건 통과 |

## 6. 반영 · 검수 안내

- 렌더러 축(배지·가드·모달)은 **화면 검수 몫**: :8081 에서 ①비exec 로 screen-perm 진입 시 안내
  카드 ②트리 "표시 전용" 배지 ③프린트 "미배선" ④읽기 끄면 자동 해제 ⑤전체 규칙 뷰(고아 배지)
  ⑥member 로 KPI 일괄 기입 → 403 안내문(스피너 무한 아님) 육안 확인 부탁드립니다.
- **라이브 반영**: 렌더러 축은 새로고침만으로 반영되나 **perm:save 위생은 서버 축** — A군과
  같은 재기동 1회에 자동 동승(별도 재기동 불요). 재기동 창 = 사장님 지정 대기 유지.
