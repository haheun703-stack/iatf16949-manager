# S3-2 후반 — 표준팩 xlsx 템플릿 238종 + 회사명 토큰 — 검수요청 (2026-08-23 일, 봇 · 41호 M1)

- 근거: 40호 ④-2 ⓐ 도장("42종 전부 회사명 프로파일 치환 + 미추출 추출·클리어") · S3-2 검수요청(8/19) §4.
- ⚠ 라이브 무접촉. 서버 3기는 8/23 16:17 재기동(주말 재부팅으로 전부 다운 — 자동 시작 미등록 반복) · 라이브 pid 9776 · :8081 27496 · **:8083 = 표준팩 DB 재생성(클린 설치) pid 17780**(새 080 template_path 반영 위해 DB 삭제 후 재설치 + E2E봇 재시드).
- 결과: **클린 설치 게이트 13/13(⑬ 신설) · 출력 실증 11/11** → M1 "클린 설치 GREEN"은 데이터·파일 자산 모두 충족.

## 1. 실측 재고 — "42종"은 파일 기준, 시트 기준은 238종

| 구분 | 수 | 비고 |
|---|---|---|
| 표준팩 양식(080) | 294 | |
| 출력 템플릿 필요(셀맵/그리드 보유) | **238** | 번들 설계본·추출본 39 + 정본 규정집에서 시트 추출 199(45개 규정집, 시트 전부 해소) |
| 출력 대상 아님 | 56 | 규정 문서 행(A-2100 등)·열람형 참조 문서·필드 미정의 — template_path NULL |

## 2. 산출물

| 파일 | 내용 |
|---|---|
| **`scripts/gen-pack-standard-templates.mjs`** | 생성기(결정적·멱등). 정본 시트 1장 잘라내기 → 기록 클리어(셀맵 셀 + 그리드 행×열, 수식 보존) → 문자열 중립화 → 잔재 게이트 → `resources/templates/standard/<code>.xlsx`(시트명 = 코드). **이미지 미복사**(정본 이미지 2,948장 = 회사 로고·현장 사진 — 거의 전 양식 1장 = 로고). 대장형(뒷영역 60행↑·숫자/날짜 30%↑)은 행 절단 대신 **숫자·날짜 셀만 클리어**(구조 보존) — 3종(K1100-01·L1100-24·L4102-01) |
| **`scripts/lib/neutralize-xlsx.mjs`** | 치환 규칙: 회사명(㈜티피씨·㈜TPC·주식회사 TPC…) → **`{{companyName}}` 토큰** · `TPC-` 접두 제거 · 실명 공란 · 사업부명 → **사업부 1~4**(인발 1·필라넥 2·조관/강관 3·쇼바 4, 전 시트 일관) · 공정·반·제품 고유 표현 → 범용(공장 범례·반 이름·브레이징→핵심공정). 정본 199시트 회사식별 1,188셀·고유 62문자열 전량 처리 |
| `resources/templates/standard/` | **238 xlsx · 3.6MB**(이미지 제거 전 37MB) |
| `packs/standard/080_forms_catalog.sql` | template_path → `templates/standard/<code>.xlsx`(238) · 없으면 NULL(번들 TPC 추출본 경로를 표준팩에 남기지 않음) |
| **`src/main/docgen/form-export-engine.ts`** `applyProfileTokens` | 출력 시 시트 문자열의 `{{companyName}}`·`{{companyNameShort}}`·`{{companyNameEn}}`·`{{ceoName}}`·`{{address}}`·`{{phone}}` → company_profile 값. 토큰 없는 TPC 정본엔 no-op · 값 비면 ''(폴백 회사명 금지, S1 규약). typecheck·build·build:server 통과 |
| `scripts/e2e-clean-install.mjs` **⑬** | template_path 전건 파일 실재 · 레이아웃 보유 양식 전건 템플릿 보유 · 238파일 셀 문자열 TPC/실명/브레이징 0 |
| **`scripts/e2e-standard-templates.mjs`** | :8083 출력 실증: 프로파일 companyName=테스트값 → 8양식 submissionCreate(fact 칸 전부 채움) → exportXlsx → 재판독(시트명=코드·토큰 잔존 0·회사명 치환·TPC 0·주입값 실재) → 프로파일 원복 |
| `dailyq-S3-2_xlsx템플릿_생성리포트_260823.md` | 238행 리포트(소스·행수·대장클리어·뒷영역 숫자%·클리어/중립화/이미지 수) |

## 3. 실측

| 검증 | 결과 |
|---|---|
| 생성기 | 238/238 · 잔재 0 · 기록 클리어 1,795셀 · 중립화 94셀(26양식) · 대장 클리어 3 |
| `e2e-clean-install` | **13/13 · 판매 가능 YES** (⑬ 스캔 238·없음 0·미커버 0·TPC 0) |
| `e2e-standard-templates` (:8083 pid 17780) | **11/11** — L1200-10 회사명 치환 54셀 · J1100-04 9 · H1100-04 3 · B1100-01/A2200-01/M1200-11/F2100-02 주입 ○ · 그리드 전용 2종은 헤더 칸 없음(주입 —, 정상) |
| 레거시 | 080 OR IGNORE → 라이브 template_path 불변(⑫ 행수·양식명 불변 유지) · 엔진 토큰 치환은 TPC 정본에 토큰이 없어 no-op |

## 4. 코워크 확인 요청 (:8083 pid 17780 · E2E봇 / qms1234 · 회사 프로파일에 회사명 1회 입력 후 양식 출력)

1. **출력 품질 표본 3종 육안**: `L1200-10 대여자산 관리대장`(제목 회사명 토큰 치환) · `A5100-02 내부심사 실시 계획서`(사업부 1~4 번호화 26셀) · `D1100-02 연간 제품안전교육 계획서`(교육 대상 문구 범용화). 로고 자리 공란이 어색한지.
2. **판단 요청 ①** 로고 — 전 양식 이미지 1장 = TPC 로고를 제거해 **로고 자리 공란**. ⓐ 공란 유지(고객이 정본 교정) ⓑ 프로파일에 로고 파일 키 추가 + 출력 시 삽입(M2 설치 마법사에 편승) — 봇 의견 ⓑ(M2).
3. **판단 요청 ②** 대장형 3종(뒷영역 숫자 클리어) 구조 확인 — 특히 `L4102-01 공정능력 평가표`(계산기형: 측정 데이터 비움, 수식 보존).
4. 번들 TPC 추출본(templates/am_forms·batch4~6·sq_gap_forms 43파일)은 **그대로 둠**(라이브 template_path 가 참조) — 표준팩은 standard/ 사본만 사용. 정리 시점 = TPC팩 분리 때.

## 5. 다음

- M1 = 데이터층+파일 자산 GREEN. 잔여 = **S3-2b 판단**(description 재집필·예시 수치) · 로고 ⓑ · `IATF_REVIEW_COPY` 스위치(M2) · 설치 마법사(M2, 9/2) · 라이브 자동 시작 등록(재부팅마다 3기 다운 — 도장 대기) · 라이브 `mastersDir` 경로 도장.
