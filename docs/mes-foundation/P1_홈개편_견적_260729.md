# P1 견적 — 관제탑 홈 개편(커버리지 모드) 상신 (2026-07-29, 사무실 봇)

**근거 = 25번 지시서 §3 P1 + 23번 목업 v2 + 24번 목업 v3. §7 규율(공수·마이그 번호·신규
채널 수 선상신) 이행. 착수 게이트 = 재가(§3 "재가 즉시") — 본 문서는 견적 선상신.**

## §1 실측 요약 (견적 근거 — 7/29 사무실)

| 원천 | 실측 | P1 소용 |
| --- | --- | --- |
| 기준정보(앱 DB, M0) | 품목 2,308 · 간선 2,759 · 라우팅 1,042 · 공정 66 · 거래처 139 · 불량유형 250 | ⓐ 밴드 "기준정보 4종" 즉시 가능 |
| MES 기록(사이드카 mes_records.db) | sqc_daily **111,564행**(일자×구분×품번, 최신 **2026-07-16**) · sqc_parts 1,472품번 · mac_daily 28,458 — 원본 240만 측정행, 빌더 `scripts/mes_records_build.py` 기존재 | ⓑ 행렬·ⓒ 매트릭스의 MES 원천 **이미 집계 존재** |
| 앱 작성 기록 | form_submissions(작성가능 244종) + 완료 이력(0063) | ⓑ "오늘 기록·원천=사람" 열 |
| 앱 DB 기록 계열 | mes_records·mes_import_files·lot_registry·mat_receipt·raw_captures **전부 0건** | 사진 열은 P3 전까지 "—" 정직 표기 |
| POP 수불 원장 | 사이드카·앱 DB에 **없음** — 덤프 내 실존 여부 미실측(⚠️판독 노트: POP_* 검사계열 = 빈 테이블 선례) | ⓒ 정렬 기준 리스크 — §3-R1 |

신선도 "MES 13일 지남" = 7/17 덤프(데이터 7/16까지) 이후 미반입 — **G5(P0 다운로드 담당) 이행
즉시 기존 빌더 재실행으로 해소**되는 운영 문제(코드 변경 0).

## §2 견적 (합계 **4.5~5.5일** · 마이그 **0~1건(0114 예약)** · 신규 채널 **2±1** · 신규 테이블 **0**)

| 항목 | 내용 | 공수 | 신규 채널/마이그 |
| --- | --- | --- | --- |
| ⓐ 데이터 파이프라인 밴드 | 수신상태(기존 mesRecords:status)·기준정보 4종(기존 semimes:summary)·오늘 수신·데이터 할 일(트리거 집계) — 렌더러 조합 | 0.5일 | 채널 0 |
| ⓑ 공정 실황 행렬(커버리지) | 열=공정 7(수입검사·밴딩·용접·브레이징·리크·포장·출하 — 23번 v2), 행=오늘 기록·수량·최근시각·상태·불량·원천. 사이드카(sqc/mac)+앱 작성 결합, WRKCTR/QC_GBN↔공정 매핑=mes_codes+상수(별도 엔진 금지) | 1.5일 | 채널 1(mesRecords:processLive) · 마이그 0(상수 매핑 시) |
| ⓒ 품번×공정 매트릭스 | 1차 정렬 = **sqc_parts 검사 실측 상위**(있는 데이터로 정직) + routing_step 공정 매핑. "월 수불량(POP 수불)" 정렬은 §3-R1 스파이크 결과로 결정 | 1.0일 | 채널 1(재사용 가능 시 0) |
| ⓓ+ⓔ 트리거 보드 승격 · 의무 보드 하단 탭 | PortalHome(1,255줄) 재배치 — 정보 손실 0, 기존 채널 그대로 | 1.0일 | 채널 0 |
| 검수 하네스 | E2E(기록 주입→행렬 반영→공백 트리거 발행)·28화면 회귀·grid 8종·캡처 PNG(dev CDP — 이번엔 촬영) | 0.5~1.0일 | — |
| (조건부) R1 스파이크 | 덤프 내 POP 수불 원장 실존 실측 → 있으면 빌더 확장 +0.5일 | 0.5일 | 사이드카 확장(앱 DB 무변) |

- **신규 테이블 0** — MES 집계는 사이드카(앱 DB 밖) 확장으로만, M1 견적서 §2.5 계약 유지.
- 테마 = 현행 밝은 테마 유지(25번 신규④ — 토큰 무변). /tv 다크는 P2 범위.
- §7 동반 개정 1건: 화면분류표·19번 골격 규칙의 A군 홈 정의 갱신(문서 0.25일 포함됨).

## §3 리스크·정직 고지

- **R1 POP 수불**: 덤프 내 수불 원장 미확인 — 스파이크 실측 후 ⓒ 정렬 기준 확정. 부재 시
  "검사 실측 정렬"로 대체하고 반전 상신(가짜 숫자 금지 — 25번 신규③).
- **R2 신선도 종속**: 커버리지 신호등의 "오늘" 행은 G5 운영 이행에 종속 — 미반입 기간은
  신호등이 정직하게 공백(회색)으로 표출(23번 v2 문법 그대로).
- **R3 사진 열**: raw_captures 0건(④수집함 = G1 재가 대기) — P1에서는 열 자리만 만들고
  "—" 표기, P3 가동 시 자동 채움(재작업 0 설계).
- 검증 = 복사본/재구성 DB·라이브 무접근·자기검수 회피(사무실 재현 검증) — §5 규율 그대로.

## §4 착수 조건

재가(§3 P1 게이트) 접수 시 즉시 착수 — 예상 산출물: 마이그 0~1(0114)·채널 2±1·
PortalHome 개편 + 신규 컴포넌트 2~3(PipelineBand·ProcessLiveMatrix·PartProcessMatrix)·
E2E 스크립트 1·캡처 세트. 완료 기준 = §5 검수(28화면 회귀 포함) 전 통과.

*작성 = 사무실 봇(2026-07-29). 25번 지시서 접수 확인 — P0(G5)는 사람 액션이라 봇 몫 없음,
단 희망 시 [월수금] MES 덤프 다운로드 정기 의무 신설(품질팀, 0.25일·마이그 1)로 관제탑
보드에 흡수 가능(기승인 의무 체계 연장 — 별도 재가 불요 판단, 이의 시 반려).*
