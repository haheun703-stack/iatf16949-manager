# M-9 라이브 반영 완료 보고 (2026-08-17, 봇)

- 근거: 38호 도장 ①(사장님 확정 — `dailyq-38호도장+C프라임종결_코워크_260817.md`)
- ⚠ **도장 조건은 "오늘 점심시간"이었으나 창을 넘겼습니다**(접수 시각 13:56). 임의 실행하지 않고
  사장님께 재문의 → **"지금 즉시"** 확정 후 13:59 실행. 근무 중이라 재로그인 1회가 발생했습니다.

## 1. 재기동 실측

| 항목 | 값 |
|---|---|
| 신판 | PID **35160** · `2026-08-17 13:59:42` |
| 직전 판 | PID 40288 · `2026-08-16 10:17:06` (M-9 서버분 미반영분) |
| 기동 로그 | 채널 **197** · `migrations up-to-date` · **`대상 = ★라이브(copy=false)`** |
| :8081 검수 복사본 | **무피해**(PID 40784 유지) |
| 36호 5항 | 이행 — 사전 조회 결과 스윕 대상 0 · 라이브 런처 `KEEP` 판정 |

## 2. ⚠ 스윕이 오보를 냈습니다 — 발견·수정 (커밋 `943f1be`)

재기동 [2/4]에서 `FAILED pid 35828 - could not close (permissions?)` 출력.
**실측 결과 35828 은 정상 종료돼 있었습니다** — `Stop-Process` 직후 즉시 `Get-Process` 로
확인한 **경합**이었습니다(종료는 비동기).

이 오보는 무해하지 않습니다: 사람에게 "고아 창이 살아남았다"고 알리는 셈이고,
그건 정확히 M-12 에서 찾아 헤매던 증상입니다. 200ms × 최대 10회 재확인으로 정정했습니다.

> C′ 처분에서 새로 만든 스크립트가 **첫 실전에서 바로 결함을 드러냈습니다**. 실측 없이
> 넘어갔으면 다음 재기동 때 누군가 없는 고아 창을 찾았을 것입니다.

## 3. ★스모크 주체 변경 — 라이브에 E2E봇 계정이 없습니다

코워크 회신 §"재기동 후 확인"은 **E2E봇 로그인**을 전제했으나,
**라이브 `/api/auth:login` E2E봇 = 401**(실측). 8/13 전수 검수의 "라이브 E2E봇 없음"과 일치합니다 —
E2E봇 시드는 **검수 복사본에만** 했습니다.

36호 개정 6항은 "**복사본(`health.copy=true`) + E2E봇 한정**"이므로, 코워크가 실명 계정으로
라이브에 접속하는 것은 규약 위반입니다. → **스모크 주체가 비었습니다.**

**사장님 확정(8/17): 사장님이 30초 육안 확인**으로 대체.
(대안이던 "라이브 E2E봇 시드"는 라이브 계정 추가 = 현장 파급이고 36호 6항을 라이브까지
넓히는 일이라 별도 도장이 필요해 채택하지 않았습니다 — 필요해지면 재상신합니다.)

### 사장님 30초 절차

1. 데일리Q 로그인(재기동 직후라 재로그인 1회)
2. 아무 양식이나 열어서 값 몇 개 입력
3. **[초안 저장]** 클릭
4. **오류 없이 저장되면 정상** — 종전에는 여기서 `'…' 칸(오늘의 사실)이 비어 있습니다` 500 이
   났고, 양식을 옮기면 작성 내용이 말없이 사라졌습니다.
5. (덤) 다른 양식으로 이동 → 자동저장이 조용히 실패하지 않고, 실패 시엔 **이동이 취소되고
   사유가 뜹니다**.

→ 결과 한마디 주시면 이 파일 §4 에 기록하고 코워크 회신으로 종결하겠습니다.

## 4. 확인 결과 (사장님 회신 후 기입)

- [ ] 초안 저장 정상 여부:
- [ ] 특이사항:

## 5. 참고 — 이미 확보된 검증

라이브 스모크는 "실제 화면에서 한 번 더 보는" 성격입니다. 기계 검증은 끝나 있습니다:

- `scripts/e2e-m9-draft.mjs` **전후 대조** — 구판(:8081) **4/8**(초안 저장 500 재현) →
  신판(임시 :8102, 동일 소스) **8/8**. 표본 `M4200-01`(fact 106칸) = 8/13 검수 실측과 동일 표본.
- 완료 저장 + fact 공란 = **거부 유지**(서버 최종 관문 생존) · `createdBy:'위조'` →
  `created_by=E2E봇`(**STAMP 위조 방어 불변**).
- 라이브는 `server/index.cjs` 를 **직접 로드**합니다(번들 아님) — 임시 서버와 동일 코드 경로.
