# S1+S4 검수요청 (2026-08-18, 봇)

- 근거: 39호 도장(8/18 확정 — `dailyq-39호도장+M9종결_코워크_260818.md`) ①②③ 일괄 승인 ·
  ⑤ = S1+S4 먼저. 코워크 권고(단언 목록에 TPC 7종+실명 5인 명시) 반영.
- 커밋: 접수 `75a6e13` → S1 · S4 (이 파일과 같은 푸시에 포함)
- ⚠ **라이브 반영 아님** — 검수 합격 후 별도 도장으로 반영(0144 는 다음 라이브 재기동 시 자동 적용).

## 1. S1 — 회사 프로파일 외부화 (하드코딩 10곳 전부 배선)

| # | 위치 | 처분 |
|---|---|---|
| 1 | `ai-handlers.ts` 코파일럿 프롬프트 | 상수 → `copilotSystem()` per-call (companyShort+factoryName) |
| 2 | `briefing.ts` 브리핑 비서 | companyShort+divisionLabel(→factoryName 폴백) 조합 |
| 3 | `form-handlers.ts` AI 자동작성 프롬프트 | **3줄**(39호는 2줄로 오기 — :709 제품 목록 추가 발견) 재조합 |
| 4 | `scoring-handlers.ts` COMPANY_CONTEXT | 상수 삭제 → `buildCompanyContext(db)` per-call ×2곳 |
| 5 | `report-handlers.ts` 엑셀 제목 | 접미부 조합, 공백 시 접미부 생략 |
| 6 | `ProcessCoverDocument.tsx` 표지 로고 | `logoKo/logoEn` props — 부모가 `useCompanyProfile()` 로 공급 |
| 7 | `seed.ts` 회사명·사업부 기본값 | **중립화('')** — TPC 값은 0144 로 이관 |
| 8 | `HeaderBand.tsx` 대시보드 헤더 | `divisionLabel` prop — 공백 시 접두 생략 |
| 9 | `isir-import.ts` plant='2공장' | `getProfileValue(db,'plant')` — NULL 시 COALESCE 기존값 유지 |
| 10 | `login.html` COMPANY 상수 | 신규 무인증 `GET /api/brand` fetch — 실패/공백 = '데일리Q' 폴백 |

핵심 구조:
- **신규 키 6종**(companyNameEn·companyNameShort·divisionLabel·processes·products·plant) —
  `CompanyProfile` DTO + `PROFILE_KEYS` + GET 핸들러 **3점 동시 등재**. 누락 시 SAVE(전체 객체
  라운드트립)가 해당 키를 '' 로 와이프하는 결합을 실측 확인 → register.ts 에 주석으로 명문화.
- **`0144_company_profile_tpc_values.sql`** = 데이터 전용 · `[TPC팩 후보]` · ON CONFLICT DO NOTHING.
  마이그가 시드보다 먼저 → 라이브/현행 설치 무열화, S2 태그 스킵 시 판매판 자동 중립화.
- 공백 정책: 값이 비면 구절/줄 생략(빈 괄호·빈 대시 금지) — 라이브 값으로는 **현행 문구 바이트 동일**.
- health 슬림 계약(37호 ④·M-12) 불변 — 회사명은 별도 무인증 단일 필드 라우트로 분리.

## 2. S4 — 판매판 게이트 `scripts/e2e-clean-install.mjs`

- 8단언: ①마이그 전수 ②FK 0 ③시드 소스 파리티(미지의 json=FAIL) ④시드 적용 ⑤프로파일 키 완전성
  ⑥**TPC 계열 7종 0건** ⑦**실명 5인 0건** ⑧companyName 플로우스루(스캔 후 주입).
- 라이브 무접촉 구조: `%TEMP%` mkdtemp 전용, **DB 경로를 env/argv 에서 받지 않음**(verify-migrations 모형).
- **첫 실행 = 의도적 RED (베이스라인)**: `판매 가능 여부: NO — TPC 계열 478행 · 실명 124행` (6/8).
  지표 = 패턴×테이블 매치 행수 합(39호 §1 "발생 수"와 단위 다름). 상위 테이블 = regulation_sections 지배적.
- README-e2e **§5 판매판 회귀** 신설 — 활성 153건 GREEN 세트와 분리, RED 가 정상인 유일한 하네스.

## 3. 기계 검증 실측 (전건 통과)

| 검증 | 결과 |
|---|---|
| `npm run typecheck` (node+web) | 통과 |
| `npm run build:server` + `npm run build` | 통과 |
| `verify-migrations.mjs` (0144 포함 143개) | 전수 적용 · FK 0 |
| `e2e-clean-install.mjs` 첫 실행 | 6/8 — ⑥⑦ 의도적 RED(위 베이스라인) |
| `e2e-w4a`(health 슬림·9) · `e2e-w4b`(권한 27) · `e2e-cr13a`(승격 봉쇄 20) | **56/56** (:8081 복사본) |
| `/api/brand` 실측 | `{"companyName":"주식회사 티피씨"}` · 비로그인 health 슬림 유지 |
| SAVE 라운드트립(E2E봇, :8081) | **17키 전량 보존**(와이프 회귀 없음) · 신규 키 = 0144 값 확인 |
| `/login` 페이지 | api/brand 배선 · 하드코딩 잔재 0 · 데일리Q 폴백 존재 |

## 4. 코워크 확인 요청 (:8081 관례)

1. **화면 실측**: 대시보드 헤더 `AM사업부 · 기준일 …` / 문서 BOM 표지 `티피씨/TPC` 로고 /
   로그인 파티클 클릭 → `주식회사 티피씨` 모임 — 종전과 동일해야 함(바이트 동일 목표).
2. **AI 경로 1회**: 양식 자동작성 또는 AI 채점 1회 — 프롬프트 정상(회사 컨텍스트 포함) 확인.
   채점 리포트 xlsx A1 = `IATF 16949 AI 채점 리포트 — TPC AM사업부`.
3. **클린 설치 하네스 대조**: `e2e-clean-install.mjs` 재실행 → §2 베이스라인(478/124)과 대조.
4. 설정 팝오버 작성자 저장 1회 → 신규 키 값 보존 재확인(§3 실측의 재현).

## 5. 범위外 플래그 (S2/S3 대상 — 이번 검수 대상 아님)

FormScope 5사업부 union(`ipc-types.ts` — 구조적, 타입이 DB 값과 결합) · SCOPE_COLORS ·
`process-handlers.ts` 프롬프트 예시 2곳(TPC-CP-03·정밀인발튜브사업부) · '2026-12-31' 폴백 중복 ·
seed `revisionNumber` · `docNoPrefix` 키 보류(소비처 없음 — 죽은 설정 금지).
