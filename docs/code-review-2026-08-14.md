# 전수 검수 — 2026-08-14 (검수 처분 A·B·C 이행분 + 코드·로직·버그·슬러지)

> **방식**: 4축 병렬 판독(A 서버·보안 / B 렌더러·권한 UI / C 하네스·게이트 / D 교차·이행 대조)
> → 봇 전건 재검증(로드베어링 Read 실증 + 구성 검증) → 실행 실증 2건(동시 내보내기 경합 프로브
> ×5·라이브 무오염 실측). 축 B·D 가 신규 무통지(사용자 관리)를, 축 A·C 가 copy 게이트 허점을
> **독립 발견 = 교차 성립**. 추측 지적 불채택 — 추정 잔존 항목은 본문 명시.
> **대상**: 오늘 커밋 4건(71b51e1·7b123d5·a6f87ae·e8ee08b) 전 diff + 접점 서버·렌더러·하네스.

## 0. 총평

어제 Critical 2건(C-1 승격·C-2 라이브 개방)은 **닫힌 것이 재확인**됐고, 오늘 신규 Critical 은
**0건**이다. 대신 오늘 작업이 만든 **새 결함 2계열**이 나왔다: ①M-1 소생이 sq 내보내기의
**라이브 DB 경로 각인**을 함께 깨웠고(재기동 후 발화 — 재기동 전 처분 필수) ②C-1 봉쇄가
사용자 관리 화면에 **새 무통지 5동선**을 만들었다(어제 "내가 만든 403에 catch 없음"과 같은
유형의 재발). 그리고 오늘 넣은 **하네스 게이트 자체의 허점 4건**을 적대 판독이 잡았다 —
게이트는 실재하나 우회 여지가 남아 있다. 문서 과대 주장 3건은 자기정정으로 §5에 등재.

## 1. Critical

**없음.** (어제 C-1·C-2 = 닫힘 재확인 — §4 이행 대조)

## 2. Major (11)

### 오늘 커밋이 만든 것 (회귀 — 우선 처분)

| # | 내용 | 근거 |
|---|---|---|
| **N-1** ✔재검증 | **sq:assessExport 소생이 라이브 DB 오염을 동반** — 웹 모드에서 `report_path` 에 서버 임시 토큰 경로(1회 다운로드/30분 TTL 후 삭제)를 UPDATE. 기존 데스크톱 정상 경로도 덮어씀. 라이브는 아직 구판이라 무피해 — **재기동하는 순간부터 발화 → 재기동 전 처분 필수** | `sq-report-exporter.ts:136` ↔ `index.cjs` 토큰 정리(축 A 발견·봇 Read 확정) |
| **N-2** ✔재검증 | **C-1 이 사용자 관리에 새 무통지 5동선을 만듦** — appUser 쓰기 3종이 executive 전용이 됐는데 ①모달 진입 role 게이트 없음(SettingsMenu 무가드 확정) ②비exec 화면에서 편집 UI 활성 ③부서/역할/활성/등록/삭제의 403 을 store `catch{}` 가 삼킴 ④이름 변경 실패 문구 "이미 있는 이름" 오안내. B군 M-10 수리는 resetPassword 1곳만 | `UserManageModal.tsx`·`activeUserStore.ts:94-115`·`SettingsMenu.tsx:286` (축 B·D 독립 동시 발견 = 교차) |
| **N-3** ✔구성 확정 | **w4b 시작 스윕이 게이트보다 앞 + 전체 DELETE** — 복사본 확인(로그인 후 copy 게이트) 전에 `DELETE FROM screen_permission` 전 행. assertCopyDb 우회(N-5)와 겹치면 라이브 규칙 소실로 증폭. E2E 산물 식별 없는 전량 삭제 자체도 과격 | `e2e-w4b.mjs:59` (오늘 추가분 — 축 C) |
| **N-4** ✔구성 확정 | **w4a 만 copy 게이트가 소프트** — health.copy≠true 여도 check 실패로 카운트만 하고 계속 진행 → 3단 조업달력 **쓰기 실행**. 나머지 9종은 하드 exit | `e2e-w4a.mjs:49-51` (축 C) |
| **N-5** ✔구성 확정 | **assertCopyDb = 프리픽스 문자열 비교뿐** — 정션·subst 드라이브로 우회 가능, `APPDATA` 미설정 환경에선 라이브 거부가 아예 수행 안 됨(realpath 미수행) | `lib/e2e.mjs:27-33` (축 C) |
| **N-6** ✔구성 확정 | **health.copy 가 "라이브 아님"을 보증 못 함** — `!!IATF_DATA_DIR` 존재 여부만 봄. env 가 라이브 폴더를 가리켜도 copy=true(거짓 안심) | `server/index.cjs` IS_COPY_SERVER (축 C) |

### 기존 잠복 (오늘 diff 밖 — 오늘 무게가 실린 자리)

| # | 내용 | 근거 |
|---|---|---|
| **N-7** ✔재검증 | **M-11 가드가 UserSwitcher 로컬 role 을 신뢰** — 웹에서 member 가 비번 없이 스위처 클릭 1회로 '경영진' 선택 → 매트릭스 **열람**(perm:list = 로그인만). 저장은 서버 403 유지. 진짜 가드 = 세션(auth:me) role 이어야 함 | `AppShell.tsx` isExec ↔ `UserSwitcher.tsx:103-105`(봇 Read 확정) |
| **N-8** ✔재검증 | **NULL 비번 계정 = 첫 로그인자가 점유** — 신설 계정(비번 미설정)은 처음 입력한 비번으로 즉시 등록돼 실명 STAMP 로 활동 가능. 라이브 실측 NULL 0명 = 현재 무피해, 신설 창구가 위험 | `auth.cjs:40-48`(봇 Read 확정) |
| **N-9** | **actorDenied 데스크톱 축은 자기신고 신뢰** — bridge 직접 호출자가 `actorRole:'executive'` 를 실으면 통과(서버만 세션 덮어씀). 데스크톱=로컬 DB 직접 수정 가능 환경이라 실질 위협 등가이나, 검수요청 A 의 "조회 전용" 주장은 과대(§5 정정) | `app-users-handlers.ts` (축 D) |
| **N-10** | **정직 표기 배지의 축 과장** — 배지는 화면 단위인데 강제는 (화면×행위) 단위. fmea 는 엑셀만 가드(쓰기 채널 fmea:rowUpdate 등 비가드)인데 배지 없음 = "쓰기·수정·삭제 403" 문구 과장. item-master(수정만)·mat-receipts(삭제만) 동일 | `ScreenPermPage.tsx:349`·`screen-perm-pages.ts` (축 B) |
| **N-11** | **구세대 하네스 9종 = 기본 대상이 라이브 :8080** — batch1~6·b4·w2-smoke(쓰기 채널 포함)·w2-screens. env 없이 실행하면 라이브로 발사. + smoke-grid-export.cjs 는 env 없으면 라이브 DB open(readonly) | 축 C 열거 |

## 3. Minor (12)

1. 의무(Obligation) 계열 무통지 잔여 — 생성·수정·삭제 catch 없음(모달 열린 채 침묵), 이행(complete) 가드·catch 전무, 목록 조회 실패가 "의무 없음" 오인 표시 (`ObligationModal.tsx:71-88`·`obligationStore.ts`) — 봇 Read 확정
2. `obligationStore.complete` 가 `{id}` 만 전송 — 데스크톱 이행 시 done_by=NULL 적재(주체 원칙 위반·웹은 STAMP 가 구제)
3. `window.prompt` = Electron 미지원 — 데스크톱에서 비번 재설정 버튼 무반응(웹 전용 동작)
4. perm:save ipc 핸들러 자체엔 role 검사 0 — 데스크톱은 아무나 저장 가능("서버 403 정본" 문구와 어긋남)
5. `removeOne` singleFlight 밖(연타는 confirm 중첩이 실질 차단 — 규약 위반) · KPI 일괄 저장 버튼 singleFlight 미경유
6. 고아 판정 비대칭 — 전원 휴직 부서가 '고아'로 오표기 가능·users 미로드 순간 전 규칙 고아 표시(추정)
7. `form-builder` = 강제 화면인데 트리 미노출 — 엑셀 차단 규칙을 걸 UI 동선이 없음
8. lib/e2e.mjs 수출 5종(loginBot 등) **소비처 0** — 게이트 4줄이 8개 하네스에 신규 복붙(그 표류가 N-4 원인). S-2 "동시 종결" 주장 대비 실체 축소(§5)
9. assertBaseNotLive 정규식 — `:8080?x`·`#x` 우회, 비표준 포트 라이브엔 무력 · cr13a 는 IATF_DATA_DIR 미검사(w4c 와 불일치)
10. 다운로드 토큰 = 비암호 난수 + 소유 세션 미바인딩(로그인자 간 선취 가능·30분 창) · 파일명이 채널명 고정(핸들러 의도 이름 소실 — sq_assessExport.xlsx)
11. bridge 공용 DB 연결(`connection.ts`)에 busy_timeout 없음 — authDb(4000ms)와 겹치면 SQLITE_BUSY 즉사(봇 Read 확정)
12. restart.bat WINDOWTITLE 스윕이 동명 제목 무관 창(메모장 등)까지 /t 트리킬 · `e2e-tvboard-poll.mjs` 분류 누락(무게이트 미고지 — 읽기 프로브라 완화)

## 4. 이행 대조(어제 → 오늘) 요약 — 축 D 전건 대조

- **닫힘(17)**: C-1·C-2·M-1·M-2·M-4·M-7·M-10(지적 3곳)·M-11(명세)·M-12·M-13·M-14·M-15·M-18 + Minor 6종 + S-1·S-4·S-5
- **부분(5)**: M-5(관측·정리 동선만 — 고아 발생 근본 그대로)·M-17(활성 10종만)·S-2(§5)·S-3·S-6
- **미이행 = 처분 범위 밖(6)**: M-3·M-6·M-8·M-9(★라이브 데이터 소실 현존)·M-16·NSIS — D군·잔여 트랙
- **수치 재검산**: 오늘 문서 A·B·C 의 단언 수·화면 수·마이그 수 **전건 일치**(어제 자기정정 유형 재발 0)

## 5. 내 문서 자기정정 (3)

1. 검수요청 A "데스크톱 사용자 관리 = 조회 전용" → **과대**. 정확히는 "기본 UI 흐름에서 거부" — bridge 직접 호출은 actorRole 자기신고로 통과(N-9)
2. 검수요청 C "S-2 동시 종결·check/login/api 공용화" → **부분**. 게이트만 공용화 — login/api/check 는 복제 잔존, lib 헬퍼 5종은 소비처 0
3. 검수요청 C "KST 5벌 회수" → 실 3벌(w4c·m1-attach 잔존 + 오늘 1벌 신규). pc1 헤더 사용례(`서규하`)는 게이트와 모순된 채 방치

## 6. 실증·무혐의 (봇 직접)

- **동시 내보내기 경합 = 기각** — 동일 채널 동시 2발 ×5회 전부 양쪽 성공(169,210B). 슬롯 주입→소비가 단일 동기 구간(전 핸들러 첫 await = showSaveDialog). 단 이 안전은 "다이얼로그 전 await 금지" 암묵 불변식 — 명문화 필요(슬러지)
- **라이브 무오염 실측** — E2E봇 계정 0·E2E- 지시 0·E2E봇 주체 기록 0·권한 규칙 0행·recordSource=direct·:8080 구판 정상 서빙
- actor 주입 우회·검사 순서·health 노출 적정·0143 구판 호환 — 축 A 무혐의 + 봇 교차 확인

## 7. 처분 제안 (우선순위 — 내일)

**A′. 라이브 재기동 전 필수(서버)**
1. **N-1** sq:assessExport 웹 모드 report_path 각인 차단(웹=미갱신 또는 영구 보관 경로)
2. **N-6** IS_COPY_SERVER 판정 강화 — IATF_DATA_DIR 가 라이브 경로면 copy 아님
3. **N-8** NULL 비번 선점 제거(신설=비번 필수 or 선점 금지 → resetPassword 경유) — 판단 여지 有

**B′. 렌더러**
4. **N-2** 사용자 관리 — 진입 role 게이트 + 무통지 5동선 catch·정확한 문구(admin 화면이므로 문안 간단)
5. **N-7** M-11 가드를 세션(auth:me) role 로 — 스위처 우회 봉쇄
6. **N-10** 배지 축 정밀화(행위별) 또는 문구 완화 · Minor 1·2·5·6·7

**C′. 하네스**
7. **N-3·N-4·N-5** 스윕 위치(게이트 뒤)+범위 한정 · w4a 하드 게이트 · assertCopyDb realpath+무조건 검사
8. Minor 8·9·12 — lib 헬퍼 실사용(복붙 회수) or 헤더 정정 · tvboard-poll 게이트 · 구세대 9종 처분(게이트 이식 or 명단 격리)

**D. 사장님 판단(기존 + 승격 1)**
9. M-8 죽은 라우트 · **M-9 초안 저장(최우선 — 라이브 소실 현존)** · NSIS · (승격 제안) M-6 세션 스냅샷 — 권한 변경 시 세션 무효화

---

*검증 누계(오늘): 4축 병렬 판독(교차 2건 성립) + 봇 재검증(Read 확정 8·구성 확정 4) + 실행 실증 2(경합 기각·무오염). 라이브 접촉 0.*
