# 전체 검수 보고서 — 2026-07-07 야간

방법: 독립 검수 에이전트 3개 병렬(①오늘 diff 정밀 ②팀허브·렌더러 로직 ③슬러지 스윕) + 상위 발견 직접 재검증.
범위: 오늘 커밋 16277cd..2f18de7 전체 + 팀허브 신규 영역(7/6~) + src/ 전체 슬러지.

## 종합 판정

**Critical 0 — 배포 차단 결함 없음.** 오늘 나간 4차 설치판 그대로 사용 가능.
수정 대상은 버그 3건(그중 확정 1건) + 하드코딩 13곳(기존 알려진 4곳 포함) + 죽은 코드 2덩어리.

## 🔴 수정 권고 — 버그 (내일 1순위)

| # | 심각도 | 위치 | 내용 |
|---|---|---|---|
| 1 | MED·확정 | `TeamDetailView.tsx:340-348` (StepCard) | try/finally에 **catch 부재**. IPC reject 또는 SQ_ITEM_DETAIL null 응답 시 `detail=null·loading=false` → 렌더 조건 `loading \|\| !detail`이 **영구 "불러오는 중" 스피너**. 형제 컴포넌트(RegRow·TeamRegSection 등)는 전부 catch 방어됨. 수정: catch 추가 + loading/null/data 3상태 분리 |
| 2 | MED | `migrate.ts` snapshotBeforeMigrations 보존정리 | 파일명 정렬(마이그번호 우선)이라 **스냅샷 롤백 직후 재기동 시 방금 만든 백업이 먼저 삭제**될 수 있음(pre-0060_새것이 pre-0062_옛것보다 정렬 후순위). 정방향 운영은 무해. 수정: mtime 정렬 |
| 3 | MINOR | `team-handlers.ts` readinessPct | `/wsum`에 0 가드 없음(전 항목 0점이면 NaN% 표시). sq-handlers 동일 계산엔 `\|\| 1` 있음 — 정합화 |

## 🟠 제품화 — 하드코딩 13곳 (클린채널 로드맵과 묶어 일괄)

- **실명 4건(최우선)**: 서상규×2(`form-handlers.ts:557`·`scoring-handlers.ts:85` — COMPANY_CONTEXT 중복 2벌), 이인창(`process-handlers.ts:37,49` 프롬프트 예시), 이정훈+실주소+실전화(`seed.ts:144-156`)
- **실명 포함 D:\ 경로가 번들에 실림**: `form-export-engine.ts:21-22` FALLBACK_MASTERS(김권표 경로 — 최후순 폴백이라 동작 무해, 문자열 노출이 문제)
- 기타: `ai-handlers.ts:41`·`briefing.ts:105`(TPC 프롬프트), `report-handlers.ts:73`, `isir-import.ts:17-28`(고객사 10개), `ProcessCoverDocument.tsx:18`, `SqReadinessPage.tsx:70`, `Dashboard.tsx:76`·`TopBar.tsx:31`
- 처방: 제품화 감사 3층 팩 분리 때 일괄. 단 **실명 4건+경로 1건은 그 전에라도 선제 제거 권장**

## 🟡 슬러지 — 두 덩어리에 집중

1. **v4 잔재 (즉시 삭제 안전, 사용처 0건 검증됨)**:
   - `src/renderer/src/domain/` 11파일 전체(entities 7+value-objects 4)
   - `register.ts` 죽은 핸들러 **19채널 ~540줄**(clause/document/task/team/dashboard 등 — 이 파일의 라이브는 COMPANY_*·PRINT_TO_PDF뿐) + ipc-channels/ipc-types 대응 항목
   - `ai/index.ts:81` getProvidersStatus, `scripts/seed_defect_cases.py`(대체 명기됨)
2. **중복 6계열(수렴 대상)**: ch()+ChannelKey 12벌 / company_profile 단일키 조회 6벌 / '2026-12-31' 상수 3벌 / D-day 계산 8벌(ceil·round 혼재 — 경계일 ±1 불일치 가능) / `ClauseCoverageView.tsx:8` 부서색 자체구현(team-theme 정본과 **불일치**: 생산팀 rose vs green) / Anthropic 클라이언트 2벌(의도 문서화됨)

**클린 확인**: TODO/FIXME 0 · 주석코드블록 0 · `as any` 0 · 위험한 빈 catch 0 · 데모버튼 노출 0. 렌더러·AI 레이어 매우 깨끗.

## 🟢 LOW/PLAUSIBLE (기록만, 수정 비권장~선택)

- useDday: 초기로드 vs 저장 경합(이론상)·구독 직전 알림 놓침(≤1h)·regex 범위 미검증(수동 DB 편집 시 D-NaN)
- Sidebar: profile null이면 심사일 저장 무음 폐기
- briefing.ts:23 `today`가 UTC라 KST 00~09시 폴백 D-day +1(기존 코드)
- TEAM_REGS deprecated 필터 부재(현재 0건)·formsTotal에 규정문서 행 포함(표시 불일치)·폴백 항목 펼침 시 지침 섹션 빈 목록(문구 없음)
- 0060 form_approvals에 UNIQUE(submission_id,step) 없음 — 상태머신 구현 시 추가

## ✅ PASS 하이라이트

신규설치 0001→0062 순서·FK·행존재 전부 통과(0062의 sq_items 18코드 실재 확인) · SQL 5본 멱등 · singleInstanceLock 등록순서/가드 정확 · 스냅샷 실패 시 기동 비차단 · Sidebar Enter/blur 이중저장 없음 · zustand 셀렉터 트랩 재발 없음(e0ee5c8 방지 주석 유지) · 팀 전환 race 원천봉쇄(alive 플래그) · useDday 누수 없음 · auditDate 파이프라인 왕복 일치.

## 판단 필요 (사장님/도메인)

1. DOCGEN 2채널+quality-manual-generator.ts — 품질매뉴얼 생성 부활 계획? (현재 도달 불가)
2. AI_DRAFT_LIST·FORM_SCORE_LIST·CASE_UPDATE — 예약 기능인지 삭제인지
3. seed.ts persons/documents 시드 — v4 핸들러 제거 시 함께 정리?
4. 일회성 scripts 10개 — 삭제보다 `scripts/archive/` 이동 권장(마이그 생성기 이력 보존)
