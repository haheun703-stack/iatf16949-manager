# 31호 관제탑 3정5행 재편 — 정보 손실 0 확인표 (2026-08-06)

> 근거 = 지시서 31호 §3·§7 (제거 = 위치 이동만, 데이터·채널 삭제 0). PB2a 확인표 방식.

## 1. 홈에서 이동·집약된 블록 (§3)

| 기존 홈 블록 | 새 위치 | 손실 |
| --- | --- | --- |
| 품번×공정 실황 표 | 생산관리 메뉴 → **품번×공정 실황**(신규 조회 화면 part-process — 표 컴포넌트 무수정 재사용, ★중점관리·히트맵 포함) | 0 |
| 기록 커버리지 매트릭스 | 홈 3층 **공정 스트립 클릭 시 펼침**(같은 화면 2층 상세 — 컴포넌트 무수정) | 0 |
| 공정 실황 도넛 7카드 | 3층 **스트립 1줄**로 축약(문제 공정만 강조·주간 % 유지). 도넛 컴포넌트 파일은 존치(미표출) | 0 (수치 전부 스트립·상세에 있음) |
| 파이프라인 밴드 — MES 수신 상태 | 1층 **문제 배너**(미반입 ≥2일·사이드카 미가용 시만 표출, 클릭 = MES 기록 현황) | 0 (문제 없을 땐 비표출 = §1 청소) |
| 파이프라인 밴드 — 데이터 할 일 칩 | 1층 문제 배너("할 일 n · 연체 n · 데이터 발행 n", 클릭 = 보드 점프) | 0 |
| 파이프라인 밴드 — 기준정보 4종 집계 | **품번 트리 화면 기존 표출 실측 확인**(semimes:summary 동일 채널) — 기준정보 모듈 첫 항목 | 0 |
| 심사 D-day | 배너 미포함 — 상단 TopBar DdayBadge 상시 표출 기존재(**동일 데이터 2회 금지** §5-2 적용) | 0 |

## 2. 신설 (one-in 대응 out 명시 — §5-1)

| 신설 | 대응 제거/강등 |
| --- | --- |
| 1층 문제 배너 | 파이프라인 밴드 제거(통합) |
| 2층 오늘 할 일 TOP5 | (in) — 대응 out = 도넛 7카드 → 스트립 강등 |
| 3층 공정 스트립 | 도넛 카드·커버리지 매트릭스 상시 표출 제거 |

홈 위젯 수: 배너(조건부)·TOP5·스트립 + 기존 잔존(내 할 일 히어로·데이터 할 일 보드·KPI·팀별
현황·보드/매트릭스 탭 존). **기존 잔존 블록의 추가 정리는 코워크 검수 판정에 위임** — 31호 §3
명시 목록만 이행(임의 확대 금지). §5 가드 3줄은 PortalHome 주석에 UI 헌법으로 명문화.

## 3. 사이드바 (§4)

- 기본 접힘 2섹션: APQP·Core Tool, 문서·양식 — **항목 삭제 0**, 헤더 클릭 펼침(localStorage
  영속·현재 화면이 섹션 안이면 자동 펼침).
- 흐림(dim) 처리 = 상단 7모듈 메뉴 중복 항목 12종(진입 존치 — 시각 서열만):
  sq-audit·sq-dashboard·iatf-dashboard / dashboard·case-work·obligations·team-hub /
  parts·fmea·msa / document-bom·process-workbench.

## 4. §6 모달 시트 (적용 1호)

- 공용 ModalSheet(PC 중앙 모달·폰 풀스크린 1컴포넌트, ESC·바깥클릭 시 미저장 확인, 자동 저장 없음).
- 수집함 태깅: 우측 고정 폼 → **모달(좌사진·우폼 2단)**. 우측 패널은 요약 카드 상시로 전환 —
  기존 기능(업로드·태깅·재태깅 거부·요약) 전부 유지. ⑥-1 후속 적용(보드 처리·확인자 서명·취소
  +재등록)은 도장 후.

## 5. §8 소견 B 안1 (사장님 확정)

- T4 발행 버킷 = 전표일 유지(증거 정직). 태깅일 > 전표일이면 제목 = "소급 기입 n일" 라벨,
  연체 판정 축 = 태깅일(태깅일 이후에도 미기입일 때만 '연체'). 당일 태깅은 기존과 동일.
