# 7차 트랙 1호 교정(K2100-06) + 표본 5종 심층 실측 (2026-07-30, 사무실 봇)

레거시 셀맵 스윕(`legacy_cellmap_sweep_260730.md`) 접수 이행분. **K2100-06 교정 완료(0115) +
표본 5종 실측 = 부실 4·부분 정상 1(부실률 80%)** — 배치 규모 판단 근거 §4.

## §1 K2100-06 선입선출 관리 기준서 — 교정 완료 (마이그 0115 + E2E 10/10)

- 실측: 마스터 단일 시트(변형·AM 없음, 25행×AV). 갑지형 상단(결재란 담당 AN2 = M4200-01
  동일 패턴) + 기준 테이블(헤더 15행, 데이터 = **3행 병합 2블록** 16:18·19:21) + 도식화
  영역(라벨 A22:B25 + 대형 병합 C22:AV25) + 빈 개요 블록 A4:AV14(라벨 부재 — 시트 몫).
- 재설계: 기준 grid(순 A·시기 C·장소 I·관리기준 O·담당부서 AQ, start 16·stride 3·max 2)
  + 도식화 textarea(C22) + 작성자(auto, AN2). 구 6필드(헤더 라벨 1행 오인)·잔재 설명 제거.
- 검증(라이브 새 복사본 e2e-data8, 서버 0115 자동 적용·:8081 병행 — 라이브 :8080 무접촉):
  **E2E 셀검증 10/10 첫 실행 전 통과** · engine 2/2 · media/merges OK · 실물
  `captures/batch7_e2e_K2100-06.xlsx`. 하네스 = `scripts/e2e-batch7.mjs`(BASE 가변 —
  7차 진행분 누적용).
- **audit `legacy` 플래그 신설**(스윕 §2 기준 공식화: fields>0 + 설명 "자동 추출" 잔재/공란):
  **legacy_cellmap 157→156**. 이후 진척은 audit 한 줄로 추적된다.

## §2 표본 5종 심층 실측 (마스터 병합·프리셋·수식까지 대사)

| # | 코드 | 판정 | 결정 근거(실측) |
| --- | --- | --- | --- |
| 1 | L2100-02 코일인수 검사일지(-조) | **부실** | 다행 일지(9행~)를 1행(9행)만 매핑 + **고아 셀맵**(폭05mm→U9, 필드 없음) + 측정 하위구조(강종/두께/폭/조수·허용차·판정) 오인. ★마스터에 **-조(조관) 변형만 존재** — L2100-01/05 선례(조 전용 기각→AM 실사용 MES 양식 채택) 동계열 → **사업부 적용성 판정 필요**(상위 20의 L2100-03/06/10도 동류) |
| 2 | L1100-07 설비 일상 점검표 | **부실** | 점검항목 4행(stride 2)에 숫자키 '1'~'4' + 점검방법 프리셋 '육안'을 **일별 결과 칸**(N6~N12)에 중복 매핑(오인) + 31일 결과 매트릭스 미인식. 교정 = 헤더 3 + 항목 grid + 매트릭스 시트 수기(H2100-02 최소 매핑 선례) |
| 3 | L1100-15 업무 일지 | **부분 정상** | grid 8열 = §3 설비 보전활동 실구조(발생처·설비명·설비번호·활동내용·소요시간·소요자재·구분·비고)와 **정확 일치** · 제출 2건 유효. 미비 = 날짜 헤더(A3)·§1 영선활동·§2 명일 예정 미매핑 → **파괴 재설계 불요, 경보강 후보**(제출 보유 grid 클래스의 표본) |
| 4 | L1200-02 치공구 이력카드 | **부실** | 파손·변경 이력 16행의 **날짜 열**(AI12~27)에 숫자키 '1'~'16' + 생산업체 3행(AI6~8)에 '1'~'3' + 이력/수량/이관일 1행만 + 금형약도(도해 영역)에 텍스트 매핑. 교정 = 헤더 7 + 이력 grid 2개 + 특이사항 textarea + 약도 시트 수기 |
| 5 | K2100-05 장기 재고 점검대장 | **부실** | ⚠️**수식 셀 입력 매핑**(평가결과→N28, 마스터 N28=수식 — 저장 시 수식 덮어씀 위험, 표본 중 최위험 유형) + 월별(1~12월) 매트릭스 흩뿌림·필드 없는 셀맵 다수·중복 키(평가자 등 상하단 2회). _구본 시트 병존(첫 매치=신본이라 시트 선택 자체는 정상) |

## §3 부실률·유형 분포

**부실 4/5 = 80%** (경보강 포함 시 5/5 손댈 것). 유형(중복 집계): 다행표 1행 매핑 3 ·
숫자키 잔재 3 · 라벨/프리셋 오인 3 · 고아 셀맵 2 · 매트릭스 미인식 2 · 도해 영역 텍스트
오인 2 · **수식 셀 입력 매핑 1(위험 최상)** · 사업부 변형 미확정 1.

## §4 배치 규모 제안 (판정 요청)

1. 부실률 80% → "선별 교정"이 아니라 **전면 검수 전제**가 맞다. 다만 클래스 분리:
   ⓐ 작성형 재설계(대부분) ⓑ **grid 보유 8종 = 경보강 트랙**(L1100-15 실증 — 파괴 금지)
   ⓒ **수식 셀 충돌 = 위험 우선**: 157종 전체 cell_map↔마스터 수식 셀 자동 대사 스캔을
   다음 스텝으로 제안(K2100-05 유형 전수 색출, 0.5일 미만·읽기 전용) ⓓ 사업부 변형
   미확정(조·쇼바·필라 표기) = 판정 게이트 후 착수.
2. 1차 배치 = **상위 20(점수순)** 권고. 공수 추정 2~2.5일(6배치 실측 속도 기준, 판정
   대기 별도). L2100-02/03/06/10 적용성 판정(조관 전용 vs AM 실사용)이 선행 관문.
3. 정직 고지: 화면 캡처 미촬영(E2E 실물 xlsx 갈음 — 코워크 보완 선례 준용).
