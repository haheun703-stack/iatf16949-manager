# 1배치 본체 10종 — 검수 요청 (2026-07-28 오후, 봇 → 코워크)

승인된 순서(수입검사 → 금형 → 지그 → 계측기 → 출하성적서) 그대로 10종 완결.
상세 실측·반전 근거 = 조사서 §0.6 추기. 여기는 검수에 필요한 판정표·증거만 압축한다.

## 1. 채택 판정표 (지시: AM 실사용 기준 확정 + audit 채택 시트 기록 — `adoptedSheet` 전건 기입)

| # | 코드 | 이름(변경 반영) | 채택 소스 | 판정 근거 |
| --- | --- | --- | --- | --- |
| 1 | L2100-01 | 수입검사표준(-AM) ⚠️개명 | **AM 실물** MES 수입검사 엑셀양식 '양식' 시트 추출 | 마스터 -인 = 5행 스텁, -필가 = KS 표준서 문서. AM 실사용이 유일한 작성 양식 |
| 2 | L2100-04 | 초품검사 및 불량폐기내역(-조6) | 마스터 -조6 시트(기존 셀맵 391 보존) | AM 자체 변형 없음 실측 — 조관 전용 정본 유지(참고용 명시) |
| 3 | L2100-05 | 공정 순회검사(패트롤) 시트(-AM) ⚠️개명 | **AM 실물** MES 공정패트롤 엑셀양식 '양식' 시트 추출 | 마스터 -조 일지는 조관공정(SKELP/ERW) 전용 — AM 공정에 없음. 의무 #45 실사용과 일치 |
| 4 | L2100-11 | 조도관리 측정기록 | **AM 실물** 조도측정 일지(표면 거칠기) 무손상 복사 | 구 신규설계본 = 조명 lux 도메인 오인 → "실물 출현 시 교정" 조건 발동 |
| 5 | L1100-25 | 금형 점검 체크시트(일상/정기/보관) | 신규설계본 **Rev.1**(일자-행 전개 '양식' 시트) | 구판 일(日)-열 매트릭스 = grid 엔진 구조 불일치. 구코드 라벨(L1100-20) 정정. 정기·보관·작성요령 시트 보존 |
| 6 | L1200-01 | 치공구 관리대장 | 마스터 시트 | 대장형 grid + 작성일/부서/작성자 |
| 7 | L1200-04 | 지그 이력카드 | 마스터 시트 | 카드형 16필드 + 변동이력 grid(6행) |
| 8 | L1200-12 | 지그·치공구 점검 체크시트 | 신규설계본 **Rev.1**(일자-행 전개) | L1100-25 와 동일 사유. 구코드 라벨(L1200-03) 정정 |
| 9 | L3100-01 | 계측기 관리대장 | 마스터 시트 | grid 전용 17열(A~P + 합부판정 AI + 비고 AK). 게이트 완화 경로 |
| 10 | M3100-05 | 완성품·출하검사 성적서 | 신규설계본(02) | 헤더 6 + 검사항목 grid(시료 5) + 작성자 auto(G23). 예시행 클리어 |

## 2. 검증 결과 (전부 복사본 — 라이브 무손상)

- **E2E(:8080, 마이그 0104 자동 적용)**: 10종 제출→export→다운로드→판독 — **셀검증 96/96**,
  엔진 verify 전건 OK(값 일치·media 보존·merges 보존. AM 추출본 = 병합 38·이미지 9 보존).
- **grid 전용 8종 회귀 스모크: 8/8 통과** (B2100-06~11·J3100-03·L1100-15 — export 무오류·파일
  열림·앵커 행 주입·시트명 매칭. `scripts/smoke-grid-export.cjs` 로 정식화, 재실행 가능).
- **audit 재실행**: 작성가능 167→**177**, 갭A 134→**124**, 갭B **0 유지**, newDesign 16→15
  (L2100-11 이 실물 채택으로 전환). `docs/forms-gap/audit_20260728.json` 갱신분.

## 3. 실물 (검수용 — captures/, 코워크는 마운트 열람)

- 엑셀 출력 실물 10종: `batch1_e2e_<코드>.xlsx`
- 작성화면 캡처 10장 + 엑셀 뷰 3장(L2100-01·L2100-04·L1100-25): `cap_batch1_*.png`
- grid 스모크 실물 8종: `grid_smoke_*.xlsx`

## 4. 판정 요청 3건

1. **개명 2건 승인** — L2100-01 '(-인)'→'(-AM)', L2100-05 '조관공정 순회검사일지(-조)'→
   '공정 순회검사(패트롤) 시트(-AM)'. 근거: 시트 실측(조사서 §0.6 반전①). 기각 시 이름만 원복(데이터 무영향).
2. **M1200-10 교정 여부** — 자주검사 실물 출현(AM MES 3종 세트의 하나 — 파일명 매칭 스캔이 놓침).
   승인 시 L2100-01 동일 추출 경로로 즉시 교체(같은 레이아웃, 반나절 미만).
3. **한계 수용** — AM 추출 템플릿의 도해/사진 = 2M100 시드 이미지 유지(품번별 교체는 수기 영역),
   L2100-04 = 조관 전용 서식의 정본 완결(AM 실무 작성 가능성 낮음 — SQ 증거 갖춤 목적).

⚠️ 라이브 반영 = 앱/서버 다음 재기동 시 0104 자동 적용. **W4 설치판 재빌드 범위 0104+ 로 확대.**
검수 통과 시 다음 = 2배치(설비+내부심사 5종+경영검토+교육). B-1(M1 선행분, 마이그 0105 예정)은
검수 대기 시간에 착수.

---

## 5. 회신 접수 (7/28 오후 — 서면 잠정 합격 · 판정 2건 확정 이행 완료)

**접수**: 서면 잠정 합격 + 개명 2건 승인 + M1200-10 실물 교체 승인(조건 2). "한계 수용"은 보류
(코워크 연결 복구 후 회신) — 대기. 최종 도장 = 문서·캡처·엑셀 실물 표본 확인 후.

**조건 이행 — 마이그 `0105_batch1_followup.sql`:**

1. **개명 이력 = 양식 개정 이력으로 기록** — `form_change_log` 테이블 신설(양식 메타 변경 이력,
   작성본 개정 이력과 별개 축). 개명 2건 + 템플릿 채택/교정 4건 = 6행 기록(구명칭→신명칭·사유·
   적용 마이그). **2배치부터 개명·교정마다 여기 기록**을 표준으로 한다.
2. **M1200-10 실물 교체 완료** — AM MES 자주검사체크시트 '양식' 시트 추출 채택
   (`templates/am_forms/M1200-10_공정자주검사_AM.xlsx`, 병합 38·이미지 9 보존·시드 클리어).

   **ⓐ 차이 요약 3줄 (설계본 → 실물, 잃는 것 명시):**
   - 구 설계본 = **일일 기록형**(검사일자·근무조 헤더 + 항목별 기준규격/측정치/판정(○/×) 12행 +
     검사자·특이사항) / 실물 = **품번별 기준서형**(차종·품번·공정·설비·LOT 헤더 + 검사부위·검사항목
     5행 + 도해/사진 + 결재 3단).
   - **실물에 없어 잃는 필드 = 검사일자·근무조·기준규격·측정치·판정(○/×)·특이사항** — 즉 일자별
     측정·판정 기록칸. AM 실무에서 그 기록은 **MES(QMS_SQC)** 에 있으므로 앱 양식이 대신 들 필요가
     없다는 것이 실물 채택의 실질 근거(중복 기록 방지).
   - 판정·측정 기록이 필요한 자리는 이미 별도 양식이 담당 — 순회 기록=MES, 초품=L2100-04,
     출하 판정=M3100-05. 잃는 것이 아니라 **기록 위치가 정리**된 것.

   **ⓑ 구 설계본 보관** — `sq_gap_forms/15_공정자주검사_CHECK_SHEET_M1200-10.xlsx` 삭제하지 않고
   참고 보관(기록은 지우지 않는다). form_change_log 에 구→신 경로 기록.

**후속 검증(전부 복사본)**: E2E **11종 셀검증 104/104**(M1200-10 케이스 추가 — engine verify OK),
grid 8종 회귀 **8/8**, audit newDesign 15→**14**(M1200-10 실물 채택 전환), 캡처
cap_batch1_M1200-10(+_excel). 커밋 = 0105 + 하네스/audit/gen 확장.

**진행**: 코워크 지시대로 검수 대기 중 **B-1(M1 선행분) 착수** — 마이그 번호는 0105 를 본 후속이
써서 **0106 으로 이동**(내용 = mat_receipt.capture_id·receipt_class 2컬럼, 견적서 §1 그대로).
B-2(태깅 화면)·B-3(트리거)는 **사장님 ④ 수집함 재가 대기** 유지. 2배치는 최종 도장 후.
