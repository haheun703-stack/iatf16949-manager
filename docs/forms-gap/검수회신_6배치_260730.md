# 검수 회신 — 6배치 판정 3건 + 캡처 공백 보완 (2026-07-30, 코워크 → 봇)

전 판정 = 마스터 8권(M-1100·L-3100·M-4100·M-4200·A-7100·A-8101·K-1100·H-3100) 직접 재스캔 +
E2E 실물 표본 2종(L-4101-01 변환본·J1100-14 좌면 추출) 소급 확인 근거. **결과 = 승인 2 · 반전 1.**

## 1. 판정 1 — 열람형 10종 선례 재적용 = **승인** (A7100-01 서약서 하위유형 명시 승인 포함)

10종 전부 마스터 재실측으로 "기정의 본문/이미지/기준표" 확인:

| 코드 | 재실측 | 판정 |
| --- | --- | --- |
| J3100-04 | 기준표 44행(구분×세부×처리구분 기정의) | ✓ |
| M1100-06 | 이미지 75장 절차도, 텍스트 실행 4행 | ✓ |
| L3100-05 | 코드표 29행 기정의 | ✓ |
| L3100-06 | 판정 기준 84행 기정의 | ✓ |
| M4100-05 | 구역도 이미지 9장 | ✓ |
| A7100-01 | **"~하겠습니다" 서약 본문 10개조 실측** — 계약서 열람형(사용자 확정 260729)의 '서약서' 하위유형 부합. 서명은 인쇄·수기 동선 | ✓ **명시 승인** |
| A8101-01 | 674행 책자·이미지 40장 | ✓ |
| K1100-07/08/09 | cols=1 순수 조문 텍스트 3종 | ✓ (사용자 확정 직적용 정합) |

## 2. 판정 2 — H3100-05 코드 중복 처리 = **ⓒ 승인 (지침 개정 시 정리)** + 확장 지시 1

H-3100 마스터 재스캔 결과 중복이 봇 보고보다 넓다: "고객만족도 평가서(실   적) (H3100-05)" 외에
**H3100-03 도 2개 시트**("평가서(설문지)"·"평가 보고서")가 같은 코드를 달고 있고, H3100-01 구자료
시트도 잔존. 이는 개별 오기가 아니라 **H-3100 고객만족도 계열의 코드 체계 전반이 흐트러진 상태** —
ⓐ(신규 등록)는 오기를 정본화할 위험이 있어 기각, ⓒ가 맞다. **확장 지시**: 지침 개정 대상에
"평가서(실적) H3100-05" 단건이 아니라 **H-3100 코드 중복 3건 일괄**(H3100-03 ×2·H3100-05 실적·
구자료 시트 정리)로 기록할 것(규정 원문 15종 트랙과 같은 총무팀 동선에 편승 가능).
기능 영향 없음 확인: resolveSheet 첫 매치 = 종업원 보고서 "1" 시트로 정상(시트 순서 실측).

## 3. 판정 3 — M4200-01 첫 시트 한정 매핑 = **반전 (AM 시트 채택 지시)**

**실측 반전**: M-4200 변형 8개 중 **"AM_조도측정 체크시트(M4200-01)" 시트 실존** — 봇이 매핑한
"정밀인발(인발반)"은 resolveSheet 첫 매치일 뿐, AM사업부 전용 변형이 따로 있다. 앱은 AM사업부
운영이므로 **1배치 "AM 실사용 기준 확정" 선례(L2100 반전①) 직적용 대상**이다.

- 캡처 증빙: `cap_batch6_M4200-01.png` — 현재 앱 작성화면에 인발 공정 필드(화성처리·구부 1/4호기·
  스웨이징)가 표출됨. AM 작업자가 쓸 수 없는 칸이다.
- **지시**: ⑴ AM 시트 추출 채택(B2300 시트명 정정 추출 경로 — AM 시트는 7번째라 첫 매치 불가)
  ⑵ AM 시트 레이아웃 재실측 후 fields·cell_map 재설계(실측: "갑지" 표제 + 공정 LAY-OUT 구조 —
  인발반 시트와 앵커 다름) ⑶ form_change_log 'template_adopt' + audit adoptedSheet 갱신
  ⑷ 타 사업부 7개 시트 = 시트 수기 유지(그 부분은 원판정 유지).
- 후속 배치 체크 항목 추가: **"사업부 변형 시트 존재 시 AM 변형 우선 탐색"**(시트명 자릿수 체크와
  같은 반열 — 이번이 L2100 이후 재발 2번째).

## 4. 캡처 공백 보완 — 코워크 촬영 완료 (§4 한계 해소)

봇의 정직 고지(PNG 미촬영)를 코워크 검증 환경(:8080 + 렌더러 빌드 + 헤드리스 크롬)에서 보완:
**cap_batch6_*.png 18장**(진행 17 작성화면 + A7100-01 열람형 확인) — captures/ 반영.
검증 DB = 마이그 체인 재구성 + 0113 적용(audit 302/244/36 하네스 수치 재현 일치).
※ 0113 적용 시 FK 제약 1건(신규 등록 B2300-08 연계 — seed.ts 미실행 재구성 DB 한정 사안,
사무실 라이브 복사본 적용은 정상이므로 마이그 결함 아님. 기록만 남긴다).

## 5. 종합 — 6배치 = **조건부 합격** (M4200-01 반전 이행 시 최종 도장)

하네스(E2E 106/106·grid 8/8·audit 302/244/36) 재현 확인 + 실물 표본 정합(변환본 수식 225 보존·
좌면 추출 예시기록 0). **양식형 배치 트랙(1~6배치) 완주 인정** — 잔여 갭A 36 = 전량 규정 본문형.
미결 = M4200-01 반전 이행 1건(반나절 미만 추정 — L2100-01 추출 선례 동일 경로).

**누계 기록**: 7/28~30 3일, 1~6배치 + ⓑ4종 = 양식형 전환 완주, 작성가능 167→244, 갭A 134→36(전량
규정 본문형), 열람형 정리 22종, 신규 등록 +1(B2300-08).

---

## §6 최종 도장 (7/30 2차 — 반전 이행 확인 완료)

**M4200-01 반전 이행(0114) 실물 확인** — 코워크 검증 환경에 0114 적용 후 작성화면 재촬영:
AM 공정 필드(너트 스폿·용접 3~ 측정값, 을지-1 57·을지-2 49) 표출 확인, 필드 107·추출 템플릿
`batch6/M4200-01_AM조도측정체크시트.xlsx` 반영 확인. 봇 정직 고지분(반전 후 PNG 미촬영)은
`cap_batch6_M4200-01_AM반전후.png` 로 보완(반전 전 인발 화면 캡처와 전후 쌍 성립).

**6배치 = 최종 도장 완료. 양식형 배치 트랙(1~6배치) 완주 확정** — 미결 0.
누계(7/28~30): 작성가능 167→244 · 갭A 134→36(전량 규정 본문형) · 열람형 22 · 신규 등록 +1.
잔여 트랙 = 규정 본문 적재(원문 대기) · P1 홈 개편(재가 대기, 마이그 0115+) · W4(3결정).
