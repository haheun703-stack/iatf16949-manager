# 2배치 — 검수 요청 (2026-07-28 저녁, 봇 → 코워크 · 조건부 출발분)

**하네스: E2E 8종 셀검증 62/62 · engine verify 전건 OK(값·media·merges) · grid 8종 회귀 8/8 ·
audit 작성가능 177→185(+8) · 갭A 124→116 · 갭B 0 유지.** (조건 ③ 이행 — 이 줄이 표준 첫 줄)

마이그 `0107_batch2_forms.sql` · 신규 템플릿 0(전부 마스터 정본 시트) · 개명/교정 0(form_change_log 추가 없음).
실물 = captures/batch2_e2e_*.xlsx 8종 + cap_batch2_*.png 10장(작성 8 + 엑셀 뷰 2).

## 1. 조건 ② 적용 결과 — 원지시 14종 → 진행 8 · 보류 5 · 제외 상신 1

**진행 8종** (전부 마스터 빈 틀 실측 — 시드 없음):

| # | 코드 | 이름 | 유형 · 처리 |
| --- | --- | --- | --- |
| 1 | A2200-03 | (  )년 경영검토 보고서 | 712행 대작이나 **빈 틀 실측**(시드 탐침 전부 빈칸) — 헤더 4 + 검토항목 grid(3행 병합 14블록, A열 번호 실측) + 검토내용·특기사항 + 작성자 auto |
| 2 | F1100-01 | 년 사내외 교육훈련 계획서 | 대장형 grid 20열(월별 12열) + 작성일자·작성자(라벨 병합 셀 덮어쓰기 — L2100-04 K3 선례) |
| 3 | F2100-10 | 작업자 숙련도 CHECK SHEET | **매트릭스 채점형** — 최소 매핑(대상자 3·특이사항·작성자). 점수·소계 SUM 수식 = 시트 보존(계산기형 선례) |
| 4 | L1100-01 | 설비제작 사양서 | **문서형 대작(226행)** — 식별 헤더 3(품명·차종·목적)만. 상세 본문 = 사람 몫 |
| 5 | L1100-09 | 설비 이력카드 | 카드 2 + 수리/보전 이력 grid 9열(단가 열 = 정본 충실 유지 — M1 "돈 구조화 금지" 경계는 수불 구조화 건이라 무관 판단) |
| 6 | L1100-24 | 설비능력 지수표 | **계산기형(Cmk 자동서식)** — L4102-02(0103 ⑤) 선례 그대로: 비수식 헤더 10만, 측정값·수식 보호 |
| 7 | M1100-02 | 설비 비가동시간 관리 대장 | 3행 병합 블록 grid(stride 3 × 6건, 9열 — 전/후 검사 판정 열 실측 포함) + 작성자 auto |
| 8 | M1100-03 | 설비 비가동시간 이력관리 대장 | 비가동 1건 카드형 11필드(품번·LOT 전후·시간 3종·중단코드) |

**조건 ② 보류 5종 — '한계 수용' 판정에 걸리는 유형(시드 실데이터·사진·타사업부 전용), 판정 후 처리:**

| 코드 | 보류 근거(실측) |
| --- | --- |
| A5100-03 | 과거 심사 실데이터 시드(발행번호 TPC24-0723·긍정평가 서술 등이 마스터에 그대로 — 마스터는 원본이라 시드 클리어 불가, export 출력물에 잔존) |
| A5100-04 | 사업부별 실평점·지적사항 매트릭스(95.5점·지적 서술 등 실데이터) |
| A5200-03 | 실데이터 + **심사 사진 24장** 시드 · 인발사업부 서식 |
| A5200-04 | 실데이터 시드(쌍용산업·Φ63.5·24년 7월) · 인발사업부 서식 |
| A5200-04-01 | **필라넥워터사업부 전용** 성적서(제목 명기) — L2100-04 조관 전용과 같은 유형 |

**제외 상신 1종(실측 반전)**: M1100-05 설비 비가동 주요 코드 = **고정 코드 정의표**(E-102~ 참조
문서, 전 행 기정의)로 작성 양식이 아님. 기록을 주입하면 정본 코드를 덮어씀 → 문서 열람형 전환 제안
(fields 미정의 유지). 판정 요청.

## 2. 검증 상세 (전부 복사본 — 라이브 무손상)

- E2E: 8종 제출→export→다운로드→판독 **62/62** (A2200-03은 항목 grid 2건 + 검토내용까지 판독,
  M1100-02는 stride 3 블록 주입 확인). engine verify 값·media·merges 전건 OK.
- grid 전용 8종 회귀 스모크 **8/8** (재차단 0).
- audit `adoptedSheet` — 진행 8(마스터 시트명) + 보류 5(사유) + 제외 1(제안) 전건 기록.

## 3. 판정 요청 2건

1. **M1100-05 제외(문서 열람형 전환)** 승인 여부 — 실측 근거 §1.
2. 보류 5종 처리 방향 — '한계 수용' 판정 시 함께 회신 요청: 마스터 원본은 시드 클리어가 불가하므로
   ⓐ 한계 수용(시드 잔존 그대로 완결) ⓑ AM용 빈 틀 신규 설계본 제작(0065 문법) ⓒ 계속 보류
   중 택일. 봇 권고 = **ⓑ**(내부심사는 AM 자체 실시 필요 — 타사업부 실데이터가 남는 출력물은
   심사 증거로 부적절).

⚠️ 마이그 0107 = W4 설치판 재빌드 범위 **0107+** 로 확대. 라이브 = 다음 재기동 시 자동 적용.
다음 = 1배치 최종 도장·한계 수용 판정 회신 처리 → 보류 5종 처리 → 3배치(공정·물류·식별 20종).

---

## 4. 판정 회신 접수·이행 (7/28 밤 — 3건 전부 확정, 마이그 0108)

**하네스(이행 후 재검증): E2E 9종 셀검증 66/66 · engine verify 전건 OK · grid 8종 회귀 8/8 ·
audit 작성가능 186 · 갭A 114 · 갭B 0 · viewOnly 신설 [M1100-05].**

- **판정1 '한계 수용' = 조건부 수용 접수** — 시드 사진 일반 품번 수용(M4/AI 판독 트랙 합류).
  예외 = 심사 품번 28236-2MAA0 실사진 교체(10월 전): **심사숙제 4번 행 추가 완료**(폴더 루트,
  품질팀·기한 9/12 — 사진 도착 시 봇이 템플릿 교체). L2100-04 정본 완결 수용 접수.
- **판정2 이행** — ⓑ 승인 4종(A5100-03/04·A5200-03/04)은 별도 착수(조건: A-2 전부 + **문항·평가
  구조 마스터 재사용·기록값만 클리어** — 창작 금지. 9월 초 전). **A5200-04-01 재분류 이행 완료**:
  정본 완결(평가 블록 4 매핑 — 주간 A7·야간 Y7·인발 A45·강관 Y45. 하단 무제 3섹션은 미매핑 명시.
  빈 틀 확인). form_change_log 재분류 기록.
- **판정3 이행 완료** — M1100-05 문서 열람형 전환: fields 미정의 유지 + description 📖 열람형 표시 +
  form_change_log 전환 사유 기록 + **audit 집계 분리**(갭A에서 제외, summary.viewOnly 신설 — 영구
  미완처럼 보이는 왜곡 방지).
- 실물 추가: batch2_e2e_A5200-04-01.xlsx · cap_batch2_A5200-04-01.png · cap_batch2_M1100-05_viewonly.png.
- **순서 재량 결정**: 다음 = **ⓑ 4종 먼저**(실측이 이미 끝나 있어 컨텍스트 유리 + 9월 초 데드라인
  선제 제거, ≈1~1.5일) → 이어서 3배치(공정·물류·식별 20종). 3배치 보고 규칙 = 첫 줄 하네스·정직
  3분류·반전 상신 그대로.

⚠️ 마이그 0108 = W4 설치판 재빌드 범위 **0108+**.
