# 4배치 검수 요청 (2026-07-29) — 우선순위 상위 17종

**하네스: E2E 13종 셀검증 135/135 · engine verify 전건 OK(media·merges 포함) · grid 전용 8종 회귀 8/8 · audit 작성가능 203→216(+13) · 갭A 91→74(−17) · 갭B 0 유지 · 열람형 7→11(+4).**

- 마이그 **0111** + `gen-batch4-templates.mjs`(마스터 추출 3종 + 0065 설계본 정비 2종 — 기록/예시행 클리어 계 382셀·양식번호 정정 2건) + `e2e-batch4.mjs`(리포 하네스 정식화).
- 실물: `captures/batch4_e2e_*.xlsx` 13종 + `cap_batch4_*.png` 14장(작성화면 13 + 열람형 확인 1).
- **검증 = 코워크 클라우드 검증 환경**(마이그 체인 0001~0111 재구성 DB + :8080 웹서버) — **라이브 무접근·무손상**. 재구성 DB의 audit가 7/29 라이브 복사본과 **완전 동치**(301/203/91/열람형 7) 실측 → 검증 유효성 근거(§2-③). ⚠️ W4 설치판 재빌드 범위 **0111+** 로 확대.
- 안전: 17종 전부 form_submissions 0건 실측 — DELETE+INSERT 무손실. 멱등 확인(마이그·gen 재실행 = 동일 결과, 예시행 재클리어 0셀).

## §1 정직 3분류 (원지시 "잔여 갭A 91 우선순위순" → 실모수: 양식형 54 중 상위 17종. 조사서 "4배치+ ~48종" 추정은 실측 54로 정정 — B2300 7종은 응집 클러스터라 5배치 선두 분리)

### ⓐ 진행 13종 — 마스터 직접 8 + 마스터 추출 3 + 0065 설계본 정비 2

| 코드 | 양식 | 방식 | 비고 |
| --- | --- | --- | --- |
| L1200-07 | CF 점검 시트 | 분기점검 grid(본행+특이사항행 stride 2×4 — M1200-06 선례) | 분기 라벨·점검 기준 = 시트 프리셋 |
| L1200-10 | 대여자산 관리대장 | 대장 grid(21열 전열 매핑) | 활용기호 A/B/C = 시트 프리셋 |
| J1102-02 | 관리계획서 특별특성 | 특성 목록 grid(빈 틀 실측 — 작성형 확정) | 상단 승인블록 = 병합 구조 부재로 시트 수기(§4) |
| J1102-03 | 관리계획서 체크리스트 | AIAG 21문항 grid(예/아니오/N.A·코멘트·책임자·기한) + 헤더 4 | 단계 체크(시작품~양산) = 시트 수기 |
| K1200-04-01 | 타사업부 실적,체제 평가 계획서 | 문서형 17필드(평점 9+총평점 — A5200-04-01 평가 블록 유형) + 작성자 auto(결재 작성칸) | 배점·기준·등급표 = 시트 프리셋 |
| H3200-01 | 고객불만 접수 대응 현황 보고서 | 2행 병합 grid(본행 23열 + 상세행 2열, stride 2×18) | 대응시간·PPM·경비합산 수식 8열 = 보존 미매핑(L4102-02 선례) |
| F2100-05-01 | 공정면허 이론평가서 1안 | **최소 매핑 5**(공정명·작업자·평가자·평가일자·점수) | 문항 10·답안 기입·자격구분 □ = 시트 수기(시험지 특성) |
| F2100-05-02 | 공정면허 이론평가서 2안 | **최소 매핑 3**(평가일자·소속·성명) | 정답열(AF) = 채점용 프리셋 유지 |
| B2100-04 | 장기테마 | **추출 템플릿**(헤더+빈 블록 10) + 4행 블록 grid 13열 | 마스터에 19년~ 실기록 — §2-② |
| B2100-05 | 즉실천항목 | **추출 템플릿**(헤더+빈 블록 10) + 4행 블록 grid 14열 | 마스터에 실기록 **4,443행**(살아있는 대장) — §2-② |
| F1100-04 | 지식 관리표 | **추출 템플릿**(헤더+빈 30행) + 대장 grid 11열 | 마스터에 기록 144행. 연번 수식 클리어(§4) |
| B1100-12 | 리크·리워크 이력대장 | 0065 설계본(03_) 정비: 예시행 17셀 클리어·양식번호 B1100-10→12 정정 + 대장 grid 17열 | §2-① 코드충돌 재부여 잔재 |
| B1100-13 | 부적합품 처리대장 | 0065 설계본(10_) 정비: 예시행 클리어·양식번호 B1100-11→13 정정 + 대장 grid 16열 | N열 수량대사 수식 보존 |

### ⓑ 열람형 전환 4종 — M1100-05 판정3 선례 재적용 (이의 시 반전)

| 코드 | 양식 | 실측 근거 |
| --- | --- | --- |
| L1200-08 | 지그계약서 | 검사구·물품납입 계약서 조문 전문(제1~n조·금액·지체상금, 갑을·인감) — 체결은 서면·인감 동선, 기록 주입 시 조문 정본 덮어씀 |
| L1200-09 | 지그 사용대차 계약서 | 사용대차 계약 조문 전문(Ⅰ장 총칙~관리의무) — 동일. 대여 기록은 L1200-10 대장으로 |
| B1100-07 | 품질문제 발생 신속 대응 계통도 | 계통도 이미지 3장 + 신속대응 조직 연락망(실명·연락처) 기정의 |
| B1100-08 | 부적합품 처리 기준_가로 | 전면 이미지 도해(가로/세로 2시트 동일 문서) — 처리 기록은 B1100-13 대장으로 |

전부 form_change_log 'reclassify' 기록(0111) + 📖 표시 + audit viewOnly 분리. **선례 있는 유형이라 판정 대기 없이 이행** — 이 문서로 사후 검수, 이의 시 반전(마이그로 원복 가능). 단 계약서 2종은 선례 중 첫 '법률 문서' 하위유형 — §3-1에서 명시 확인 요청.

### ⓒ 원본 부재·신규 창작 0종 — 17종 전부 원본 확보(마스터 15 + 0065 설계본 2)

## §2 실측 반전 (기록)

1. **[반전] B1100-12/13 "원본 부재 → 신규 설계"가 아니었다** — 착수 전 파일명 검색은 실물 무(無)였으나, **0065 설계본이 이미 존재**(파일명이 구코드 03_B1100-10·10_B1100-11 — 코드충돌 재부여 잔재). 파일명 매칭이 재부여 코드를 못 따라간 사례 = 3배치 반전①(내장 시트)의 변형. 신규 창작 없이 정비(예시행 클리어+양식번호 정정, M1200-09 선례)로 종결.
2. **[반전] "빈 틀" 전제가 3종에서 반대** — B2100-04(19년~ 실기록)·B2100-05(**4,443행** 실기록)·F1100-04(144행 기록). 마스터 직접 주입 시 실기록을 덮어씀 → **헤더+빈 블록 추출본 채택**(1배치 AM 추출·"빈칸이 가짜보다 낫다" 선례 연장). 원본 기록은 마스터에 무손상 보존, 추출본이 작성 정본.
3. **[발견·선례] 검증 환경 재구성 실증** — 마이그 체인 0001~0111 만으로 빈 DB에서 재구성한 검증 DB가 라이브 복사본 audit와 **완전 동치**(301종/작성가능 203/갭A 91/열람형 7, 0111 적용 전 기준). 라이브 접근 없이 배치 검증 가능 — 브리지 단절 시에도 검수 하네스가 성립한다.
4. **[함정 선제 수정] 데이터 영역 세로 병합** — F1100-04 원본 A7:A13(프로세스 그룹 병합)이 남으면 grid 행 주입값이 병합 종속 셀에 가려짐 → 추출 시 데이터 영역(r6+) 병합 해제. 후속 추출형 배치의 체크 항목으로 기록.
5. **[데이터 발견] `L4101-01`(Xbar-R 관리도, 양식형)과 `L-4101-01`(규정 본문형 집계)이 forms 에 별개 등록** — 이중 등록 의심. 5배치 착수 시 실측 확인 예정(이번 배치 무접점이라 수정 보류 — 라이브 데이터 임의 수정 금지 원칙).

## §3 판정 요청 (2건 — 전부 사후 확인형)

1. **열람형 4종 선례 재적용 승인** (§1-ⓑ) — 특히 계약서 2종(L1200-08/09)이 M1100-05 동형("기정의 본문") 판정에 부합하는지. 이의 시 해당 종만 반전.
2. **마스터 실기록 보유 3종의 추출본 채택 승인** (B2100-04/05·F1100-04) — 1배치 AM 추출 선례의 연장 적용이 맞는지. 이의 시 반전(마스터 원본은 무변경이라 template_path 해제만으로 원복).

## §4 한계 (정직 고지 — b4 v1 한계 선례)

- J1102-02 상단 승인 블록(관리계획서 번호·승인/일자 등)·J1102-03 단계 체크 = **시트 수기** — 병합 구조 부재로 안전한 값 셀 특정 불가(라벨 오버플로 지대). J1102-03 부품번호(E8)·개정수준(S8)은 라벨 오버플로 회피 앵커 — 검수 시 실물 확인 요청.
- H3200-01 표제 "년/월"(제목 병합 내) = 시트 수기. 수식 8열(대응시간 L짝수행·PPM S·경비 AG~AM) = 보존 미매핑.
- B2100-04/05 월별 진행상태 서브블록(R~AB)·EXIT CRITERIA 키 = 시트 몫. 추출본 블록 10개 한정 — 초과분은 시트에서 계속(11건째부터).
- F1100-04 연번(B열) = 원본 수식이 기록행 소속이라 클리어 — 수기 기입.
- F2100-05 계열 답안 기입 = 시트 수기(시험지 특성 — 응시 기록·점수만 앱 관리). 정답열 프리셋은 채점용으로 유지(배포 시 노출 주의는 시트 소관).
- 캡처 14장은 클라우드 검증 환경(:8080 헤드리스 크롬) 촬영 — 사무실 실환경 재현은 `e2e-batch4.mjs`·`cap` 동선 그대로 가능(스크립트 리포 정식화).

## §5 잔여 (5배치 후보 — audit_20260729.json 갱신본 기준)

갭A 74 = 규정 본문형 37(별도 트랙·원문 대기) + **양식형 37**. 선두 = **B2300 정성품질 7종**(우선순위 60, 응집 클러스터) → L4101-01 Xbar-R(§2-⑤ 확인 동반) → F1101-03/04 온보딩 → L2300-04 → B2200-05(⚠️ 0065 설계본 11_ 기존재 — fields 정의만으로 종결 가능) → 영업/개발/관리 잔여.
