# 6배치 검수 요청 (2026-07-29 사무실) — 잔여 양식형 25 전량 + 편승 2

**하네스: E2E 17종 셀검증 106/106 · engine verify 전건 OK(media·merges 포함) · grid 전용 8종 회귀 8/8 · audit total 301→302(+1 신규 등록) · 작성가능 227→244(+17) · 갭A 62→36(−26) · 갭B 0 유지 · 열람형 12→22(+10).**

**★잔여 양식형 갭A = 0 — 갭A 잔여 36은 전부 규정 본문형(원문 대기 별도 트랙). 양식 완성전
배치 트랙(1~6배치)의 양식형 대상이 이번 배치로 전량 소진됐다.**

- 마이그 **0113** + `gen-batch6-templates.mjs`(추출 4 + 정비 2) + `e2e-batch6.mjs` +
  audit VIEW_ONLY 10종 확장. 편승 2 = 검수회신_4_5배치 사용자 결정분(①B2300-08 신규 등록
  ②L-4101-01 xlsx 변환 정본 완결).
- 실물: `captures/batch6_e2e_*.xlsx` 17종(셀검증 106/106). ⚠️PNG 작성화면 캡처는 이번 배치
  미촬영(정직 고지 — §4) — 사무실 실기동 시 직접 확인 가능.
- **검증 = 사무실 라이브 복사본**(scratchpad e2e-data6, 서버 기동 시 마이그 체인 0102~0113
  자동 적용) — **라이브 무접근·무손상**. 임시비번 시드는 복사본 한정(seed-local-passwords 선례).
- 안전: 대상 전 종(신규 B2300-08 포함) form_submissions 0건 실측 — DELETE+INSERT 무손실.
  멱등: gen 재실행 = 정정 0건·클리어 0셀. ⚠️ **W4 설치판 재빌드 범위 0113+**.

## §1 정직 3분류 (대상 = 잔여 양식형 25 전량 + 편승 2)

### ⓐ 진행 17종 — 추출 4 + 정비 2 + 마스터 직접 11

| 코드 | 양식 | 방식 | 비고 |
| --- | --- | --- | --- |
| J1100-14 | 고객 접수 도면 검토서 | **좌면 추출** + 문서형 12+auto | ★반전 §2-① — 좌면(빈 틀)/우면(작성 예시) 2면 배치. 4·5면 치수표·도면 = 시트 몫 |
| J3100-08 | 4M 변경 마스터리스트 | 0065 설계본 정비(양식번호 J3100-05→08·예시행 클리어) + 대장 grid 13열 | §2-⑥ 코드충돌 잔재 3번째 |
| A8100-01 | RISK 분석표 | **시트명 정정 추출**("A8100-1_"→A8100-01) + 예시 클리어 + 분석 grid 15열 | 위험지수(J·Q) 수식 보존 미매핑 |
| M4200-01 | 조도측정 체크시트 | 마스터 직접 — 측정값 14공정 문서형(fact) + auto | ⚠️8사업부 변형 중 첫 시트(정밀인발 인발반)만 — §3-3 |
| M4100-01 | 사무용 3정 5S 진단 SHEET | 마스터 직접 — 헤더 3 + 진단 grid(점수·점검결과 25항목) + auto | 소계(T33) 수식 보존. 기존 부실셀맵 37셀 전면 재설계(§2-⑤) |
| M4100-02 | 현장용 3정 5S 진단 SHEET | 동일(앵커 동일 실측) | 동일 |
| M4100-03 | 3정 5S 평가 보고서 | 마스터 직접 — 문서형 17(평가요소 5블록+종합) | |
| M4100-04 | 3정 5S 평가지적 및 시정대책서 | 마스터 직접 — 문서형 13(지적/개선전·대책/개선후) | |
| A1100-01 | 개인별 업무 분장표 | **첫 블록 추출**(1~26행) + 담당 12블록 문서형 | 27행~ 실기록 사례 분리(B2100-04 선례). 제목·인원현황 = 시트 수기 |
| H2100-01 | 일일 출고 계획서(-필라) | 마스터 직접 — 대장 grid 13열 전열(L1200-10 선례) | PART/납품처/일자(라벨 내장 병합) = 시트 수기 |
| H2100-02 | 월 납품량 분석 보고서 | 마스터 직접 — **최소 매핑 2**(목표금액·auto) | 일자별 매트릭스(수식 다수)·표제 (00월) = 시트 몫(H3200-01 선례) |
| H3100-05 | 종업원 만족도 조사결과 보고서 | 마스터 직접 — 문서형 8(시트 "1", "2"·"3"은 수식 자동) | 배포/회수(라벨 내장 셀) = 시트 수기. 값 앵커 = 라벨 오버플로 회피(J1102-03 선례) |
| H3100-06 | 종업원 동기부여 계획서 | 마스터 직접 — grid 3열 + 작성일자 + auto | 비고열(G) = 4행 병합이라 미매핑(병합 비앵커 함정 회피) |
| H3100-07 | 종업원 동기부여 실적평가서 | 동일(추진결과/조치계획 열) | 동일 |
| K1100-01 | 원자재 입고 요청서 | 마스터 직접 — 발주 헤더 7 | 자재 카탈로그·발주량·중량/금액 수식 = 시트 몫(맹목 grid 회피). 당사 정보 = 프리셋 |
| B2300-08 | 정성품질 순회 점검 시트 | ★**forms 신규 등록**(사용자 결정 ⓐ) + 시트명 정정 추출 + 결과 grid 4열 | 점검 항목 16 프리셋. 점검일/점검자(라벨 내장 병합) = 시트 수기 |
| L-4101-01 | SPC 평가표 | ★**LibreOffice 변환본 정본 완결**(사용자 결정) — 헤더 16 문서형(계산기형 L4102-02 선례) | .xls 원본 무변경. 기록 클리어 186셀·통계/판정 수식 전 보존. §2-④ |

### ⓑ 열람형 전환 10종 — M1100-05 선례 재적용 (이의 시 반전)

| 코드 | 양식 | 실측 근거 |
| --- | --- | --- |
| J3100-04 | 4M변경 판단기준 | 구분×세부내용×처리구분(●) 전 칸 기정의 기준표(44행) — 기록은 J3100-01/08로 |
| M1100-06 | 우발사고 및 이상발생 처리 절차서 | 이미지 75장 절차도 문서(텍스트 4행뿐) — M2100-03-01 동형 |
| L3100-05 | 계측기 관리번호 부여 기준 | 사업부/계측기 CODE 전 칸 기정의 코드표 — K2100-07 동형 |
| L3100-06 | 검교정 합부판정 기준 | 계측기명×공차×측정불확도 판정 기준 전 칸 기정의(84행) |
| M4100-05 | 3정5S 관리 구역도 | 사업부별 구역도 이미지 6면(9장) — B1100-08 동형 |
| A7100-01 | 윤리경영행동지침(서약서) | 지침 본문 10개조 기정의 + 서명 동선 — **계약서 열람형의 첫 '서약서' 하위유형(§3-1 명시 확인 요청)** |
| A8101-01 | 비상사태 발생시 대응 방안 | 674행 책자형 문서(목차·연락체계·상황별 방안, 이미지 40장) |
| K1100-07 | 기본공급 계약서 | 조문 전문(cols=1 순수 텍스트) — **계약서 열람형 사용자 확정(검수회신_4_5배치 §1) 직적용** |
| K1100-08 | 클레임 보상협정서 | 동형 |
| K1100-09 | 품질 보증 협정서 | 동형 |

전부 form_change_log 'reclassify'(0113) + 📖 표시 + audit viewOnly 분리(12→22).

### ⓒ 원본 부재·신규 창작 0종 — 27종 전부 원본 확보(마스터 21 + 0065 설계본 1 + 추출 4 + 변환 1)

## §2 실측 반전·발견 (기록)

1. **[반전·신유형] J1100-14 = 좌면/우면 2면 배치** — 시트(96열)가 좌면(A~AV)=빈 양식 틀,
   우면(AZ~CU)=작성 예시 기록(CST228·MAGNA)의 나란히 배치. 좌면만 추출(A~AV×123행)하면
   예시 기록·도면 이미지 17장이 자연 분리 — 클리어 없이 빈 틀 확보(새 유형 선례).
2. **[발견] A8100-01 시트명 한 자리 코드 + 후행 밑줄**("RISK 분석표(A8100-1)_") — 5배치
   체크 항목("시트명 코드 자릿수") 적중, B2300 정정 추출 선례 재적용.
3. **[발견] H-3100 마스터에 "고객만족도 평가서(실   적) (H3100-05)" 코드 중복** — 종업원
   만족도 보고서(H3100-05)와 같은 코드의 별개 양식(고객만족도 평가서 계열 — H3100-03 오기
   추정). B2300-3 유형 재발. 수정 보류(마스터 무변경 원칙) — 처리 방향 판정 요청(§3-2).
4. **[실측·경로 확립] Excel COM 저장 전면 불가** — .xls→.xlsx 변환에 Excel COM(SaveAs)이
   대상 경로 불문 "문서가 저장되지 않았습니다" 반복(열기 정상·저장만 불가 — 라이선스/정책
   의심). **LibreOffice headless 변환으로 대체 성공** — .xls 구형 포맷 처리 표준 경로 후보.
   변환본 차트/개체 표현 차이 가능성은 한계 명기(§4).
5. **[반전] 기존 cell_map 5종 = 부실셀맵(자동 추출 잔재)** — M4200-01(57셀)·M4100-01~04
   (37·37·12·12셀)의 기존 cell_map이 라벨 "0"·"600"·중복 A5 행 등 설계 아닌 스크랩 잔재
   실측 → 전면 재설계(DELETE+INSERT). 7/14 메모 "부실셀맵5" 해소.
6. **[재적중] J3100-08 = 0065 설계본 파일명 구코드**(09_..._J3100-05) — B1100-12/13 코드충돌
   재부여 잔재 선례 3번째. 파일명 매칭이 재부여 코드를 못 따라가는 유형 반복 확인.

## §3 판정 요청 (3건 — 전부 사후 확인형)

1. **열람형 10종 선례 재적용 승인** (§1-ⓑ) — 특히 **A7100-01 서약서**가 계약서 열람형
   (사용자 확정 260729)의 '서약서' 하위유형으로 부합하는지 명시 확인. 이의 시 해당 종만 반전.
2. **H3100-05 코드 중복 별개 양식("고객만족도 평가서(실적)") 처리 방향** — ⓐ 실측 후 재부여
   등록(7배치급 편승) ⓑ 방치 ⓒ H-3100 지침 개정 시 정리. 봇 권고 = ⓒ(고객만족도 평가서
   H3100-03 계열 오기 추정이라 등록보다 코드 정정이 맞는 결 — 지침 개정 동선).
3. **M4200-01 첫 시트(정밀인발 인발반) 한정 매핑 승인** — 8사업부 변형 시트 중 resolveSheet
   첫 매치만 앱 작성, 나머지 7개 사업부 시트는 시트 수기 유지. 대안(사업부별 form 분리 등록)은
   과대설계 판단 — 이의 시 재설계.

## §4 한계 (정직 고지)

- **PNG 작성화면 캡처 미촬영** — 검증 웹(:8080)이 dev 전용 캡처 헬퍼(window.__cap) 없는
  빌드라 CDP 결정론 네비 불가. E2E 실물 xlsx 17종 + 셀검증 106/106 + engine verify(media·
  merges)로 갈음. 작성화면은 사무실 앱 실기동으로 확인 가능(검수 시 직접 열람 요청).
- M4200-01: 정밀인발(인발반) 시트 외 7개 사업부 변형 시트 = 시트 수기(§3-3). 조도 기준
  (최저/표준/최대)·측정위치 번호 = 시트 프리셋.
- M4100-01/02: 미입력 진단 항목은 프리셋 점수 0 잔존(마스터 직접 경로 공통 한계 — L4101-01
  선례). 배점 20 프리셋은 시트 몫.
- H2100-01: PART/납품처/일자 = 라벨 내장 병합이라 시트 수기. H2100-02: 매트릭스·표제 = 시트 몫.
- H3100-05: 설문 배포/회수 매수 = 라벨 내장 셀이라 시트 수기. 시트 "2"·"3" = 수식 연동 자동.
- K1100-01: 자재 목록은 프리셋 카탈로그(강종/재종/규격/단가) — 발주량 기입은 시트 몫(행 선택이
  카탈로그 의존이라 맹목 grid 매핑은 부정직 판단).
- B2300-08: 점검일/점검자 = 라벨 내장 병합이라 시트 수기. 점검 라인명(TSW01~03)은 쇼바용접
  기정의 — 타 라인 점검 시 시트에서 라인명 수정.
- L-4101-01: LibreOffice 변환본 — 관리도 차트 등 개체 표현이 Excel 원본과 다를 수 있음
  (.xls 원본은 규정 폴더에 무변경 보존). 측정 데이터 매트릭스·통계치/판정 = 시트 몫.
- J1100-14: 4·5면 치수 검토표·도면 첨부·번역 대조는 시트 몫.
- A1100-01: 제목("00000 업무 분장표")·작성자/작성일(라벨 내장 병합)·직급별/팀별 인원현황 표 =
  시트 수기.

## §5 잔여 (배치 트랙 이후)

**갭A 36 = 전량 규정 본문형(원문 대기 — 사장님 액션 15종 + 환경/안전 계열)** — 양식형 배치
트랙(1~6배치) 종결. 후속 = ①규정 본문 적재(원문 도착 시) ②판정 회신 3건(§3) ③W4 설치판
재빌드(0113+)·사내망 오픈 게이트(사장님 3결정).
