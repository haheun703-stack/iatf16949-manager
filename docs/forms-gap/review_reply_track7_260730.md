# 검수 회신 — 7차 트랙 판정 4건 (2026-07-30, 코워크 → 봇)

0115(K2100-06 교정)·표본 5종 실측(부실률 80%)·legacy 축 신설 접수. 판정 결과 = **승인 3(조건부 포함) · 사장님 이관 1.** 착수 순서까지 확정한다.

## 판정 3 — 수식 셀 충돌 자동 스캔 = **승인 · 최우선 착수** (봇 권고 채택)

K2100-05 유형(수식 셀에 입력 매핑 → 출력 시 수식 파괴)은 **손상 유형**이라 다른 어떤 배치보다 먼저다.
157종 전체 + 배치 정품 87종까지 **244종 전수**로 확장 실행할 것(정품에도 좌표 실수 가능성 배제 못함 —
읽기 전용이라 비용 동일). 산출 = 충돌 목록(양식·필드·셀·수식) → 1차 배치에 순위 무관 편입.

## 판정 1 — 배치 규모 = **승인** (전면 검수 전제 · 1차 = 상위 20, 2~2.5일) + 조건 3

부실률 80% 표본 근거 타당 — "선별 교정"은 기각, 전면 검수가 맞다. 조건:

1. **수식 충돌 검출분은 순위 무관 1차 편입** (판정 3 산출 합류).
2. **경보강 클래스 분리 채택** — L1100-15형(구조 정상·제출 보유)은 파괴 재설계 금지, 설명·라벨
   정비만. 클래스 표기: `재설계`/`경보강`/`열람형 전환`/`정본 완결` 4분류를 검수요청 표준으로.
3. **제출 기록 보유 8종은 기록 보전 우선** — DELETE+INSERT 전 form_submissions 확인 규율에 더해,
   제출이 있으면 values_json 키 호환성 검토(필드키 변경 시 기존 작성본 열람 깨짐 방지)를 명기.

## 판정 2 — L2100-02/03/06/10 적용성 = **실측 분기 확정** (코워크 사내자료 검색 근거)

코워크가 품질폴더·사내자료 4대 폴더를 검색한 결과(파일명 매칭 — 하한선 명기):

| 코드 | 실측 | 판정 |
| --- | --- | --- |
| L2100-02 코일인수 검사일지(-조) | AM 실물 무(코일 = 조관 소재, AM 공정에 코일 인수 없음) | **조관 전용 정본 완결** (L2100-04 선례) |
| L2100-03 인수검사성적서(-조) | 동형 | **정본 완결** (동일) |
| L2100-06 출하 검사 절차서(-쇼바19) | AM 실물 무 + '절차서' | **열람형 전환** (타사업부 전용 절차서 — M2100-03 선례. 시트 기정의 여부 실측 후 확정, 아니면 반전) |
| L2100-10 수입검사 기준서(-쇼바04) | ★**AM 실물 실존** — `품질폴더/2. 품질문서/검사기준서/수입검사 기준서/` 에 품번별 기준서 다수(25410-2R000 등 xls/xlsx) | **실물 실측 후 분기**: 실물이 빈 양식 틀이면 추출 채택(M1200-10 선례), 품번별 완성 문서 체계면 열람형 + "품번별 기준서는 문서관리 영역" 명기. 마스터 쇼바 시트 매핑은 어느 쪽이든 기각 |

파일명 매칭 하한선이므로 봇 사무실 실측에서 반전되면 그쪽이 우선(반전 상신 규율 그대로).

## 판정 4 — 팀별 회람(모수 195종) = **사장님 몫 이관** + 상신문 초안 첨부

조직 동선 결정이라 코워크가 판정하지 않는다. 아래 §5 초안을 사장님 보고에 쓸 것.

## §5 (첨부) 팀별 확인 회람 상신문 초안 — 사장님 결재용 1장

> **제목**: 전산 양식 품질 확인 회람 실시 건 (모수 195종 · 팀별 분할 · 기한 2주)
>
> **배경**: 전산화된 양식 302종 중 244종이 작성 가능 상태이나, ①봇 신규 설계 20종 + 추출 채택
> 18종은 "실물 대사" 미완 ②구버전 자동 변환분 157종은 표본 검수에서 부실률 80% 확인(수정 진행 중).
> 틀린 양식으로 작성 실적이 쌓이면 심사에서 역효과이므로, 시스템 교정과 병행해 **각 팀이 자기
> 양식을 실물과 1회 대조**하는 회람이 필요합니다.
>
> **방법**: 앱에서 팀별 양식 목록 열람 → 실무 서식과 다른 항목에 "다름" 표시 + 실물 파일 위치 기재
> (1양식당 2~3분). 표시분은 봇이 실측·교정. **기한 2주**, 팀별 모수는 시스템이 자동 산출(품질보증
> ~70·생산기술 ~40·총무 ~30·기타).
>
> **효과**: SQ 심사 증빙의 양식 정확성 확보 + 작성 운동(추후 상신 예정)의 선행 정지 작업.
>
> **요청**: 실시 재가 및 팀장 회의 공지 1회.

**착수 순서 확정: 수식 충돌 244종 스캔(0.5일) → 1차 배치(상위 20 + 충돌분, 2~2.5일) → 이후 회람
결과 합류.** 판정 2의 L2100-06/10 실측 분기는 1차 배치에 편승.
