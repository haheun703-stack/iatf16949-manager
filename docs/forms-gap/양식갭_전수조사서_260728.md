# 양식 완성전 — 갭 전수 조사서 (2026-07-28, 스트림 A 1단계)

> 사장님 지시: "프로세스·지침 산하 각 항목별 양식을 만들어 넣고 엑셀본으로 뽑을 수 있게 — 비어 있는 게 많다. SQ도 동일."
> 실측 도구 = `scripts/forms-gap-audit.cjs`(읽기 전용·재실행 가능, 진척 추적에 재사용) · 전체 행 = `docs/forms-gap/audit_20260728.json`
> 기준 DB = 7/28 복사본(0102 적용, 라이브 동등). **이 조사서는 착수 전 검수용 — 코워크 판정 후 배치 제작 진행.**

## 1. 실측 요약 — 갭의 실체는 3층이다

| 층 | 정의 | 실측 | 의미 |
| --- | --- | --- | --- |
| 전체 양식 | forms(비 deprecated) | **301종** | — |
| 작성 가능 | form_fields 정의 보유 | **167종** | 입력·저장 가능 |
| **갭A** | **셀맵(form_fields) 미정의** | **134종** | 작성 자체가 불가 — 본선 대상 |
| **갭B** | fields 有 · **엑셀 좌표(form_cell_map/grid) 無** | **5종** | 입력은 되나 엑셀 출력 매핑 없음 — 빠른 승리 |
| **갭C** | 원본(정본 파일) 실존 미확인 | 양식형 98 중 **실존 9 / 미발견 89** | 셀맵 정의 전 원본 확보 or 신규 설계 필요 |

**갭A 134의 세분(중요)**: **규정 본문형 36 + 양식형 98.**
규정 본문형(코드 `X-####`, 예: M-1200 공정관리 규정)은 0053 처리원칙("빠진 규정을 forms 로 등록해
목록 추가")의 산물로 **셀맵 대상이 아니다** — 이 36종 중 본문(regulation_sections) 보유 21, **미보유 15는
'규정 본문 적재' 트랙**으로 분리한다(양식 제작 아님). **실제 셀맵 제작 모수 = 양식형 98종.**

갭B 5종(전부 원본 실존·fields 완비 — 좌표만 붙이면 끝):
`K1200-07` 외주 ISIR·검사협정 접수대장 · `L2100-07` 수입검사 관리대장 · `L4102-02` 양산단계 공정능력 산출 ·
`M1200-10` 공정 자주검사 CHECK SHEET · `M1200-11` 브레이징 조건관리 CHECK SHEET

갭C 주의(판독 한계 명시): 실존 스캔은 4대 자료 폴더(전체 자료모음·갭양식 신규제작·팀별 구분 모음·서규하 SQ)
445파일에서 **파일명 코드 매칭** — 파일명에 코드가 없는 원본은 못 잡는 하한선이다. 그래도 89종 대부분은
목록표 기반 등록만 된 "빈 껍데기"로 판단(문서 BOM 파일없음 39와 같은 결). **경로 = 0065 갭양식 13종 선례:
원본이 없으면 수집을 기다리지 않고 표준 대장형으로 신규 설계(사장님·팀 검수로 확정)한다.**

참고 실측: 의무 연결 32종 · 프로세스 연결 283종 · SQ 연계 237종 / SQ 체크포인트 161 중 미비 116·부분 44 /
문서 BOM 파일없음 39·갱신 9·미등록 2.

## 2. 우선순위안 — SQ 심사(10/15, D-79) 역산

정렬 점수 = SQ 배점×2 + 미비 체크포인트×3 + 핵심 의무(내부심사·경영검토·교정·교육) 가중 + 의무 연결 + 프로세스.
팀별 분포(양식형 98): 품질보증 35 · 생산 23 · 생산기술 11 · 총무 9 · 구매 9 · 개발 3 · 영업 6 · 관리 2.

### 1배치 — SQ 최우선 리스크 직결 (15종, 갭B 포함)

| 코드 | 양식 | 팀 | 근거(SQ 배점·미비CP) |
| --- | --- | --- | --- |
| L2100-01/04/05/11 | 수입검사표준·초품검사·순회검사일지·조도관리 | 품질보증 | 2_x 계열 170~180점·미비 9~12 — 최우선 리스크 "CP/CPK·수입검사"와 일치 |
| L1100-25 | 금형 점검 체크시트 | 생산기술 | **우선순위 1위(564점)** — SQ 240점·미비 18·의무 #73 연결(0098) |
| L1200-01/04/12 | 치공구 관리대장·지그 이력카드·지그치공구 점검 | 생산기술 | 4_x 금형영역 140점·미비 12 |
| L3100-01 | 계측기 관리대장 | 품질보증 | 교정 계열(0102 연계)·7.1.5.2 |
| 갭B 5종 | (위 표) | — | **원본·fields 완비, 좌표만 — 첫날 반나절 승리** |
| M3100-05 | 완성품·출하검사 성적서 | 품질보증 | 출하검사 = MES 부재 영역(수기 관리) 증거 |

### 2배치 — 설비·핵심 의무 (15종)

설비 3(L1100-01/09/24) · 설비 비가동 3(M1100-02/03/05) · **내부심사 5**(A5100-03/04·A5200-03/04·04-01) ·
경영검토(A2200-03) · 교육 2(F1100-01·F2100-10) — IATF 대시보드 핵심 의무 4축과 직결.

### 3배치 — 공정·물류·식별 (20종)

M1200 공정관리 8종(작업지시서·열처리일보·인수인계·낙하품·셋업품…) · M2100 식별추적 5종(LOT 기준서·선입선출…
— 반-MES lot_registry 와 접점) · M3100-01/06 · K2100-01/07/10(장기재고) · K1200-06(협력사 모니터링).

### 4배치+ — 잔여 (~48종)

B2300 정성품질 7 · B1100/B2100 부적합 계열 · F 교육평가 잔여 · 영업/개발/관리 잔여. 상세 순번 = audit JSON.

## 3. 공수 견적 (과거 실적 근거)

| 유형 | 근거 실적 | 단가 | 모수 | 소계 |
| --- | --- | --- | --- | --- |
| 갭B 좌표 보완 | P9 셀맵 크래프팅 | ~30분/종 | 5 | 0.5일 |
| 대장·체크시트형(원본無=신규 설계) | 0065 갭양식 13종/1일 | 20~40분/종 | ~70 | 4~6일 |
| 복합 시트형(성적서·계획서류) | P5~P7 정답따라쓰기 셀맵 | 1~2시간/종 | ~28 | 4~6일 |
| 규정 본문 적재 15종 | 0056 규정본문 57종 선례 | 원문 확보 시 일괄 | 15 | 원문 의존 |

**합계: 봇 작업 약 9~12일 분량 = 배치 8~10회(배치당 10~15종 + 코워크 검수 게이트).**
1~2배치(30종)만 끝나도 SQ 배점 연동 갭의 최상위가 커버된다. 신규 설계분은 배치 검수 때 실물(현장에서
실제 쓰는 서식)과 대사 — 사장님·담당 팀 확인 1회를 배치 검수에 편승시킨다.

## 4. 원칙 (지시 반영 확인)

- **엑셀 출력 = 기존 셀맵→xlsx 파이프라인 재사용.** 신규 양식도 form_fields+form_cell_map 정의 =
  곧 엑셀 출력이 되게 하고 **별도 엔진을 만들지 않는다.** 갭B 5종이 이 원칙의 첫 검증대.
- 예시값 조작 차단(field_class)·작성자 auto(0097)·완료저장 게이트 등 기존 계약을 신규 양식에도 동일 적용.
- 진척 추적 = `forms-gap-audit.cjs` 재실행(갭A 건수 감소가 곧 진척률) — 완주 게이트 방식 그대로.

## 5. (종결) 질의 3건 — 7/28 밤 코워크 판정 (조사서 합격)

1. **배치 구성 = 승인.** 시퀀스 지정 1건 반영: **1배치 첫 반나절 = 갭B 5종 선두 필수** — 특히
   `L2100-07` 수입검사 관리대장은 스트림 B-3 트리거 소멸 감지("당일 수입검사 기록 감지")가 참조하는
   양식이라, 갭B가 먼저 끝나면 B-3 E2E 가 실양식으로 검증된다(A·B 맞물림 지점).
2. **원본 미발견 89종 = 신규 설계 우선 승인** (0065 선례). 조건 2개:
   ⑴ 신규 설계본에 **문서번호·개정번호·제정일 블록**을 IATF 문서관리 규정대로 박고, audit JSON 에
   **"신규 설계본" 표시**를 남겨 심사 때 대체 이력을 설명 가능하게 할 것.
   ⑵ 배치 검수 때 실물 대사 + 담당 팀 확인 1회 편승 — 확인에서 실물이 나타나면 **신규 설계본을 실물
   기준으로 교정**(버리는 게 아님).
3. **규정 본문 미보유 15종 = 별도 트랙 확인.** 원문 확보 = 사람 액션(사장님 액션 리스트 ③ — 김권표
   이사 자료 or 각 팀 보관본).

**전략 메모(코워크, 배치 반영 불요·인지용)**: 양식 제작은 "작성 가능"을 여는 것이고 SQ 증빙 충족은
작성 실적(현재 4/265)이 만든다 — **1~2배치 완료 시점에 "팀별 작성 운동" 착수를 사장님께 상신**할 것.
그 시점이 SQ 준비도 1%가 실제로 오르기 시작하는 지점이다.

**▶ 착수 게이트: 사장님 "배치안 확정" 도착 즉시 1배치 착수(갭B 5종 선두).**

## §0.5 추기 (7/28 밤 — 1배치 선두 갭B 5종 완결 + 실측 반전 2건)

**갭B 5종 완결(마이그 0103 + 엔진 확장 294cf4f, E2E 셀검증 22/22, 재감사 gapB=0).** 진행 중 발견:

1. **[반전] "원본·fields 완비, 좌표만" 전제가 깨졌다** — 마스터 규정집에 시트가 실존하는 건
   L4102-02 하나뿐(그마저 X-bar R 계산기형 — 비수식 헤더만 매핑, Cpk·작성자는 수식 보호 의도적
   미매핑). L2100-07 정본 = 0065 리포 템플릿, **K1200-07·M1200-10·M1200-11 은 원본이 어디에도
   없어 신규 설계본 3종 제작**(A-2 승인 범위, Rev.0·제정일 블록, gen-gap-b-templates.mjs 재현 가능).
   §1 갭B 정의 행의 "전부 원본 실존"은 파일명 매칭 오탐이었음 — 정정.
2. **[엔진] template_path 폴백 + grid 게이트 완화** — 신규 설계본의 export·렌더모델 성립 조건이라
   1배치 전체(신규 설계 89종)의 공통 선행. resolveMastersDir 3단 폴백과 같은 결의 소스 해소 한 단계로
   판단(새 엔진 아님 — "별도 엔진 금지" 취지 내). 첫 시트 폴백은 템플릿 소스만(마스터 오주입 방지).
   부수 발견: grid 전용 양식(L1100-15·B2100류 8종)이 기존 cell_map 게이트에 걸려 export 불가였던
   상태 — 게이트 완화로 함께 해소(추가 검증은 해당 양식 차례에).
3. audit 에 `newDesign` 표시 신설(A-2 조건⑴) — 현재 16종(0065분 13 + 신규 3).

**§0.5 추기 2 (7/28 밤 — 코워크 반전 2건 승인 + 조건 이행)**: 엔진 확장 = diff 검수 승인(기존 167종
회귀 영향 0 판정). 조건 = **grid 전용 8종 일괄 스모크 → 8/8 통과**(J3100-03·B2100-06~11·L1100-15,
export 무오류·파일 열림·앵커 행 주입 정상 — 시트명까지 마스터 실제 시트에 정확 매칭. 예: L1100-15
'업무 일지' r29, B2100-11 r13). **재차단 대상 0.** 셀 단위 정밀 검증은 해당 배치 차례에.
L2100 사업부별 변형 시트는 AM사업부 실사용 기준 실측 확정 + audit 에 채택 시트명 기록(코워크 지시).

---

## §0.6 추기 (7/28 오후 사무실 — **1배치 본체 10종 완결** + 실측 반전 3건)

**1배치 본체 10종 완결**: L2100-01·04·05·11 → L1100-25 → L1200-01·04·12 → L3100-01 → M3100-05.
마이그 `0104_batch1_forms.sql`(fields·grid_spec/columns·cell_map·template_path·설명 정리) +
템플릿 스크립트 `gen-batch1-templates.mjs`. 검증 = 복사본 E2E(:8080) **10종 제출→export→다운로드→
판독 셀검증 96/96**, 엔진 verify 전건 OK(값·media·merges), **grid 전용 8종 회귀 스모크 8/8**
(`smoke-grid-export.cjs` 로 정식화 — 재실행 가능). 실물 = captures/batch1_e2e_*.xlsx 10종 +
cap_batch1_*.png 13장(작성화면 10 + 엑셀 뷰 3). audit: 작성가능 167→**177**, 갭A 134→**124**, 갭B 0 유지.

**실측 반전 3건 (코워크 지시 "AM 실사용 기준 확정"의 실행 결과 — audit `adoptedSheet` 필드에 전건 기록)**:

1. **[반전] L2100 변형 시트에 AM사업부 것이 없다** — 마스터 L-2100(73시트)의 변형은 쇼바용접·
   정밀강관(-조)·정밀인발튜브(-인)·필라네크(-필가/-필표)·수출포장(-수) 5계열뿐. 실측:
   `수입검사표준 (L2100-01-인)` = **5행 스텁**(롤렛 규격 조각, 작성 양식 아님), `-필가` = KS 허용차
   표준서 문서. **AM 실사용 = MES 엑셀양식 3종**(수입검사·공정패트롤·자주검사, '양식' 시트 공통
   레이아웃 — 8. 사내자료/12. 25450-07870 세트 240313 실측). → L2100-01·05 는 AM 실물에서 '양식'
   시트만 추출해 채택(`templates/am_forms/`, 병합 38·이미지 9 보존 검증). 시드값(2M100 실데이터)은
   매핑 셀만 클리어("빈칸이 가짜보다 낫다"). **이름 정정**: '수입검사표준 (-인)'→'수입검사표준(-AM)',
   '조관공정 순회검사일지(-조)'→'공정 순회검사(패트롤) 시트(-AM)'. L2100-04 는 AM 자체 변형 없음
   실측 → 마스터 -조6 정본 유지(조관공정 전용 명시).
2. **[반전] L2100-11 조도 = 표면 거칠기(도메인 오인 교정)** — 구 신규설계본(12_조도관리)은 작업장
   **조명 조도(lux)** 로 설계했으나, AM 실물 `조도측정 일지.xlsx`(품질폴더/5. 수입검수)는 **표면
   거칠기 조도**(조도팁[스타일러스]·도면기준·실측값#1~3·측정실). A-2 조건 "실물 출현 시 교정" 정식
   발동 → 실물 무손상 복사 채택, 구 설계본 참조 해제(파일은 이력 보존).
3. **[상신] M1200-10 공정 자주검사 — 실물 출현** — 갭B에서 "원본 부재 → 신규 설계"로 처리했으나
   `자주검사체크시트 엑셀양식_개발팀_240313-07870`(AM MES 3종 세트의 하나) 실물 확인. 파일명이
   "공정자주검사"와 달라 파일명 매칭 스캔(하한선)이 놓친 것. **교정 여부 코워크 판정 대기** —
   승인 시 L2100-01 과 동일 추출 경로로 즉시 교체 가능(같은 '양식' 레이아웃, 반나절 미만).

부수: ⑴ 05_금형(L1100-25)·04_지그(L1200-12) 신규설계본 = 일(日)-열 매트릭스가 grid 엔진(행 반복)과
구조 불일치 → **Rev.1 일자-행 전개** 재설계 + 0065 구코드 라벨(L1100-20/L1200-03 '(제안)') 정정.
⑵ 02_출하성적서 예시행 클리어(0065 01 대장 선례). ⑶ 10종 전부에 남아 있던 "필드 미정의" 낡은
설명 교체. ⑷ E2E·스모크 하네스를 리포 스크립트로 정식화(`e2e-batch1.mjs`·`smoke-grid-export.cjs`).

**한계(정직 고지)**: AM 추출 템플릿의 도해/제품·지그 사진은 2M100 시드 이미지 그대로(품번별 사진
교체는 앱 밖 수기 영역 — 차기 과제). L2100-04 는 조관공정 전용 서식이라 AM 실무 작성 가능성 낮음
(SQ 증거 갖춤 목적의 정본 완결). ⚠️ 마이그 0104 = W4 설치판 재빌드 범위 0104+ 로 확대.

---

## §0.7 추기 (7/29 — **3배치 완결: 진행 13·열람형 6** + 실측 반전 3건)

**3배치(공정·물류·식별) 완결(마이그 0110 + gen-batch3-templates.mjs + e2e-batch3.mjs).**
E2E 13종 셀검증 **132/132** · grid 8종 회귀 8/8 · audit 작성가능 190→**203**·갭A 110→**91**·
열람형 1→**7**. 검수요청 = `3배치_검수요청_260729.md`. 실측 반전:

1. **[반전] 마스터 시트 15종 전부 실존** — 갭B "원본 부재" 반전과 반대 방향. 신규 템플릿 0.
   §1 갭C(실존 9/미발견 89)의 파일명 매칭 하한선이 규정집 **내장 시트**를 못 세던 것 — 3배치처럼
   마스터 워크북 직접 스캔이 정답(후속 배치도 동일 접근).
2. **[반전] 전용 서식 2종(M1200-01/02)** — -인/-필/-강 변형만 존재·AM 실물 부재(AM 작업지시=MES
   소관, AM 열처리 공정 없음). A5200-04-01 유형 정본 완결(빈 틀 신설 없음).
3. **[반전·정정] 직접 필드 라벨 일치 규칙** — E2E 가 K1200-06 unmapped 를 잡아 즉시 정정.
   비grid 필드는 form_fields.label ↔ form_cell_map.label **문자열 일치**가 매핑 조건(예시 괄호
   덧붙이기 금지 — 안내는 description/section 으로).

열람형 전환 6종(M1200-05/07·M2100-02·M2100-03-01/02·K2100-07) = M1100-05 판정3 선례 재적용
이행(form_change_log 6행·📖 표시·audit viewOnly 분리) — 검수요청 §3 사후 확인, 이의 시 반전.

---

## §0.8 추기 (7/29 — **4배치 완결: 진행 13·열람형 4** + 실측 반전·발견 5건)

**4배치(우선순위 상위 17종) 완결(마이그 0111 + gen-batch4-templates.mjs + e2e-batch4.mjs).**
E2E 13종 셀검증 **135/135** · grid 8종 회귀 8/8 · audit 작성가능 203→**216**·갭A 91→**74**·
열람형 7→**11**. 검수요청 = `4배치_검수요청_260729.md`. 검증 = 코워크 클라우드 재구성 DB(라이브 무접근).
모수 정정: "4배치+ ~48종" 추정 → 실측 **양식형 54**(잔여 갭A 91 − 규정 본문형 37). 실측 반전:

1. **[반전] B1100-12/13 = 0065 설계본 기존재** — "원본 부재" 아님. 파일명이 구코드(03_B1100-10·
   10_B1100-11)인 코드충돌 재부여 잔재라 파일명 매칭이 못 잡던 것(3배치 반전①의 변형).
   예시행 클리어+양식번호 정정으로 정비 — 신규 창작 0.
2. **[반전] 마스터 "빈 틀" 전제가 3종에서 반대** — B2100-04(19년~ 실기록)·B2100-05(4,443행)·
   F1100-04(144행). 직접 주입 시 실기록 훼손 → 헤더+빈 블록 **추출본 채택**(1배치 AM 추출 선례 연장).
3. **[발견·선례] 검증 환경 재구성** — 마이그 체인만으로 재구성한 DB가 라이브 복사본 audit와 완전
   동치(301/203/91/열람형 7). 라이브·브리지 없이 배치 검증 성립.
4. **[함정 선제 수정] 추출형의 데이터 영역 세로 병합**(F1100-04 A7:A13)은 grid 주입을 가림 —
   추출 시 병합 해제. 후속 추출형 배치 체크 항목.
5. **[데이터 발견] L4101-01 / L-4101-01 이중 등록 의심** — 5배치 착수 시 실측 확인(수정 보류).

열람형 전환 4종(L1200-08/09 계약서·B1100-07 계통도·B1100-08 이미지 기준표) = M1100-05 판정3
선례 재적용 이행(form_change_log 4행·📖·audit viewOnly 분리) — 검수요청 §3 사후 확인, 이의 시 반전.
계약서 2종은 선례 첫 '법률 문서' 하위유형이라 명시 확인 요청 동반.

---

## §0.9 추기 (7/29 — **5배치 완결: 진행 11·열람형 1** + 1·3배치 검수 소급 + 반전·발견 4건)

**5배치(정성품질 클러스터+잔여 상위 12종) 완결(마이그 0112 + gen-batch5-templates.mjs + e2e-batch5.mjs).**
E2E 11종 셀검증 **91/91** · grid 8종 회귀 8/8 · audit 작성가능 216→**227**·갭A 74→**62**·
열람형 11→**12**. 검수요청 = `5배치_검수요청_260729.md`. 검증 = 클라우드(4배치 동일)·라이브 무접근.

**검수 소급(브리지 복구, `검수회신_1_3배치_260729.md`)**: 1배치 실물 표본 3종 점검 → **최종 도장**
(발견 1건 = Rev.1 설계본 보조 시트 구코드 잔존 — 0112 편승 정정 완료) + 보류 '한계 수용' 회신(수용).
3배치 판정 2건 = 마스터 재실측 후 **승인**(열람형 6종·전용서식 2종). 4배치 판정 2건 = 자기검수
회피로 사무실/사장님 몫 명기.

실측 반전·발견:

1. **[반전] B2300 시트명 한 자리 코드**("B2300-1"~"-7") — resolveSheet 매칭 불가 → 시트명 2자리
   정정 추출 경로(마스터 무변경·엔진 무변경). 후속 체크 항목: 시트명 코드 자릿수.
2. **[해소] L4101-01 이중 등록 의심(§0.8-⑤) 해소** — L-4101-01 SPC 평가표 = 독립 .xls 별개 실체.
   .xls 구형 포맷 처리(변환 후 정본 완결)는 별도 판정 보류.
3. **[발견] "정성품질 순회 점검 시트(B2300-3)" 코드 중복** — B2300-03 과 같은 코드의 별개 양식
   (forms 미등록). 처리 방향 판정 요청(권고 = 신규 등록·코드 재부여, 6배치 편승).
4. **[반전] B2300-07 = 병합 블록 1건 서술형** — "사례 시트" 명칭과 달리 행 반복 대장 아님(문서형 설계).

열람형 전환 1종(B2300-01 CFT 실명 분장표) = M1100-05 선례 재적용 — 검수요청 §3 사후 확인, 이의 시 반전.
잔여 = 갭A 62(규정 본문형 37 + 양식형 25 — 6배치 선두 = J1100-14·J3100 4M·L3100 계측 기준·M4100 3정5S).

---

## §0.10 추기 (7/29 사무실 — **6배치 완결: 진행 17·열람형 10 = 잔여 양식형 전량 소진** + 반전·발견 6건)

**6배치(잔여 양식형 25 전량 + 편승 2) 완결(마이그 0113 + gen-batch6-templates.mjs + e2e-batch6.mjs).**
E2E 17종 셀검증 **106/106**(첫 실행 전 통과) · engine verify 전건 OK · grid 8종 회귀 8/8 ·
audit total 301→**302**(B2300-08 신규 등록) · 작성가능 227→**244** · 갭A 62→**36** · 갭B 0 ·
열람형 12→**22**. 검수요청 = `6배치_검수요청_260729.md`. 검증 = 사무실 라이브 복사본
(서버 마이그 체인 0102~0113 자동 적용) — 라이브 무접근·무손상.

**★잔여 양식형 갭A = 0. 갭A 잔여 36 = 전량 규정 본문형(원문 대기 별도 트랙) — 양식형 배치
트랙(1~6배치) 종결.** 선행 = 4·5배치 미결 판정 전량 회신(`검수회신_4_5배치_260729.md` —
사무실 봇 실물 재실측 + 사용자 결정 3건: 계약서 열람형 확정·B2300-08 신규 등록·SPC xlsx 변환).

실측 반전·발견(상세 = 검수요청 §2):

1. **[반전·신유형] J1100-14 = 좌면(빈 틀)/우면(작성 예시) 2면 배치** → 좌면만 추출하면 클리어
   없이 빈 틀 확보(예시·도면 이미지 17장 자연 분리).
2. **[발견] A8100-01 시트명 "A8100-1_"** — 자릿수 체크 항목 적중, B2300 정정 추출 선례 재적용.
3. **[발견] H3100-05 코드 중복 별개 양식**("고객만족도 평가서(실적)") — B2300-3 유형 재발.
   판정 요청(봇 권고 = 지침 개정 시 정리 — H3100-03 오기 추정).
4. **[경로 확립] Excel COM 저장 전면 불가 실측 → LibreOffice headless 변환 대체 성공** —
   .xls 구형 포맷 처리 표준 경로 후보(L-4101-01 적용).
5. **[반전] 기존 cell_map 5종 = 부실셀맵(자동 추출 잔재)** — M4200-01·M4100-01~04 전면 재설계.
   7/14 "부실셀맵5" 해소.
6. **[재적중] J3100-08 = 0065 설계본 파일명 구코드**(J3100-05) — 코드충돌 재부여 잔재 3번째.

열람형 10종(판단기준·절차도·기준표 2·구역도·서약서·책자형·계약/협정서 3) = M1100-05 선례
재적용 + 계약서 열람형 사용자 확정(260729) 직적용 — 검수요청 §3 사후 확인, 이의 시 반전.
판정 요청 3건 = ①열람형 10종(서약서 명시 확인) ②H3100-05 중복 처리 ③M4200-01 첫 시트 한정.
⚠️W4 설치판 재빌드 범위 **0113+**.
